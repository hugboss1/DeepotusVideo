# Montage « aussi poussé que Resolve » — conception depuis les deux inventaires (21/09/2026)

> Inventaires : `2026-09-21-davinci-resolve-inventaire.md` (chez eux) et
> `2026-09-21-montage-inventaire.md` (chez nous, relevé en réel sur 8799 +
> code). Équivalent chez nous : couche `frontend/patches/montage.js`
> (`window.DzMontage`, patcher `patch_bundle_montage.py`), bloc `sonvfx`
> (intouchable), `vfxrack.js`, `subs.js`, `backend/app/services/
> montage_service.py`, `effects_engine.py`, `sfx_service.py`,
> `subtitle_service.py`. **Décisions à valider par l'utilisateur AVANT tout
> plan et tout code.** Rien de ce qui existe n'est modifié : chaque ligne
> « Conservé » reste octet pour octet ; chaque « À faire » s'ajoute par un
> champ optionnel du payload (commande ffmpeg identique en son absence,
> règle R1…S1 déjà en vigueur), une section neuve du patcher `montage` (ou
> d'un maillon aval neuf) et un banc autonome.

## 0. Contraintes mesurées qui bornent les décisions

| Fait mesuré (21/09) | Conséquence |
|---|---|
| ffmpeg 8.1.1 essentials livré : `vidstabdetect/transform`, `deshake`, `minterpolate`, `xfade` (**58** transitions), `rubberband`, `arnndn`, `afftdn`, `sidechaincompress`, `loudnorm`, `ebur128`, `chromakey`, `zoompan`, `scdet`, `vectorscope`, `waveform`, `histogram`, `curves`, `colorbalance`, `selectivecolor`, `huesaturation`, `lut3d`, `tmix`, `deflicker`, `nlmeans/hqdn3d/atadenoise`, `xstack`, `drawtext` — TOUS présents ; encodeurs `libx264`, `libx265`, `h264_nvenc`, `hevc_nvenc`, `prores_ks`, `libvpx-vp9`, `libaom-av1`, `libopus`, `libmp3lame`, `gif` | stabilisation, retime à flux optique, 58 transitions, elastic wave, réduction de bruit audio/vidéo, ducking, loudness, incrustation fond vert, dynamic zoom, détection de plans, scopes, roues primaires, courbes, HDR-moins tiennent **sans aucune bibliothèque** |
| Python embarqué 3.13 : stdlib + Pillow, **pas de numpy, pas d'OpenCV, pas de whisper local** ; transcription = ElevenLabs Scribe ou OpenAI whisper-1 (payants) | tout suivi d'objet / visage, masque magique, voice isolation, super scale, IA « Neural Engine » est **écarté** ou reste une option payante fal existante ; le « calage du texte connu » reste notre voie gratuite |
| Le lot 2 du plan `2026-09-03-plan-montage.md` (D1 accord de couleur, D2 recadrage, D3 auto-clips, D4 titres + transitions, D5 EDL/FCPXML) est **écrit mais jamais construit** (aucun `color_match.py`, `reframe.py`, `edl_export.py` dans `main`) | ces cinq tâches sont reprises telles quelles dans les lots ci-dessous, pas réinventées |
| L'historique n'empile que `{clips, mixDb}` (`pushHistory`, `DZM_HIST_MAX=10`) | tout ajout de propriété de clip ou de piste passe par le même point d'écriture ; l'extension de l'historique est un préalable (D-0) |
| Le lecteur ne montre transitions/effets qu'après une Preview 480p ; la barre OUTILS flottante recouvre V2/V1 à sa place d'origine | deux irritants P0 sans dépendance |
| `effects_engine.build_chain` s'applique à un segment V1 ou au cadre final (post-pass) ; le Montage n'émet que par clip V1 | un « clip d'ajustement » = un post-pass borné t0/t1 : mécanisme existant à exposer |
| 4 formats `_CANVAS`, MP4 H.264/AAC 1080/30 seulement ; un job de rendu à la fois ; `/measure` rend LUFS/TP/LRA | la « page Deliver » est une table de presets + une file, pas un moteur neuf |

## 1. Tableau d'écart

Priorités : **P0** irritant ou préalable structurel · **P1** parité de montage
quotidien · **P2** finition (couleur, audio, stabilité) · **P3** confort ou
rare. « Conservé » = déjà là, on n'y touche pas.

### 1.1 Timeline, découpe, trim (page Edit / Cut)

| Chez eux (Resolve) | Aujourd'hui chez nous | Décision | Prio |
|---|---|---|---|
| Pistes V/A illimitées, ciblage, verrou, sync, réordonnancement | pistes dynamiques `vidéo` / `incrust.` / `audio` / S1, verrou 🔒︎, ▲▼ et ⋮, ordre = composition (`svmTracksOf`, P1/P14) | **Conservé** | — |
| Historique complet (toute action annulable) | `pushHistory` : clips + mix seulement ; pistes, durée, style S1, projet hors historique (dit dans chaque titre) | **À faire D-0** : `dzmHistPush(etat)` pur (clips, mixDb, tracks, dur, subsStyle, segments), même pile, `DZM_HIST_MAX` 10 → 50 ; les six titres « Ctrl+Z NE retire PAS » sont réécrits — **exécuté 21/09/2026** : `dzmHistSnap` / `dzmHistApply` purs, l'instantané porte SEPT clés (`clips, mixDb, tracks, dur, subsStyle, range, markers`), `DZM_HIST_MAX` 50, les six titres réécrits ; **écarts** : une clé ABSENTE du projet est portée par l'instantané et RESTAURÉE ABSENTE (`s[k]=p[k]` sans condition — sans quoi « Annuler » laissait la piste ajoutée sur le projet de démonstration, qui n'a pas de clé `proj.tracks`) ; la coalescence est de 600 ms sur la durée et de 600 ms sur le style S1, donc deux gestes plus rapprochés que cela ne font qu'un seul pas d'annulation | P0 |
| Barre d'outils fixe au-dessus de la timeline | barre OUTILS flottante, origine (246,595) = SUR V2/V1 | **À faire D-1** : origine sous le transport et à droite des en-têtes, hors des pistes (`dzmTbGeo`) ; aimant conservé — **exécuté 21/09** : origine au-dessus du transport (`bottom:calc(100% + 8px)`) ; mesuré 1400 × 900 : barre y 446–529, transport 542, clip V2 cliquable ; **écart mesuré à 1400 × 720** : la barre dépliée (y 269–352) recouvre le pied du cadre de prévisualisation (y 114–354, zone du sous-titre incrusté) sans recouvrir aucune commande — elle reste repliable (O) et déplaçable ; à traiter si un usage sur petit écran le demande | P0 |
| Insert · Overwrite · Replace · Fit to Fill · Place on Top · Append at End · Ripple Overwrite (overlay au dépôt + boutons) | un seul mode : pose à la tête sur la piste choisie, la timeline s'allonge (`dzmAdd`, P10) ; « Remplacer la source… » (P6) = Replace à durée égale ; « Envoyer vers → Montage » | **À faire D-2** : `dzmInsertMode(clips, clip, mode)` pur avec 5 modes — insérer (ripple des suivants), écraser, ajouter en fin, poser au-dessus (piste vidéo libre suivante), écraser en ripple ; sélecteur dans le picker Bibliothèque et l'overlay du lecteur au dépôt (5 zones) ; Fit to Fill = Replace + `speed` calculé (C4 existe) — **exécuté 21/09/2026** : SIX modes (`ecraser, inserer, fin, dessus, ripple_ecraser, remplir`) dans `DZM_MODES`, cœur pur `dzmInsere` / `dzmInsereUn` / `dzmCarve` / `dzmPose`, rangée `DzmModeBar` dans le sélecteur d'assets, et `addAsset` passe désormais par `DzTracks.insere` ; **écarts** : `inserer` et `ripple_ecraser` ne rippent QUE la piste visée (choix assumé — le ripple de Resolve est lui aussi local à la piste) ; le jumeau audio n'est posé que s'il n'existe pas déjà (`twinPlan`), donc A1 peut se désynchroniser après un « insérer » ; le mode n'est PAS persisté et retombe sur « écraser » au rechargement ; Fit to Fill est le mode `remplir`, dont la vitesse est écrêtée à [0,25 ; 4] et l'écrêtage est DIT dans la note ; l'overlay au dépôt (5 zones) n'est PAS livré — seule la rangée du sélecteur l'est | P1 |
| Trim contextuel : Ripple · Roll · Slip · Slide, trim en lecture, asymétrique | bord gauche/droit = rogner/allonger (min 0,3 s), bord droit V1 + R = ripple ; pas de roll, slip, slide ; `srcIn` porté par les clips | **À faire D-3** : dans `clipDown`… non — le bloc est intouchable : couche aval `dzmTrim` qui intercepte `pointerdown` sur la jonction (roll = les deux bords, durée totale fixe), **Alt+glisser centre = slip** (déplace `srcIn` sans bouger start/end), **Maj+glisser centre = slide** (voisins compensent) ; curseur qui change ; fonctions pures `dzmRoll/Slip/Slide` bancées sous node — **exécuté 21/09/2026** : `dzmSlip` (Alt + glisser le centre, bornes fixes, `srcIn -= ds × vitesse`), `dzmSlide` (Maj + glisser le centre, les deux voisins compensent) et `dzmRoll` (Alt + glisser le losange de jonction, T3b sur `.svm-junc` pour que les coupes FRANCHES aient elles aussi une poignée), tous purs et rejoués depuis l'état du `pointerdown` ; **écarts** : la borne HAUTE du slip est inconnue à l'écran (`dzmPose` retire `srcDur` du clip posé, donc la couche n'a pas la longueur de la source et ne peut pas l'imposer) ; pas de curseur contextuel — c'est le `title` du clip qui énonce les trois gestes ; le roll ne vit que sur V1 (seules les jonctions V1 portent une poignée) ; pas de trim en lecture ; pas de trim asymétrique ; la borne de tête de source (`srcIn ≥ 0`) est tenue pour le slide et pour le roll | P1 |
| Trim editor A/B dans le viewer, compteurs, nudge | — | **À faire D-3b** (après D-3) : popover de jonction existant (transition) gagne deux vignettes A/B (`/strip` existe) et ±1 image — **exécuté 24/09/2026** (lot L7-A, T4-bis) : rangée `svm-abrow` dans le popover de jonction (état `transPop`, sections L7g1/L7g2), vignettes A = dernière image du plan gauche (`srcIn + durée×vitesse − 1/30`) et B = première du droit (`dzmAbSecs` pur), compteur « −1 ◀ ▶ +1 » (Maj : ×10) par `DzTracks.roll` avec `pushHistory` avant, `dzmAbRollDit` dit le roll partiel ; **écarts** : vignettes CÔTÉ NAVIGATEUR (`svmThumb`, `<video>` hors écran, clé « source@seconde » au 1/30 s), PAS de route `/frame` ni `/strip` ; `dzmRoll` refuse par tableau IDENTIQUE (le refus se lit sur `start`, pas sur un code) et borne sans un mot (0,3 s par plan) → roll partiel DIT par `dzmAbRollDit` ; rangée GRISÉE (E-12) si la jonction n'est pas vidéo/vidéo, pas absente ; 30 i/s EN DUR (`DZM_AB_IMG=1/30`), pas la cadence du preset | P3 |
| Smart indicator (coupe la plus proche de la tête) | aimant N sur bords/tête/0 ; ↑↓ coupe précédente/suivante | **Conservé** ; D-2 pose « à la coupe la plus proche » quand la tête n'est pas sur une coupe | — |
| Swap / shuffle de clips | glisser le centre | **À faire D-4** : Ctrl+←/→ sur un clip V1 sélectionné = échanger avec le voisin (`dzmSwap` pur) — **exécuté 21/09/2026** : Ctrl+← et Ctrl+→ échangent le clip sélectionné avec son voisin de CONTACT (≤ 0,1 s, `dzmVoisins`) en figeant les DEUX bornes extérieures du couple, si bien qu'un trou ou un chevauchement toléré reste au raccord INTÉRIEUR au lieu d'être absorbé ; **écarts** : la transition est une propriété du bord ENTRANT, donc un fondu DISPARAÎT si le plan qui le porte devient le premier de sa piste ; le jumeau A1 ne suit PAS (seule la piste du clip échangé bouge) ; et quand deux voisins sont à la même borne, `dzmVoisins` retient le `end` maximal à gauche — règle héritée, l'égalité exacte n'est pas tranchée | P3 |
| Marqueurs (couleur, titre, description, mots-clés, durée) et annotations | aucun marqueur ; losanges = transition / automation / position | **À faire D-5** : `proj.markers[] {t, dur?, color, title, note}`, M (déjà pris : muet) → **Maj+M** pose, panneau index (liste, aller-à, supprimer), rendus sur la règle ; exportés dans le JSON projet ; annotations **écartées** — **exécuté 21/09/2026** : marqueurs `{id, t, color, title, note}` (`dzmMarkerAdd` / `Remove` / `Update` / `Next`, `dzmMarkersFrom`), Maj+M pose ou retire à la tête, Ctrl+↑ et Ctrl+↓ vont au précédent et au suivant, Ctrl+M ouvre l'index (titre validé au blur, couleur au change), losanges sur la règle, persistés par `POST /save` et réassainis par `_save_record` ; **écarts** : pas de `dur`, donc pas de marqueur de PLAGE ; pas d'annotations ; pas de mots-clés ; l'espacement minimal est de 0,15 s avec une borne LARGE (`<=`) aux TROIS endroits qui la tiennent, si bien qu'un couple plus serré que 0,15 s est FUSIONNÉ à la restauration — c'est toujours le premier des deux qui reste | P1 |
| Timelines empilées / onglets, copier-coller entre timelines | projets nommés (ouvrir, dupliquer…), un seul ouvert | **À faire D-6** : copier/coller de clips (Ctrl+C/V) entre projets via presse-papiers local (`dz_montage_clipboard`) ; onglets **écartés** — **exécuté 24/09/2026** (lot L7-A, T2) : actions `copy` (Ctrl+C) / `paste` (Ctrl+V) repliées dans R_R1/R_R2, `dzmClipCopy` (copie profonde sans `id`/`transition`/`transition_s`/`src_history`, `srcOut` gardé) → `localStorage dz_montage_clipboard` `{v:1, at, clip}`, `dzmClipPaste` → `dzmInsere` en mode courant, rubrique « Édition » du menu ☰ ; **écarts** : UN SEUL clip (la sélection est simple, `selId`) ; Ctrl+C/Ctrl+V LIBRES (seule Ctrl+Maj+C est réservée) ; garde `getSelection` (L7b3) : un texte sélectionné hors champ garde son Ctrl+C natif ; refus « source » (clip de démo sans `src`, jamais rendu) ; piste cible = la piste du clip si elle est DU GENRE, sinon celle du genre au PLUS PETIT rang (v1 avant v3 : l'ordre d'écran est t1, v3, v2, v1…) ; `srcDur` inconnu du presse-papiers (« remplir » à vitesse ×1) ; `start` RÉEL du clip posé dit (en fin / ripple / remplir le posent ailleurs qu'à la tête) | P3 |
| Double timeline (Cut) | zoom 4 crans + Ctrl+molette, `− total +` | **Conservé** ; **À faire D-7** : mini-carte de la timeline entière (30 px) au-dessus de la règle, clic = centrer (`dzmMinimap` pur sur les clips) — **exécuté 23/09/2026** (sorti de L7, exécuté dans le lot E-B : `3140613`, revue `7b584f6`) : `dzmMinimap` pur (lignes par piste via `dzmKindOf`, rects en fractions, clips hors durée exclus), `DzmMinimap` sans hook, hôte EB8a/EB8b (`mmView` sur scroll et zoom) ; **écarts** : posée **hors zoom** AVANT `.svm-scroll` (dans `.svm-lanes` elle suivrait le zoom), planchers +30 px (386), clic = centrer dans l'hôte (gouttière 88 px déduite car DANS `.svm-lanes`), jetons réels `--c-video`/`--c-audio` ; rect 1,75 px à 8 pistes (`top:0;bottom:0` si trop fin) ; couleur par piste (`t.c`) ignorée ; fenêtre périmée après resize/drag inspecteur (`[zoomPct,inspW,mmCalc]` si voulu) ; rendu par événement scroll = motif maison | P2 |
| Boring detector (plans trop longs, jump cuts) | — | **À faire D-8** : `dzmBoring(clips, maxS, minFrames)` pur → surlignages gris/rouge sur V1, seuils dans un popover — **exécuté 24/09/2026** (lot L7-A, T3) : `dzmBoring(clips,{maxS:8,minFrames:12,fps:30})` pur → `{id:"long"|"jump"}`, `data-boring` sur `.svm-clip` (L7c1 repliée dans R_AJ6A), liserés gris pointillé / rouge dans `montage.css`, popover « Plans trop longs / jump cuts » (☰ › Affichage, L7c3), réglages `dz_svm_boring` ; **écarts** : V1 SEULEMENT ; jump = MÊME source (`dzmSrcKey`) en CONTACT (tolérance `dzmVoisins` 0,1 s) et écart de source `< minFrames/fps` à epsilon, en comptant la VITESSE du plan gauche ; deux IMAGES identiques en contact = jump quel que soit `srcIn` ; le jump PRIME sur le long (un seul attribut) ; `long` strict (8 s exactement n'est pas long) ; `fps` 30 implicite, pas la cadence du preset (banc croisé [5], daté) ; popover délégué par la garde de `popover()` ; clamp à chaque frappe (style maison, pas de champ libre) | P3 |
| Source tape, sync bin, multicam, Multi Source | — (E1 du plan) | **Écarté** : sources générées 1080p, jamais multi-caméra | — |
| Adjustment clips (effet sur tout ce qui est dessous, borné) | effets par clip V1 avec t0/t1 ; post-pass global existe dans `build_chain` mais non exposé au Montage | **À faire D-9** : piste `ajust.` (kind `adjust`) dont chaque clip porte une pile d'effets appliquée en post-pass sur `[t0,t1]` (`_build_montage_command` : champ optionnel `adjust_clips`) ; même rack `DzVfx.Stack` | P1 |
| Raccourcis entièrement personnalisables, presets Premiere/FCP/Avid, export | 37 actions remappables, stockage local, recherche | **Conservé** ; **À faire D-10** : preset « Resolve » (JKL, I/O, Ctrl+B lame, Ctrl+T transition) + export/import JSON du mappage — **exécuté 24/09/2026** (lot L7-A, T1) : `DZM_KM_PRESETS.resolve`, `dzmKmPreset/Export/Import` purs, trois boutons « Preset Resolve / Exporter… / Importer… » dans le panneau « ? » (L7a3), fichier `{version:1, keymap}` (`deepotus-raccourcis.json`), action NEUVE `trans_add` (fondu à la coupe du plan sélectionné, L7a1/L7a2 repliées dans R_R1/R_R2, rubrique Timeline du menu ☰), 48 actions ; **écarts** : **Ctrl+T et Ctrl+Maj+T sont RÉSERVÉES par le navigateur** (`SVM_COMBO_RESERVED`) → `trans_add` = **Alt+T**, aucun Ctrl+T reproductible ; `Backspace` n'est pas canonisable (≡ Suppr, déjà le défaut de `delete`) ; preset MESURÉ = `{range_out:"O", toolbar:"Alt+O", blade:"Ctrl+B"}` — `toolbar` déplacée parce que `svmKmMerge` IGNORE un override qui vole la touche d'une action non remappée (O était celle de toolbar) ; `trans_add` refuse sans coupe à gauche, sans clip V1 sélectionné, ou V1 verrouillée ; import : collisions DITES `{id, raison:"collision", avec}` (la fusion est rejouée à l'import, banc croisé [1] sous node), la note d'import compte les ignorés sans nommer la raison | P3 |
| I / O points d'entrée-sortie, marquage audio/vidéo séparé | in/out = bords du clip ; pas de plage de timeline | **À faire D-11** : plage I/O sur la règle (`proj.range`), utilisée par D-2 (Fit to Fill), par « couper la sélection » (P3 existant) et par le rendu partiel (D-30) — **exécuté 21/09/2026** : plage `proj.range` = `{in, out}` (`dzmRangeSet` / `dzmRangeFrom` / `dzmRangeLen` purs, barre `DzmRangeBar` sur la règle), I et U posent l'entrée et la sortie, X efface la plage, Maj+X coupe la plage en ripple, plage persistée par `POST /save` et assainie par `_save_record` ; **écarts** : quand la plage ne CHANGE pas, la poser sort TÔT sans écrire ni empiler un pas d'historique (visible sur le projet de démonstration, qui n'a aucune clé `range`) ; une demi-plage (entrée seule ou sortie seule) est NOTÉE mais ne dessine pas de bande et n'allume pas « remplir » ; « couper la plage » rippe TOUTES les pistes non verrouillées et non bouclées (`dzmCutOpts`), pas seulement la piste ciblée | P2 |

### 1.2 Lecture et viewer

| Chez eux | Chez nous | Décision | Prio |
|---|---|---|---|
| JKL, images, plein écran, zones sûres, waveform overlay, scrub audio | J/K/L ×1×2×4, ← →, F, G, waveforms sur clips, vu-mètre | **Conservé** | — |
| Transitions et effets visibles en lecture | après Preview 480p seulement | **Adapté D-12** : lecture directe des `xfade` simples (fondu, noir, blanc) en CSS dans le lecteur (opacité), les autres restent « après preview » et le disent — **exécuté 22/09/2026** : `dzmVeil` + `.svm-xfveil` écrit par `liveSync` (voile noir/blanc/opacité triangulaire sur ±s/2 autour du raccord, cache `_dzVeil`) ; **écarts** : seuls `fade`, `fadeblack`, `fadewhite` (`_XFADE_LIVE`) ; pas de crossfade A/B (un seul hôte) : `fade` et `fadeblack` se voient pareil en direct ; à égalité d'alpha entre deux jonctions l'ordre du tableau tranche ; les sous-titres restent au-dessus du voile comme au rendu | P2 |
| On-screen controls transform (PIP), dynamic zoom (rectangles vert/rouge) | poignées overlay (déplacer, échelle, rotation, aimants), Aligner 9 positions, trajectoire (≤ 8 points, linéaire) | **Conservé** ; **À faire D-13 Dynamic zoom** sur un clip V1 : deux rectangles début/fin dans le lecteur → `zoompan` (ou `crop+scale` par expression `t`) ; champ optionnel `dz:{x0,y0,w0,x1,y1,w1}` ; ease douce/linéaire — **exécuté 23/09/2026** : `_dz_spec` (clamp w ∈ [0,1, 1], x/y ∈ [0, 1−w], plein cadre aux deux bouts = aucun zoom) + `_dz_filter` = UN `zoompan` à d=1 posé APRÈS `fps=` et avant `format=`, hôte `DzmPlanProps` + rectangles `DzmDzRects` dans le lecteur, payload `o.dz` (DZ4), banc l3 [1] + édition [14] + mutations 0–2, 13–14, 22 ; **écarts** (tous mesurés) : `zoompan` et non `crop+scale` (`crop` n'accepte `t` que dans x/y, `scale:eval=frame` fige la taille — démenti ffmpeg 8.1.1) ; zoompan APRÈS `fps=` (avant, il double la durée sur un setpts) ; temps par `it`, jamais `t` ; fenêtre CARRÉE en fraction du cadre (pas de ratio libre) ; ease `doux` (smoothstep) / `lin` seulement, pas de bézier ; zoom en direct = `transform` CSS sur la `<video>` (approximation, le 480p fait foi) ; la lame du bundle hérite `dz` tel quel sur les deux moitiés (saut au raccord, non corrigé — seule `dzmRampe` garde la continuité) | P1 |
| Keyframes (losange) sur TOUT réglage, éditeur de courbes sous le clip, ease bézier, loop/pingpong | trajectoire x/y/rot (overlay), automation dB (audio, 12 pts), bornes + rampe d'effet (10 courbes) | **Adapté D-14** : étendre les points de trajectoire à `scale` et `opacity` (payload `motion_points[].scale/opacity`, `_mp_lerp_expr` généralisé) ; pas d'éditeur de courbes générique — les rampes d'effet gardent leurs 10 courbes — **exécuté 23/09/2026** : `_motion_points` rend des 6-uplets (scale 0,05..3, opacity 0..1), échelle animée = `pad` rgba + `zoompan` en horloge `it` sur une toile fixe, opacité animée = `sendcmd` `[expr]` sur `colorchannelmixer@mpo<j> aa` ; client `dzmMpLerp2`/`dzmMpKeep` (KF1–KF5, KF2c), banc l3 [4] + édition [17] + mutations 8–10, 19 ; **écarts** (tous mesurés) : candidat A (`sendcmd`→`scale`) DÉMENTI — `overlay` garde la taille INITIALE de sa 2e entrée (100 px à t=0,2 et 2,5) → candidat B : média ré-agrandi depuis owmin (net jusqu'à ~2× smax/smin, flou au-delà), `smin` relevé à smax/10 avec warning (zoompan clampe z à 1..10) ; toile bornée `fh = min(2·fw, 3·h)` (rognage vertical invisible sans rotation ; avec `rotate` + média très haut les coins tournés peuvent manquer) ; opacité par `sendcmd` `[expr]` UNE commande par segment (`v0+(dv)*TI`, JAMAIS de virgule) + plate finale — remplace l'échantillonnage `_RAMP_STEP` du plan (28 824 car. à 30 s dépassaient le plafond CreateProcess) ; horloge LOCALE de l'overlay ; pas d'éditeur de courbes ; le commentaire R4b du bundle (« l'ÉCHELLE ne se keyframe pas ») est périmé, non réécrit (section d'un lot antérieur) | P2 |
| Retime : rampes de vitesse par courbe, optical flow / frame blending / nearest, Speed Warp | vitesse constante 25…400 % sur V1 (setpts), aucune interpolation | **À faire D-15** : qualité `retime:"nearest"|"blend"|"flow"` (`tblend` / `minterpolate=mi_mode=mci`) sur V1 ; **rampes** = « diviser le clip à la tête puis vitesses différentes » (lame existante) + option « lisser la jonction » ; Speed Warp **écarté** (IA) — **exécuté 23/09/2026** : `_RETIME` {blend, flow}, `_v1_retime` (ignoré sans vitesse), `dzmRetimeOf` + `dzmRampe` (RT1/RT2), banc l3 [2] + édition [15] + mutations 3–4, 15 ; **écarts** (tous mesurés) : `blend` = `tblend=all_mode=average,setpts=PTS-STARTPTS` APRÈS `fps=` (avant : tout flou et 1re image consommée) ; `flow` = `minterpolate=fps=…:mi_mode=mci:mc_mode=aobmc:me_mode=bidir:vsbmc=1` AVANT `fps=`, `scd` au défaut (pas `scd=none`) ; rampe par DIVISION à la tête seulement (deux vitesses, bornes 0,3 s), pas de courbe ; « lisser la jonction » NON livré ; Speed Warp écarté ; continuité du zoom au raccord assurée côté client (`cl` sans arrondi, `srcIn` à l'ancienne vitesse) | P2 |
| Stabilisation (camera lock, zoom, crop, smoothing, strength) | — | **À faire D-16** : propriété de clip V1 `stab:{smooth, crop, zoom}` → passe 1 `vidstabdetect` (job en tâche de fond, cache par source comme `/proxy`), passe 2 `vidstabtransform` dans la commande ; bouton « Stabiliser » + curseurs — **exécuté 23/09/2026** : `_v1_stab` (smooth 1..100, zoom −30..30, crop keep|black), `montage_media.stab_detect` (.trf par source, verrou anti-double analyse, timeout `max(900, 3×dur+60)`), routes `POST/GET /stab` (provider `montage_stab`), section client `dzmStabNorm`/`dzmStabState` (SB), banc l3 [3]+[6] + édition [16] + mutations 5–7, 18 ; **écarts** (tous mesurés) : analyse `vidstabdetect` sur la source ENTIÈRE (coût au premier rendu, cache `.trf` jamais purgé, comme le proxy) ; entrée lue entière puis `trim` dans la chaîne (pas de `-ss`, le .trf est indexé par image d'entrée) ; chemin `'C\:/…'` par `_ff_escape_path` ; pas de « camera lock » ; `crop` keep|black seulement ; l'analyse est demandée par un bouton « Analyser » et suivie par le job (`montage_stab` exclu de list_jobs et du coût) ; pas de GET /stab au montage ; `/render` lance lui-même l'analyse manquante | P2 |
| Correction d'objectif | — | **À faire D-17** : effet `lensdistort` existe (catalogue) → **Conservé** ; renommer la fiche « Distorsion d'objectif » avec un preset « corriger » | P3 |
| Smooth Cut (gomme un jump cut par flux optique) | — | **À faire D-18** : transition `smooth` = `minterpolate` sur 0,2 s autour de la jonction (segment court) ; mesurée sur banc ; si le résultat est mauvais, écartée et dite | P3 |
| Picture in picture Resolve FX (taille, position, coins arrondis, ombre) | overlay piste incrust. : x/y/échelle/rotation/opacité | **Conservé** ; **À faire D-19** : `radius` (coins, via `geq`/masque alpha) et `shadow` sur overlay — **exécuté 24/09/2026** (lot L7-A, T5) : `_ov_transform` gagne `radius` (entier 0..200) et `shadow` (0|1), `geq` après l'opacité et avant la rotation, ombre en cinq maillons (`split`, pad 3u, noir 55 % pad 4u PUIS `boxblur u`, `overlay=0:0:format=auto`), inspecteur « Coins (px) » / « Ombre portée », aperçu vivant (`border-radius`/`box-shadow`), `renderPayload` émet `o.radius`/`o.shadow`, ONZE sections L7e (svmOvTfAt, liveSync, geste, reset compris) ; **écarts** : le `min()` de ffmpeg est à DEUX arguments → `min(R,min(W/2,H/2))` ; rayon et pads à l'échelle `k = w/(1080|1920)` (référence client, ≥ 1 px) ; ombre 55 %, canvas +6u (image à 3u, ombre à 4u, flou u), `format=auto` ; **coût mesuré : `geq` ≈ 0,105 s/image en 1080p (×29 la chaîne nue)** — masque alpha statique (`alphamerge`) noté pour L7-B ; radius/shadow IGNORÉS (warning) sur la chaîne cover et sur l'échelle animée ; un rayon SEUL rend `tf` plein cadre (chaîne transformée) ; keyframes : coins et ombre STATIQUES ; aperçu approximatif quand le cadre n'est pas mesurable ; la demi (`.5`) arrondit à 1 sous JS et à 0 en python (banc croisé [3], sans effet : la couche émet un entier) | P3 |

### 1.3 Transitions, titres, graphiques

| Chez eux | Chez nous | Décision | Prio |
|---|---|---|---|
| > 100 transitions, aperçu au survol, durée par glisser | 7 (`SVM_TRANS`, `_XFADE`), losange glissable, « à toutes les coupes » | **À faire D-20** (= D4 du plan, transitions) : exposer les **58** `xfade` en galerie par familles (fondus, glissements, volets, zooms, pixels, formes) avec vignette animée générée une fois par ffmpeg (paire de couleurs), `_XFADE` étendu, `SVM_TRANS` inchangé (couche aval ajoute) — **exécuté 22/09/2026** : `_XFADE` 58 + familles + `GET /transitions`, galerie `DzTracks.TransGrid` par familles avec aperçu animé CSS par famille (pas de vignette ffmpeg), `<select>` de l'inspecteur, `svmTransLabel` lit le catalogue ; **écarts** : durée 0,1–1 s conservée ; `crossfade`/`xfade` hérités acceptés ; `fadewhite` sans voile dans l'aperçu (le voile blanc du bundle est accroché à `flash`) ; galerie reconstruite à chaque cran du curseur (perf non mesurée) ; popover bornée 52vh (mesuré 399 px dans le volet) ; `distance` classé en fondu | P1 |
| Titres 2D (Text, Text+), lower thirds, > 100 Fusion titles, style par caractère, emoji, navigateur de polices | S1 sous-titres (16 fontes, 9 préréglages, karaoké, fondu/pop) ; narration ; aucun clip « titre » | **À faire D-21** (= D4 titres) : piste/clip **Titre** (kind `title`) rendu en ASS (même moteur, `\pos/\an/\t`) avec 8 gabarits animés dans la charte (titre plein cadre, tiers inférieur, légende, compteur, chapitre, citation, hashtag, CTA) ; champs texte/police/taille/couleur/position/entrée-sortie ; **pas** de style par caractère ni de 3D — **exécuté 22/09/2026** : `titles.py` (8 gabarits ASS `plein_cadre, tiers_inferieur, legende, compteur, chapitre, citation, hashtag, cta`, mesurés à l'image, `font_line_height` appliqué, repli par largeur mesurée), piste `t1` genre `title` gravée avant S1 (`titles_ass` `[tt{j}]`), routes `/titles` et `/title-preview` (cache borné), `Maj+T`/chip T+/« + » de T1, `DzTracks.TitleInspector` (8 cartes PNG, texte/sous-texte/couleur/police/taille), aperçu vivant `.svm-livetitle` sous le voile ; **écarts** : position et entrée/sortie portées par le GABARIT (pas de champ libre) ; pas de style par caractère, ni 3D, ni emoji, ni navigateur de polices au-delà des 16 embarquées ; boîte OPAQUE (la conception disait `&H60`) et contour encre ; un titre qui dépasse la fin de V1 est coupé par `-t total` (Resolve allonge la timeline) ; aucun `@font-face` pour les 16 fontes → l'aperçu vivant ne montre que le gabarit (ni couleur, ni police, ni taille) — la Preview 480p fait foi ; piste `t1` unique (pas de titres empilés) et déplaçable sous V2 à l'écran alors que le rendu grave au-dessus ; `dzmTtTpl` mutile un nom de gabarit au lieu de le refuser (saisie non libre) ; `_spec_apercu` borne l'aperçu à 10 s ; un carton dont le texte est vidé est refusé à voix haute et le titre précédent conservé ; `sub` sans `text` est perdu | P1 |
| Subtitle generator, pistes multiples = langues, export TTML/SRT/VTT, import TTML/XML | S1 unique, traduction remplace le texte, import .srt/.vtt, export .srt/.vtt/.txt/.ass | **À faire D-22** : pistes S2… (kind `subs`, `lang`), une seule **gravée** (bascule œil), les autres exportées ; TTML export **écarté** (aucun consommateur chez l'utilisateur) — **exécuté 24/09/2026** (lot L7-A, T6) : `dzmSubsTracks/New/Burn/BurnId/Copy` purs, menu de piste (« Graver cette piste au rendu », export .srt/.vtt/.txt, « Nouvelle piste de langue… »), tiroir : « Traduire vers → dans une nouvelle piste » (M26a, `onNewTrack`), `subsPayload()` et `subsOverlay()` suivent la piste gravée (L7f1/L7f5), `data-sub`/`data-hidden` par GENRE et `data-burn` calculé (L7f2/L7f6), `svmTracksPayload` émet `lang/burn/name` ; **écarts** : S2… = COPIE traduite ou vide, NON éditable dans le tiroir (l'éditeur, le verdict, la couverture et les emojis restent sur s1) ; segments = clips `tr:"s2"` ; `data-burn` CALCULÉ par `subsBurnId` (s1 par défaut, jamais lu sur `tr.burn` seul) ; `window.prompt` pour la langue (dialogue natif) ; « gravée » en colonne combo ; les `words` (karaoké) ne sont PAS copiés ; le backend range `tracks` tel quel et `_tracks_meta` IGNORE `lang/burn/name` (banc croisé [4]) ; TTML toujours écarté | P2 |
| Lottie / OGraf HTML, PSD multi-calques | images PNG/JPG/WebP, sprites VFX (Spritelab) | **Écarté** (Lottie = rendu hors ffmpeg) ; les feuilles de sprites existantes couvrent l'animation | — |
| Fusion : nœuds, particules, trackers, roto, 3D, scripting | nœud Effects du Studio (hors Montage), VFX particules locaux, Vectorlab/Spritelab | **Écarté** dans le Montage (le Studio est notre page « Fusion ») | — |

### 1.4 Audio (page Fairlight + Edit)

| Chez eux | Chez nous | Décision | Prio |
|---|---|---|---|
| Niveau, pan, pitch, EQ 6 bandes par clip ; fader/mixer par piste ; vu-mètres | gain, fondus + courbes, vitesse ×0,5–2 (atempo), automation 12 pts, bus dialogue/musique/sfx, faders d'en-tête, vu-mètre gradué ; fx chain : filter, eq3, denoise, deesser, compressor, distortion, echo, reverb, stereo, normalize | **Conservé** ; **À faire D-23** : `eq` 6 bandes (`anequalizer`, ou 6 × `equalizer`) et `pan` (`pan=`) ajoutés au vocabulaire `sfx_service` (types nouveaux, ordre `_FX_ORDER` étendu) ; pitch **écarté** (rubberband n'est pas requis : atempo conserve déjà la hauteur) | P2 |
| Ducking automatique (source dialogue → cible musique) | ducking auto avec presets/attaque/retour/seuil | **Conservé** | — |
| Normalisation, loudness meters aux normes (bleu/jaune/rouge), analyse hors ligne | `normalize target_lufs` par clip ; `POST /measure` ebur128 (LUFS I / TP / LRA) affiché « dernière mesure » | **À faire D-24** : cible de programme (`-14` YouTube/TikTok, `-16` podcast, `-23` EBU) dans le popover de rendu → `loudnorm` 2 passes sur le mix final (champ optionnel `loudness`), pastille tricolore sur la mesure — **exécuté 23/09/2026** (lot L4, T1–T4) : cibles `_LOUD_TARGETS` (−14/−16/−23), passe 1 = le graphe audio de `/measure` avec `loudnorm=…:print_format=json` (`_loudnorm_pass1`, sync ≤ 180 s, `_loudnorm_parse` prend le DERNIER bloc JSON), passe 2 = `[outa]loudnorm=…measured_*:linear=true,aresample=48000[outn]` mappé à la place de `[outa]` (`_loudnorm_chain`, la mesure est OBLIGATOIRE sinon ValueError) ; select « aucune / −14 / −16 / −23 » + pastille `span.svm-loudpill[data-etat]` (`dzmLoudPastille` : vert ≤ 1 dB, jaune ≤ 3, rouge, gris sans mesure) ; **écarts** : la passe 1 porte sur la PLAGE quand `range` est posé ; délai de passe 1 FIXE à 180 s ; un mix MUET (`input_i` = −inf) rend None → aucun loudnorm (pas de clamp à −99, qui amplifierait un bruit de fond de +85 dB) ; le GIF n'a pas de passe 1 (mix jeté dans `anullsink`) ; `/measure` reste ebur128 ; loudness ignorée en aperçu ; la pastille est une mesure de SESSION, jamais persistée ; **course de threads mesurée** : `-filter_complex_threads 1` ET `-threads 1` (décodeurs) posés seulement quand `vidstabtransform` ET `minterpolate` sont dans le graphe (graphe seul sous charge 3/6 plantés, les deux drapeaux 0/8) | P1 |
| Noise reduction (auto / learn), de-esser, de-hummer | denoise (`afftdn`), deesser ; pas de de-hum, pas de « learn » | **À faire D-25** : `dehum` (notch 50/100/150 Hz via `bandreject`) ; « apprendre le bruit » = `afftdn` avec `sn` sur une plage I/O (D-11) ; Voice Isolation IA **écartée** | P2 |
| Enregistrement voix off / multipiste, ADR, Foley sampler | narration TTS ElevenLabs par bloc | **À faire D-26** : bouton **Enregistrer** (MediaRecorder du navigateur → `POST /videos/upload` provider `ugc` → clip A1 à la tête, prises empilées comme clips successifs) ; ADR/Foley **écartés** | P2 |
| Elastic wave (retime sans changer la hauteur) | atempo (hauteur conservée) sur ×0,5–2 | **Conservé** (équivalent) | — |
| Track layers, bounce, external processing, VST | — | **Écarté** (hors périmètre desktop) | — |
| Sound library intégrée (audition, sync à la tête) | tiroir Sons : 606 CC0, favoris, import, génération | **Conservé** | — |
| Sync scrollers, index (clips/pistes/marqueurs), dossiers de pistes | — | D-5 fournit l'index des marqueurs ; le reste **écarté** | — |
| 2 000 pistes, FlexBus, 3D audio | 3 bus fixes, N pistes | **Écarté** | — |

### 1.5 Couleur (page Color)

| Chez eux | Chez nous | Décision | Prio |
|---|---|---|---|
| Roues Lift / Gamma / Gain / Offset + master, barres, log | grade_basic : exposition, contraste, saturation, température ; 10 presets ; LUT .cube ; « à tous les plans » | **Conservé** ; **À faire D-27** : effet `wheels` (`colorbalance` rs/gs/bs · rm/gm/bm · rh/gh/bh + `lumakey`-free master `eq`), UI trois roues (canvas maison, pas de dépendance) dans le rack — **exécuté 24/09/2026** (lot L5, T1/T4/T5) : effet `wheels` lift/gamma/gain par canal, neuf curseurs visibles au rack (repli d'un clip d'ajustement D-9), trois roues + maître dans le panneau « Étalonnage » ; **écarts** : `colorbalance` DÉMENTI par la mesure (sur un gris 128, `rs=0.3` ne bouge rien : ombres seules) → `lutrgb` `255·clip(L+(val/255)·(G−L),0,1)^(1/Γ)` (écart ≤ 1 niveau au calcul) ; `eq gamma_r/g/b` agit en YUV, écarté ; pas d'Offset séparé (le lift en tient lieu) ; bornes catalogue == cœur client (banc croisé) | P2 |
| Auto color, Shot Match, color chart match | — (D1 du plan écrit, jamais construit) | **À faire D-28** = **D1 du plan** tel quel (stats YCbCr PIL, `lutyuv`, bouton « Accorder sur le plan précédent ») ; auto color = même module vers des cibles neutres (moyenne 128/128, Y étalé) ; chart **écarté** — **exécuté 24/09/2026** (lot L5, T1/T3/T5) : `color_match.py` + effet `colormatch` (six curseurs) + `POST /api/montage/color-match` (ref ou auto), boutons « Accorder sur le plan précédent » / « Auto » du panneau ; **écarts** : accord PIVOTÉ `(val−128)·G+128+O` (la forme `val·G+O` teintait un gris neutre dès G ≠ 1 : U 128 → 192 à G 1,5) ; stats PIL converties en plage LIMITÉE (`lutyuv` voit du yuv420p limité, 0x808080 → Y 126) ; gain réduit quand O sortirait de ±128 (priorité à la moyenne) ; les deux `frame_stats` en parallèle | P2 |
| Courbes (custom RGB/Y, Hue vs Hue/Sat/Lum, Lum vs Sat, Sat vs Sat) | `curves` par presets ffmpeg dans les grades | **À faire D-29** : effet `curves` à points (`curves=r='x/y …'`) avec éditeur canvas ; Hue vs Sat / Lum via `huesaturation` par plage de teinte (6 plages) ; les autres **écartées** — **exécuté 24/09/2026, PARTIEL** (lot L5, T1/T4/T5) : effet `curves` à points (`pts_m|r|g|b` CACHÉS au rack, 16 points au plus, `interp=pchip`) + éditeur canvas M/R/G/B ; effet `huesat` (teinte, saturation, force, bande de couleur) ; **écarts** : seul « Hue vs Sat » est couvert (une bande à la fois, `strength` 10 pour désaturer tout à fait) — Hue vs Hue / Lum vs Sat / Sat vs Sat **écartés** ; RÈGLE UNIQUE des courbes (`curves_clean` == `dzmCurveClean`) rejouée sur 61 vecteurs partagés + 40 tirages + 6 cas limites, 0 divergence (banc croisé) | P3 |
| Qualifier (HSL), Power Windows (formes, softness), tracker | moteur Mask du Studio par région (non exposé au Montage) | **À faire D-30** : masque **statique** par clip (rectangle / ellipse, adoucissement, intérieur/extérieur) limitant la pile d'effets — `build_chain` sait déjà appliquer à une région ; qualifier HSL = `colorkey`/`chromakey` en masque ; **tracking écarté** (pas d'OpenCV) — **exécuté 24/09/2026** (lot L5, T2/T4/T5) : champ de clip `mask` (rect/ellipse, adoucissement, inversion) sur V1 et V2, module pur `mask_region.py`, contour dessiné sur le lecteur ; **écarts** : « `build_chain` sait déjà appliquer à une région » était FAUX (le Mask du Studio vise une couche entière) → masque `geq` calculé UNE fois puis bouclé (`geq` par image ×7, proscrit ; `drawbox` plafonné à 235) ; V1 = split → effets → `alphamerge` → `overlay=0:0:shortest=1` — `shortest=1` est une CEINTURE sous le `-t total` du rendu (mesure de clôture : sans lui, 50 images en 0,22 s, deux plans en xfade intacts ; SANS `-t` le graphe ne finit pas) ; V2 = `maskedmerge` gbrap quatre plans à la taille de l'overlay ; les EFFETS DES OVERLAYS V2 n'étaient JAMAIS rendus (défaut préexistant) → rendus depuis L5, alpha d'origine gardé (copie opaque `lutrgb=a=255` + `blend c3_mode=multiply`, sinon alpha au carré), sauf `glitch` (48/255) et `grain` (7/255) ; qualifier HSL = `chromakey` (D-33) | P2 |
| Scopes : parade, waveform, vectorscope, histogram, CIE | — | **À faire D-31** : `GET /montage/scopes?src&t` → PNG `waveform` + `vectorscope` + `histogram` (ffmpeg, une image), panneau repliable sous le lecteur — **exécuté 24/09/2026** (lot L5, T3/T6) : `POST /api/montage/scopes {src,t,effects,mask}` → PNG 512×512 de l'image ÉTALONNÉE (`grading.py`), bouton « Scopes » dans la rangée du lecteur, rafraîchi à l'arrêt ; **écarts** : POST (la pile voyage dans le corps), pas GET ; `-ss` hors durée rend ZÉRO image avec rc 0 → durée du flux `v:0` (tag `DURATION` en mkv/webm), recul `max(0,1 ; 1/fps)` + un second essai ; sémaphore de 2 calculs, client parti → 499 sans ffmpeg ; la barre OUTILS flottante recouvrait TOUTE la rangée du lecteur (défaut préexistant) → rangée remontée en tête de zone, encart porté dans le cadre en `pointer-events:none` | P2 |
| Nœuds, groupes, versions, gallery de stills, lightbox, compare wipe | grade par clip, « à tous les plans » ; `/strip` (filmstrip) | **À faire D-32** : « copier / coller le grade » (Ctrl+Maj+C/V sur la pile étalonnage), **lightbox** = grille de vignettes V1 avec pile appliquée (vignettes `/effects/preview` existantes) ; nœuds/versions/wipe **écartés** — **exécuté 24/09/2026** (lot L5, T4/T6) : actions `grade_copy` / `grade_paste` (clavier, menu de clip, boutons du panneau), presse-papiers `dz_montage_grade`, lightbox MODALE (voile + grille des plans V1, vignettes `POST /api/montage/grade-frame`, trois requêtes en vol au plus) ; **écarts** : Ctrl+Maj+C RÉSERVÉ (inspecteur des DevTools) → **Ctrl+Alt+C / Ctrl+Alt+V** (non réservées, banc croisé) — AltGr AZERTY au clavier PHYSIQUE non prouvé ; vignettes par `grade-frame` (image étalonnée), pas `/effects/preview` (un seul effet) ; le bouton « à tous les plans » reste `grade_basic` seul (NON généralisé) ; un effet `off` n'est ni emporté ni posé | P3 |
| Resolve FX > 90 (beauty, restoration, light, sharpen, deband…) | 38 effets (grain, bloom, halation, vignette, glitch, blur, letterbox, dreamy…) | **À faire D-33** : 8 effets ffmpeg de plus, sans modèle — `denoise_video` (hqdn3d/nlmeans/atadenoise), `deflicker`, `deband`, `unsharp`/`cas` (fiche « Netteté » existante étendue), `chromakey` (fond vert : couleur, similarité, blend, `despill`), `tmix` (traînée), `monochrome`, `lenscorrection` ; face/beauty/object removal **écartés** — **exécuté 24/09/2026** (lot L5, T1) : `denoise` (hqdn3d | atadenoise), `deflicker`, `deband`, Netteté étendue (`sharpen` mode `unsharp` | `cas`, défaut octet pour octet inchangé), `chromakey` (+ `despill`), `tmix`, `monochrome`, `huesat` ; catégorie neuve « Correction » ; **écarts** : le catalogue comptait 39 entrées / 40 clés, pas 38 ; `lenscorrection` EXISTAIT déjà (`lensdistort`) ; `nlmeans` ÉCARTÉ (88 s pour 2 s de 720p) ; catalogue **+10 types** et non +9 (`wheels`, `curves`, `colormatch`, `huesat`, `monochrome`, `denoise`, `deflicker`, `deband`, `chromakey`, `tmix`) ; `chromakey` n'a de sens que sur V2 (alpha perdu en yuv420p sur V1, dit dans l'aide) ; `_c` hexadécimal strict (injection d'un `;[x]ab` fermée) | P2 |
| HDR, Dolby Vision, RAW, ACES, color management | — | **Écarté** | — |

### 1.6 Média, projet, livraison (pages Media / Deliver)

| Chez eux | Chez nous | Décision | Prio |
|---|---|---|---|
| Media pool, bins, smart bins, power bins, métadonnées 200 champs, notes ★, tags | Bibliothèque unifiée (996 vignettes, provenance, recherche, « Envoyer vers »), picker par piste | **Conservé** ; notes ★ / tags Good Take **À faire D-34** côté Bibliothèque (hors Montage, table `library_assets`) — **exécuté 24/09/2026** (lot L7-B, T7, `0285290`, `2c66209`, revue `84814da`, restes `e3f1d08`, `831b8bf`, `393b7b1`) : note 0–5 des RENDUS en base (`jobs.rating` par la migration existante `V1_2_NEW_COLUMNS` + `_auto_migrate`, `PUT /api/jobs/{id}/rating` ENTIER 0..5 strict — `"3"`, 3.0, 3.5, booléen, null refusés en 400 —, `_job_to_dict` rend `rating`, `Pipeline.list_jobs(min_rating)` filtre AVANT le `limit`, `GET /api/jobs?min_rating=`), cinq étoiles par ligne et chips « ★ 3+ » / « ★ 5 » (Good Take) dans le tiroir Médias du Montage, note optimiste puis PUT EN FILE par rendu, retour arrière sur la dernière note CONFIRMÉE, offset qui suit le compte serveur sous filtre, recharge depuis la page 0 qui attend les notes en vol (bornée à 15 s) ; transfert entre machines : colonne tolérée dans les deux sens (intersection des colonnes) ; **écarts** : note sur les RENDUS seulement — l'écran Bibliothèque et les images (`library_assets`) n'ont pas d'étoiles (plan Bibliothèque du 03/09, P1 `libmeta` ; la colonne est prête à y être lue), favoris localStorage `dz_fav_renders` NON migrés ; une ligne passée sous le seuil reste visible jusqu'au prochain rechargement (l'offset, lui, est juste) ; au-delà du plafond de 15 s la recharge part et un rendu dont le PUT est encore en vol GARDE ses trois mémoires (clôture) ; pin `__dzMontageAdd` de `test_library_sendto.py` réaligné 5 → 7 (écart E-3 du 22/09, antérieur au lot) ; sondes `libsend` / `etabli` à 5 non vérifiables sans leurs `.bak` (absents du worktree) ; banc NEUF `test_montage_l7b_notes.py` (et non la section [5] de `test_montage_l7b.py`) | P3 |
| Sync audio/vidéo par waveform ou timecode | « son du plan » extrait à l'import (P12) | **Conservé** ; sync par corrélation **écarté** (numpy absent) | — |
| Proxies, Proxy Generator | proxy 480p par source (`/proxy`, cache) | **Conservé** | — |
| Quick Export sur toutes les pages : presets YouTube/Vimeo/TikTok/X, ProRes, H.264/H.265, audio seul, presets maison | MP4 H.264 1080/30 ; preview 480p ; Rendre & publier → brouillon Scheduler | **À faire D-35** : table de **presets de sortie** (`_DELIVER`) — YouTube 1080p/4K H.264, Shorts/TikTok/Reels 9:16 H.264, X 720p, ProRes 422 (`prores_ks`), H.265 (`libx265`/`hevc_nvenc` si présent), WebM VP9, **audio seul** (AAC/MP3/WAV), GIF 480p ; fps 24/25/30/60 ; sélecteur dans le popover ; presets maison sauvegardés (`deliver_presets.json`) — **exécuté 23/09/2026** (lot L4, T1–T4) : table `_DELIVER` de DIX presets (master_1080, web_4k, social_720, prores422, hevc, webm_vp9, audio_aac/mp3/wav, gif_480), `_DELIVER_FPS` 24/25/30/60, `_deliver_dims`, `_deliver_resolve`, `_deliver_tail`, routes `GET/PUT /api/montage/deliver-presets` (`builtins` + `fps` + maison, fichier atomique à côté de `montage_saved`), rangée `DzTracks.DeliverRow` dans le popover (preset avec optgroup Standard/Maison, cadence, loudness, plage, « Enregistrer ce réglage… »), `renderPayload(preview,queue)` → `DzTracks.deliverPayload` hors aperçu ; **écarts** : un preset est une CLASSE DE TAILLE (`side` = PETIT côté 720/1080/2160, ratio du projet conservé, arrondi pair), pas « YouTube/Shorts/X » ; `hevc_amf`/`hevc_qsv` listés par ffmpeg mais échouent → sonde par ENCODAGE RÉEL d'une image (`_encoder_ok`), cache d'encodeur jamais périmé pendant la vie du processus ; l'audio seul COMPOSE quand même la vidéo (nullsink, `-vn` sur la sortie, `side` 480 borne le travail) ; `crf` maison ignoré sur prores/gif/audio ; preset inconnu → master ; gif/audio absents des listes `video=1` et de la Bibliothèque (non classés vidéo — `.gif`, `.m4a`, `.mp3`, `.wav`, banc croisé [4]) ; `range` accepte des chaînes numériques ; `prompt` natif pour le nom du preset maison (le Montage n'a pas de dialogue maison) ; `dzmDeliverOpts(api)` prend l'API entière (le plan disait la liste) ; GIF `gif_480` = 12 i/s, 480 de petit côté, palettegen/paletteuse, sans audio ; un preset maison de même id est REMPLACÉ en silence ; GET en panne → master seul ; la couche porte SES listes `DZM_DEL_FPS` / `DZM_DEL_LOUD` et ne lit pas `fps` de l'API (le banc croisé `test_montage_l4_croise.py` tient les listes égales) ; progression ffmpeg absente (le job passe 10 → 30 → 100) | P1 |
| Render queue (plusieurs jobs, batch, remote) | un job à la fois | **À faire D-36** : file locale = liste de `(projet, preset)` exécutée en série par le worker de jobs existant ; « Ajouter à la file » ; remote **écarté** — **exécuté 23/09/2026** (lot L4, T3–T4) : `queue:true` sur `/render` → job né `queued` (« En file », progress 0), `_RENDER_QUEUE` servie par UN worker asyncio (`_render_worker`, `_ensure_worker` recréé si la boucle change), réponse `{queued, position}`, bouton « Ajouter à la file » (sans `job` suivi : la vue Livraison suit `GET /api/jobs`, badge `dzmDelStatut`) ; **écarts** : ce n'est PAS le worker de jobs existant mais une file dédiée en série ; pas de priorité ni d'annulation ; worker non annulé au shutdown ; jobs `queued` orphelins jamais repris au redémarrage ; pas de plafond de file ; `_ensure_worker` sans await ; position par compteur `_RENDER_PENDING` (compte l'item que le worker tient — `qsize()` aurait rendu 1 au deuxième POST) ; `queue` + `preview` → 400 ; « Ajouter à la file » grisé pendant un rendu en cours | P2 |
| Résolutions verticales/carrées, 32K, 120 fps | 4 ratios, 1080 | **Conservé** ; D-35 ajoute 4K et 60 fps | — |
| Upload direct YouTube/TikTok/Vimeo/X | brouillon Scheduler (X, TG, YT, IG) validé à la main | **Conservé** (adapté : la validation humaine reste) | — |
| DCP, IMF, MXF | — | **Écarté** | — |
| Export EDL/FCPXML/AAF, import ATEM | — (D5 du plan écrit, jamais construit) | **À faire D-37** = **D5 du plan** (EDL CMX 3600 + FCPXML, spec relue d'abord) — **exécuté 24/09/2026** (lot L7-B, T1, `5d5eadc`, revues `562a048`, `140c3d5`) : service pur `edl_export.py` (`to_edl` CMX 3600 30 i/s NON-DROP en CRLF, bobine `AX`, `* SOURCE FILE`, V1 puis audio ; `to_fcpxml`), route `GET /api/montage/export?format=edl|fcpxml` (400 format inconnu, 400 sans timeline, durée des sources sondée pour les DEUX formats, `Content-Disposition`), ☰ › Projet « Exporter EDL… » / « Exporter FCPXML… » : sauvegarde PUIS export, téléchargé par `subsDownload` ; **écarts** : FCPXML **1.9** (enfant `media-rep`, doc Apple lue le 24/09 ; la courante est 1.14 — 1.9 est la plus ancienne forme à `media-rep`) ; import réel dans DaVinci Resolve / Final Cut **NON PROUVÉ** — à faire par l'UTILISATEUR ; le fondu EDL garde la forme CMX : il COMMENCE à la coupe et consomme une poignée du plan sortant, alors que le rendu pose l'xfade à `total − tau` et MANGE sa queue (pas les mêmes images pendant le fondu), `* HANDLES: insuffisantes (n images)` quand la durée sondée ne couvre pas la poignée ; `* FROM CLIP NAME` / `* TO CLIP NAME` = NOM DU FICHIER (clé de reconnexion de Resolve), le libellé passe dans `* CLIP LABEL` ; ligne `M2` ÉMISE pour la vitesse (le plan disait non ; la branche coupe franche n'était lue par aucune ligne de banc — trou trouvé par la campagne de mutations de clôture, comblé) ; vitesse audio, boucle, autres transitions, titres, sous-titres, ajustements, overlays, sources introuvables : commentaires `* SPEED … (non exportée)`, `* LOOP`, `* TRANSITION`, `* SKIPPED` ; arrêt à 999 événements (`* TRUNCATED`) ; FCPXML : transitions NON exportées (commentaire), `timeMap` pour la vitesse, audio ancré `lane="-k"`, commentaires XML neutralisés (`--` espacé), plans V1 qui se chevauchent : `start` avancé d'autant | P3 |
| Rendu partiel (in/out) | — | **À faire D-38** : rendu de la plage I/O (D-11) — champ optionnel `range` → `-ss/-t` sur la sortie — **exécuté 23/09/2026** (lot L4, T2–T4) : `_range_args(range_out, total)` → `-ss a` JUSTE AVANT `-t` recalculé (`min(b, total) − a`), case « Rendre la plage I/O seulement » (SEULEMENT si le projet a une plage, jamais persistée), `range:[in,out]` posé par `deliverPayload` ; **écarts** : rendu partiel = coupe de SORTIE (tout est décodé jusqu'à la fin de la plage, rien n'est encodé avant `a` — mesuré [1,2] sur 30 s : 0,51 s contre 5,38 s) ; sous-titres, titres et marqueurs gardent l'horloge globale ; la plage est validée sur le total ESTIMÉ (400 « plage invalide ») puis sur le total RÉEL (un maître plus court → job failed « plage », pas 400) ; la passe 1 loudness porte sur la plage ; l'aperçu rend aussi la plage | P3 |

### 1.7 Collaboration, IA

| Chez eux | Chez nous | Décision | Prio |
|---|---|---|---|
| Cloud, verrous, chat, marqueurs partagés, présentations, live save | autosauvegarde locale, projets nommés, export/import « transfert entre machines » | **Écarté** (mono-utilisateur local) ; **À faire D-39** : **comparaison de deux projets** (ajouté / supprimé / déplacé / rogné, pur `dzmDiff` sur les JSON) — gratuit, utile pour relire une version — **exécuté 24/09/2026** (lot L7-A, T4) : `dzmDiff(a,b)` pur → `{added, removed, moved, trimmed, changed, noms}`, `DzmDiffView` (svm-pop, résumé + cinq rubriques), bouton « ⇄ » sur chaque ligne de `DzmProjects` (L7d1 repliée dans R_M14), `GET /api/montage/projects/{pid}` ; **écarts** : identité par `id` ; un slip (`srcIn` seul) est un ROGNAGE (`trimmed`, `src:[inA,inB]`), pas un déplacement ; `moved` exclut `trimmed` ; `changed` compare la forme JSON de `DZM_DIFF_CLES` (l'ordre des clés et `effects` comptent) ; tolérance 1e-6 ; sixième clé `noms` ; « ⇄ » toujours rendu, GRISÉ sur le projet courant ; `GET /projects/{pid}` rend le record DIRECT ; aucune route neuve ; garde `diff` sans état → null ; `label`/`text` de `DZM_DIFF_CLES` ne sont pas des clés de `renderPayload` (banc croisé [2], daté) | P3 |
| Transcription, text based editing, IntelliScript | calage gratuit du texte connu, transcription payante, couper par le texte, mots de remplissage | **Conservé** | — |
| AI Speech Generator (TTS, clonage 10 s) | ElevenLabs (voix clonées) | **Conservé** (payant, déjà) | — |
| Smart Reframe, Super Scale, Magic Mask, Voice Isolation, Music Remixer, CineFocus, Face tools, IntelliSearch, Slate ID, UltraSharpen, Motion Deblur, Close Up, Speed Warp | fal en option par clip (D2 du plan) | **Écarté** sauf **D-40 = D2 du plan** (recadrage par énergie de mouvement PIL, gratuit ; fal en option payante par clip, jamais par défaut) et **D-41 = D3 du plan** (auto-clips 15–60 s depuis épisodes/films, score LLM avec repli heuristique) — **exécuté 24/09/2026** (lot L7-B) : **D-40** (T3 `d70989c`, revue `e09552b` ; T4 `871d5de`, revue `637bf41`) service pur `reframe.py` (tracker PIL : différences d'images à 4 i/s en 96 px de large, barycentre des colonnes au-dessus du bruit, EMA 0,5, plus de 20 % de paires immobiles → centré ; erreur mesurée 3,0 % de la largeur), champ V1 `reframe {mode: centre|suivi|manuel}` nettoyé par `_reframe_of` (client `dzmReframeOf` : MÊMES bornes, croisé sur 169 cas tirés), `crop` à `x` animé dans les trois voies cover et la voie stabilisée, route `POST /api/montage/reframe`, section « Cadrage » de l'inspecteur (Centré / Suivre / Manuel, « Analyser le mouvement », aperçu vivant par `object-position`, grisé-jamais-masqué) ; **écarts D-40** : crop à `x` animé MESURÉ (ffmpeg 8.1.1 : passe ; un `w` animé reste −22) ; points RENDUS en secondes de SOURCE depuis srcIn (le crop précède `setpts=PTS/vitesse`), le client convertit la tête par (tête − start) × vitesse ; **FORMAT DU CHAMP corrigé en revue finale du lot (24/09/2026)** : les points du champ `reframe.points` sont en temps ABSOLU de source (le client pose srcIn de l'analyse + t rendu par `/reframe`), parce que la lame, la découpe aux plans, la coupe ripple et le rognage de tête avancent srcIn en COPIANT le champ — en relatif, le morceau droit d'un plan [0,10] suivi 0,2→0,8 coupé à 5 s cadrait 0,2→0,5 au lieu de 0,5→0,8 ; `_reframe_of` et `dzmReframeOf` soustraient le srcIn COURANT, gardent la fenêtre [srcIn, srcIn + (end − start) × vitesse] et y posent un point de BORD interpolé de chaque côté où des points tombent (mêmes règles des deux côtés, croisé sur 169 cas + 90 cas à srcIn ≠ 0, rendu réel de la découpe : le morceau droit garde le carré, l'ancien comportement le perd) ; le payload porte les points absolus (`reframePayload`) ; aucun marqueur de format (projets de la branche jamais livrés, un champ relatif posé à srcIn 0 se lit pareil) ; l'EDL dit `* REFRAME: <mode> (non exporté)` sous l'événement d'un plan au cadrage non centré (commentaire neutralisé en FCPXML) et `GET /export` n'est servi qu'en boucle locale (sauvegarde lue hors de la boucle d'événements) ; expression en ARBRE équilibré de `if(lt())` (`_rf_lerp_expr`) parce que la CHAÎNE de `_mp_lerp_expr` échoue à −22 au-delà de 93 niveaux ; points simplifiés (RDP) et graphe passé par `-/filter_complex <fichier>` au-delà de 30 000 caractères de commande ; recadrage VERTICAL non géré (y centré) ; rotation de la source lue (`_probe_dims`, side_data rotation) ; « 1_0 » lu 10 par `float()` côté serveur, refusé côté client (hors fil) ; D-39 compare désormais `reframe` (clôture) ; **D-41** (T5 `9c55f40`, revue `031dd6a` ; T6 `e3f1d08`, `11e287b`, revue `831b8bf`) service pur `autoclips.py` (`windows` 15–60 s sur phrases, `heuristic` déterministe, `score` LLM en JSON strict avec repli heuristique, clips disjoints), routes `POST /api/montage/autoclips` (texte connu calé gratuitement sur le son, sinon estimation) et `/autoclips/create` (projet NEUF : V1 + son du plan + S1 décalés, le courant intouché), bouton « ✂ auto-clips » de chaque ligne du tiroir Médias → popover (texte connu, nombre, langue, persona, « Classer avec l'IA ») → « Créer le projet », armé, ouvert par le mécanisme E-1 ; **écarts D-41** : fenêtres SANS plafond (le plafond de 400 tronquait la source vers 352 s), 60 candidats au modèle pris sur 60 tranches de temps ; transcription SYNCHRONE (l'écran attend) et mots transcrits MIS EN CACHE (source, taille, mtime, fournisseur, langue) ; la dépense : estimation d'abord, `confirm:true` + `max_usd` (le coût de l'estimation COCHÉE) exigés sur le chemin PAYANT seulement, 409 quand l'estimation refaite dépasse le plafond (la nouvelle estimation est rendue et affichée NON cochée), 409 « déjà en cours » par un VERROU par clé de cache (clôture) ; « Classer avec l'IA » COCHÉ par défaut (quelques centimes), décochable (`llm:false`, aucun appel) ; `chapter_id` bancé côté serveur mais SANS sélecteur côté client (le texte connu couvre le besoin) ; bouton de ligne au lieu du clic droit ; projets créés en `ratio` 9:16 ; aucun appel réel au LLM ni à la transcription dans les bancs et les preuves | P2 / P3 |
| Détection de scènes (media page, Cut) | — | **À faire D-42** : `scdet` → « découper aux changements de plan » sur un import externe (lame automatique), et « plans trop longs » (D-8) — **exécuté 24/09/2026** (lot L7-B, T2, `4c48d45`, revue `8517a25`) : service pur `scenes.py` (`ffmpeg -ss/-t` AVANT `-i`, `setpts=PTS-STARTPTS,scdet=threshold=10,metadata=mode=print:key=lavfi.scd.time:file=-`, temps relatifs au srcIn, triés, dédoublonnés à une image, ≤ 200, cache par (chemin, mtime, srcIn, durée, seuil)), route `POST /api/montage/scenes`, cœur pur `dzmCutAt` (t → start + t/vitesse, bords 0,05 s ignorés, écart MINIMAL de 0,05 s entre coupes, ids uniques, piste verrouillée refusée), menu contextuel de clip « Découper aux changements de plan » (vidéo rendue seulement, grisé sinon) ; **écarts** : `key=lavfi.scd.time` (sans elle, scdet imprime trois lignes PAR IMAGE) ; `setpts=PTS-STARTPTS` CONTESTÉ par la mesure de clôture — avec `-ss` avant `-i`, ffmpeg 8.1.1 rend déjà des temps relatifs : la mutation qui le retire ne rougit que la ligne de forme (srcIn 0,5 → coupe à 1,5 reste vert), gardé comme ceinture ; réponse obsolète refusée (empreinte srcIn, vitesse, start, end ET identité de la source) ; les champs en temps LOCAL du clip (`dz`, keyframes, `reframe.points`…) sont COPIÉS TELS QUELS dans chaque morceau, comme par la lame du bundle (ni recalés ni coupés) ; « plans trop longs » reste D-8 (L7-A) | P2 |

## 2. Décisions tranchées (résumé, à valider)

**D-0 — Historique complet.** Une pile, tout l'état, 50 pas. Sans lui, chaque
lot suivant reproduit la phrase « Ctrl+Z ne retire pas… ».

**D-1 — La barre OUTILS ne recouvre plus les pistes.** Origine sous le
transport, hors de la grille.

**D-2 / D-3 — Les cinq modes d'édition et les quatre trims.** C'est le cœur
« Edit » de Resolve ; tout en fonctions pures bancées sous node, gestes :
Alt = slip, Maj = slide, jonction = roll. Écart assumé : pas de trim en
lecture.

**D-9 — Piste d'ajustement** plutôt que nœuds : un post-pass borné, même
rack, même moteur. — **exécuté 23/09/2026** : piste `j1` (genre `adjust`, cinquième lettre de `dzmKindOf`, posée à `dzmTitresAt` sous T1), clip sans `src` (`dzmAdjustNew`, 3 s bornées), rack VFX ouvert sur le clip (AJ2a/AJ2b), pose d'effet (AJ7), hachures inline + `data-kind` (AJ6), payload `adjust_clips` → post-pass `_fx.build_chain` `[aj<j>]` borné, banc l3 [5] + édition [18] + bundle [D-9] + mutations 11–12, 16–17, 20–21 ; **écarts** (tous mesurés) : UNE seule piste `j1`, pas d'empilement ; effets non joués en direct (visibles après Preview, l'inspecteur le dit) ; post-pass borné APRÈS les overlays et le maître de durée, AVANT titres et S1, enveloppe réelle `blend@ajfx{j}e{idx}env` ; un effet à bornes locales hors du clip ou < 0,05 s est IGNORÉ (jamais plein cadre — mesuré avant correctif : chaîne nue) ; clip sans effets transmis avec `effects: []` et ignoré ; bornes non numériques → job en Échec comme V2 ; collecte par `str(c.get("tr"))` (V2 lit `c.get("tr")`, inerte avec des ids chaîne) ; entrée par chip « J+ », Maj+J et « + » de J1 (pas de bouton de barre flottante : le plan de la barre est pinné par le handoff) ; inspecteur générique (Vitesse/In/Out) encore affiché sur le clip d'ajustement (bruit inoffensif) ; la garde amont `vfxAddTo` (patcher vfxrack) refusait tout clip sans src → contournée par `c.kind!=="adjust"&&` (AJ7) ; `kindOf` exporté pour le banc seulement ; la note de retrait de J1 promet Maj+J ; `dzmAdjustAt` (plan : « si utile aux ▲▼ ») NON écrite, rien ne l'appelle ; la garde `not effs` du post-pass est devenue redondante avec `if not bounded` (mutation survivante, commentaire corrigé).

**D-13 / D-15 / D-16 — Dynamic zoom, retime à flux optique, stabilisation
vidstab** : les trois « propriétés de clip » de Resolve qui tiennent en ffmpeg
natif, chacune un champ optionnel et une passe. — **exécuté 23/09/2026** (lot L3 complet, T1–T10, branche `chantier/montage-l3`) : les cinq décisions ci-dessus sont livrées, chacune datée dans le tableau §1.2 et le paragraphe D-9 ; **restes transverses datés** : `put(running,10)` redondant dans l'analyse stab ; `_SB_FN` +1600 ; noms de pins SB ; le commentaire « CINQ clés » de la section [15] du banc édition disait cinq pour sept (corrigé à la clôture) ; deux backends de preuve (8765/8799) tournaient pendant la clôture, aucun n'a été relancé.

**D-20 / D-21 — 58 transitions et 8 gabarits de titre** : reprennent D4 du
plan de septembre ; les titres sont de l'ASS, pas du Fusion.

**D-24 / D-35 / D-36 — Livraison** : loudness normée, presets de sortie
(codecs déjà présents), file locale. La publication reste le Scheduler. — **exécuté 23/09/2026** (lot L4 complet, T1–T5, avec D-38) : chaque décision est datée dans le tableau §1.2 ; la publication reste le Scheduler, la file est locale et en série.

**D-27 … D-33 — Couleur** : roues, accord de couleur (D1 du plan), courbes à
points, masque statique, scopes, huit effets ffmpeg de plus. Sans tracker.

**Écartés, et pourquoi** : multicam / source tape / sync bin (sources
générées), Fusion (le Studio est notre page nœuds), tout ce qui demande un
modèle (masques magiques, visages, voice isolation, super scale), cloud et
collaboration (mono-poste), HDR/RAW/DCP, Lottie/OGraf, VST/ADR/Foley, 2 000
pistes, tracking (pas d'OpenCV, pas de numpy).

## 3. Lots proposés (chacun livrable seul ; estimation en jours-agent, deux revues et campagne de mutations comprises)

| Lot | Contenu | Sections patcher / backend | Estimation |
|---|---|---|---|
| **L0 — socle** | D-0 historique complet, D-1 barre hors des pistes, D-11 plage I/O | M27–M29 ; aucun backend | 1,5 j |
| **L1 — édition** | D-2 cinq modes, D-3 roll/slip/slide, D-5 marqueurs + index, D-4 swap | M30–M35 ; `markers` dans le JSON projet | 4 j |
| **L2 — transitions et titres** | D-20 galerie des 58 `xfade` avec vignettes, D-21 clips Titre (8 gabarits ASS), D-12 fondus en lecture directe | M36–M39 ; `_XFADE` étendu, `titles.py`, `_subs_ass` étendu | 4 j |
| **L3 — propriétés de plan** | D-13 dynamic zoom, D-15 retime (nearest/blend/flow + rampes par division), D-16 stabilisation vidstab (job de détection en cache), D-14 keyframes échelle/opacité, D-9 piste d'ajustement — **exécuté 23/09/2026** : T1–T10 livrées ; bancs l3 86/0, croisé 25/0 (`test_montage_l3_croise.py`), édition 207/0, bundle 1776/0, 23 mutations rouges (`mutations_montage_l3.py`, code de sortie 0) ; **écarts** : « nearest » = la vitesse constante historique (`fps=` duplique ou saute), PAS une valeur de `_RETIME` (= {blend, flow} seulement) ; sections réelles DZ1–DZ4, RT1–RT2, SB, KF1–KF5 (+KF2c), AJ2a–AJ7 et non M40–M45 ; `motion_points` étendu plutôt qu'un champ neuf | M40–M45 ; champs `dz`, `retime`, `stab`, `adjust_clips`, `motion_points.scale/opacity` | 5 j |
| **L4 — livraison** | D-35 presets de sortie (codecs, 4K, 60 fps, audio seul, GIF), D-24 loudness normée, D-36 file locale, D-38 rendu partiel | M46–M48 ; `_DELIVER`, `loudnorm` 2 passes, file dans le worker de jobs — **exécuté 23/09/2026** : T1–T5 livrées sur `chantier/montage-l4` ; bancs l4 120/0, croisé 20/0 (`test_montage_l4_croise.py`), édition 294/0, bundle 2092/0, ergonomie 36/0, 16 mutations rouges (`mutations_montage_l4.py`, code de sortie 0) ; **écarts** : sections réelles L4a–L4d (7 sections du patcher, L4a repliée dans R_M16REF, le bouton dans R_EA5D2) et non M46–M48 ; la file est DÉDIÉE (un worker asyncio), pas le worker de jobs ; le reste des écarts est daté ligne par ligne (D-24, D-35, D-36, D-38 ci-dessus) | 3 j |
| **L5 — couleur** | D-27 roues, D-28 accord de couleur (D1 du plan), D-31 scopes, D-33 huit effets, D-30 masque statique, D-32 copier/coller de grade + lightbox, D-29 courbes | M49–M55 ; `color_match.py`, `/scopes`, catalogue +9, masque région — **exécuté 24/09/2026** : T1–T6 livrées sur `chantier/montage-l5` (plan `2026-09-24-plan-montage-resolve-L5.md`) ; bancs de clôture (un processus par banc, code de sortie 0) l5 109/0, l5_masque 60/0, croisé 36/0 (`test_montage_l5_croise.py`), édition 557/0, bundle 2294/0, ergonomie 68/0, etalonnage 22/0, effects_catalog 21/0, effects_timing 14/0, montage_effects PASS, l7b 163/0, l7b_notes 48/0, l7b_croise 32/0, l7 33/0, l7_croise 31/0, l4 120/0, l4_croise 20/0, l3 86/0, l3_croise 25/0, l2 78/0, projets 159/0, eb 24/0, eb_croise 36/0, ec_croise 26/0, remplacer 99/0, media 88/0, pistes_rendu 33/0, pistes_dyn 20/0, transfert 38/0, traduction 29/0, historique 51/0, hygiène + sendto + security_guards verts ; **20 mutations rouges** (`mutations_montage_l5.py`, 51 lignes rouges, code de sortie 0, patcher / bundle / `.bak_dzcout` restaurés au sha256) ; `--check --force-unchained` 193 ancres ; chaîne montage → dzcout OK ; `node --check` ×2 OK ; sonde dzcout `DzTracks` 172 ; **écarts** : binaire de l'app = ffmpeg **9.0.1** (le PATH de développement porte 8.1.1 ; mesures identiques) ; catalogue **+10 types** et non +9 ; `lenscorrection` préexistant ; `nlmeans` écarté ; `colorbalance` démenti → `lutrgb` ; masque `geq` calculé une fois (aucune région dans `build_chain`) ; effets V2 enfin rendus + alpha d'origine gardé (`glitch`, `grain` en écart) ; accord pivoté ; `-ss` hors durée ; Ctrl+Alt+C/V (Ctrl+Maj+C réservé, AltGr AZERTY non prouvé au clavier physique) ; rangée du lecteur remontée (barre OUTILS qui la recouvrait) ; « à tous les plans » non généralisé ; D-29 partiel (Hue vs Sat seulement) ; `shortest=1` du masque V1 = ceinture sous `-t total` (mesuré à la clôture) ; sections réelles L5sc1 (queue) et replis R_DZ1/R_DZ4/R_EC1/R_R1/R_R2/R_K7/R_EB5A/R_M16REF, et non M49–M55 ; la campagne de mutations a trouvé DEUX trous du banc croisé (combo effective de `grade_copy`, largeur égale au défaut de la route), comblés avant son premier commit ; détail daté ligne par ligne (D-27 … D-33 ci-dessus) | 6 j |
| **L6 — audio** | D-23 EQ 6 bandes + pan, D-25 de-hum + apprentissage du bruit, D-26 enregistrement voix off, D-42 détection de plans | M56–M59 ; vocabulaire `sfx_service` +3, `scdet` | 3 j |
| **L7 — confort** | D-6 presse-papiers inter-projets, D-7 mini-carte (**sortie de L7, exécutée 23/09/2026 dans le lot E-B**, `3140613`), D-8 boring detector, D-10 preset clavier Resolve, D-3b trim A/B, D-19 coins/ombre PIP, D-22 pistes S2 langues, D-39 diff de projets, D-37 EDL/FCPXML (D5), D-40 recadrage (D2), D-41 auto-clips (D3), D-34 notes ★ | M60+ — **exécuté 24/09/2026 (lot L7-A)** : D-10, D-6, D-8, D-39, D-3b, D-19, D-22 livrées sur `chantier/montage-l7` (T1–T7, plan `2026-09-24-plan-montage-resolve-L7A.md`) ; bancs édition 400/0, bundle 2191/0, ergonomie 52/0, l7 33/0, croisé 31/0 (`test_montage_l7_croise.py`), l4 120/0, l3 86/0, l2 78/0, remplacer 99/0, traduction 29/0, pistes_dyn 20/0, pistes_rendu 33/0, subtitles_burn OK, ec_croise 26/0, eb_croise 36/0 (réaligné à la clôture : `dz_svm_boring` huitième clé), l3_croise 25/0, l4_croise 20/0, hygiène OK ; 18 mutations rouges (`mutations_montage_l7.py`, code de sortie 0) ; `--check --force-unchained` 191 ancres ; chaîne montage → dzcout OK ; `node --check` ×2 OK ; sonde dzcout `DzTracks` 159 ; **écarts** : sections réelles L7a3, L7b3, L7c3, L7g1/L7g2, L7e1…L7e8 (onze), L7f1/L7f2/L7f5/L7f6 (vingt sections en queue après L4d4, le reste REPLIÉ dans R_R1/R_R2/R_AJ6A/_EB7_ETAT/R_M14/R_EC1/R_M24H) ; comptes durs REDATÉS (useState 545, trackKind 36, sonde dzcout 159, ancres 191) ; `.mov` en sous-chaîne (`out.moved`) → jetons regex dans deux bancs ; le reste des écarts est daté ligne par ligne (D-3b, D-6, D-8, D-10, D-19, D-22, D-39 ci-dessus) ; **reste L7-B** : D-37 EDL/FCPXML, D-40 recadrage, D-41 auto-clips, D-42 scdet, D-34 notes ★ — plus les restes du L7-A : masque alpha STATIQUE (`alphamerge`) à la place de `geq` (×29), drag du losange sans lecture du refus de `dzmRoll`, note « S1 est la seule piste » encore dans `DzmTrackBtns`, S2 non éditable dans le tiroir ; **L7-B exécuté 24/09/2026** : D-37, D-42, D-40, D-41, D-34 livrées sur `chantier/montage-l7b` (T1–T8, plan `2026-09-24-plan-montage-resolve-L7B.md`) ; bancs (clôture, un processus par banc, code de sortie 0) l7b 158/0, l7b_notes 48/0, croisé 31/0 (`test_montage_l7b_croise.py`), édition 459/0, bundle 2271/0, ergonomie 62/0, l7 33/0, l7_croise 31/0 (réaligné : `reframe` dix-neuvième clé), l4 120/0, l3 86/0, l2 78/0, projets 159/0, eb 24/0, eb_croise 36/0, ec_croise 26/0, l3_croise 25/0, l4_croise 20/0, remplacer 99/0, media 88/0, transfert 38/0, traduction 29/0, hygiène + sendto verts ; 19 mutations rouges (`mutations_montage_l7b.py`, code de sortie 0) ; `--check --force-unchained` 192 ancres ; chaîne montage → dzcout OK ; `node --check` ×2 OK ; sonde dzcout `DzTracks` 163 ; **écarts** : datés ligne par ligne (D-34 l.111, D-37 l.119, D-40 et D-41 l.129, D-42 l.130) ; restes des revues fermés à la clôture (`393b7b1` : verrou de transcription en cours, estimation dans le 409, `confirm` gratuit sans `max_usd`, mémoires d'une note en vol au-delà de 15 s, `reframe` dans D-39 — pins l7_croise 18 → 19 clés) ; la campagne de mutations a trouvé un TROU de couverture (M2 de la coupe franche) et une FAUTE n°6 (`_eval_ff` du banc l7b tuait le banc au lieu de le rougir), corrigés ; aucune section `L7B` neuve au patcher à la clôture | 8 j (à découper) |

Ordre conseillé : **L0 → L1 → L2 → L3 → L4 → L5 → L6 → L7** (valeur
d'usage décroissante ; L0 est un préalable technique). Total hors L7 :
**≈ 26,5 j**.

## 4. Ce que cette conception ne fait pas

- Elle ne touche à rien d'existant : bloc `sonvfx`, `subs.js`, `vfxrack.js`
  restent intouchables ; toute correction de leurs comportements se porte
  dans un maillon aval (règle de `chaine-patchers-bundle`).
- Elle ne promet aucune IA : chaque ligne « IA » de Resolve est écartée ou
  renvoyée à une option fal déjà payante et déjà cadrée (E2 du plan).
- Elle n'ajoute aucune dépendance : ffmpeg 8.1.1 livré, Pillow, stdlib ;
  toute vignette, roue ou courbe est dessinée en canvas maison.
- Aucun plan ni code avant la validation de ce tableau.
