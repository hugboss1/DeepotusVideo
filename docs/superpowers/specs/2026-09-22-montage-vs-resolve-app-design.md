# Montage « ergonomie, design, usage » — tableau d'écart contre l'application Resolve 21.1 (22/09/2026)

> Sources : relevé de l'application `2026-09-22-davinci-resolve-app-inventaire.md`
> (ce jour, sur le bureau), relevé fonctionnel du site du 21/09, relevé de
> notre Montage du 21/09 (`2026-09-21-montage-inventaire.md`), et un relevé
> de code fait ce jour sur `main` = `b81fa40` (bundle appliqué, `montage.js`
> 6 053 l., `montage_service.py` 3 583 l.) pour les quatre exigences posées
> par l'utilisateur. **Décisions à valider AVANT tout plan et tout code.**
> Les décisions D-0…D-42 de `2026-09-21-montage-vs-resolve-design.md` restent
> valables ; celles-ci s'ajoutent sous le préfixe **E-** (entrée/sortie et
> ergonomie). Rien de l'existant n'est réécrit : chaque ligne « À faire »
> s'ajoute par une section neuve du patcher `montage` (ou d'un maillon aval),
> un champ optionnel ou une route neuve, et un banc autonome.
>
> **VALIDÉ le 22/09/2026 par l'utilisateur : lots E-A, E-B, E-C ; ordre E-A → L3 → E-B → E-C ; consigne : ne pas complexifier le code, rester efficient et robuste.**

## 0. Les quatre exigences, mesurées dans le code (état au 22/09)

| Exigence | État | Ce qui existe, exactement | Ce qui manque |
|---|---|---|---|
| **Récupérer dans Montage les rendus de Studio** | partiel, par effet de bord | (A) `GET /api/montage/project` sans sauvegarde pose les **4** derniers jobs `done` à artefact vidéo (`.mp4 .mov .webm .mkv .m4v .avi`, `.limit(60)` de candidats, `montage_service.py:1352-1432`) sur V1 avec un jumeau A1 « · son du plan » ; (B) le sélecteur `openPicker` (bouton **lier** du groupe BIBLIOTHÈQUE, « + » d'une piste vidéo) — panneau **« Ajouter sur la piste V1 »**, sections « Images (Bibliothèque) » / « Rendus vidéo » / « Sons (Bibliothèque) », **12** rendus au plus (`.slice(0,12)` sur les 50 derniers jobs), chips cliquables et glissables ; (C) Bibliothèque → onglet Renders → vignette → **« Envoyer vers… » → « 🎞 Montage — clip vidéo »** (`patch_bundle_libsend.py:201`), qui pose sur **`v2`** en dur (`:253-259`) ; (D) « Remplacer la source… » (`GET /newer`, rapprochement par titre) | le mot **Studio n'existe nulle part** (aucun filtre par provenance : `seedance / heygen / composition / template / news` sont indistincts) ; deux règles « est-ce une vidéo » différentes (Bibliothèque : par `provider`, picker : par extension) ; (C) pose sur `v2` même quand la piste n'existe pas (sauvegarde du 04/09 sans v2 → clip **invisible** mais rendu) ; pas de retour de Studio vers le Montage |
| **Récupérer les vidéos de Chapitres** | partiel, par effet de bord | Chapitres (`id:"episodes"`, `POST /api/episodes/render`, `provider="episode"`, `outputs/final/<job>.mp4`, 9:16) : le job passe (A), (B) et (C) puisqu'il est `done` et `.mp4` ; chip de provenance **« Episode »** dans la Bibliothèque | **aucun bouton dans Chapitres** vers le Montage (`__dzSendNav("montage")` n'existe que dans la Bibliothèque) ; arrivée sur `v2` = incrustation **muette** (le son gravé n'est lu que par le jumeau A1, créé seulement par (A)) ; les **scènes** de l'épisode ne sont pas transmises (un `.mp4` plat) |
| **Créer un montage neuf** | **absent** | panneau **Projets** (`enregistrer sous…`, `renommer`, `dupliquer`, `ouvrir → remplacer ?`, `× → supprimer ?`) ; `POST /projects` **refuse une timeline vide (400)** — c'est un « enregistrer sous » ; le seul « repartir » est le bouton **bibliothèque** de la barre de titre (« écraser la sauvegarde ? oui/non ») qui **repose les 4 derniers rendus** | aucun « Nouveau montage vide » ; « ouvrir » est destructif à deux clics sans annulation ; un projet = une timeline |
| **Envoyer vers le Scheduler en fin de montage** | existe, **automatique et non choisi** | **« Rendre & publier → »** (`svm-goldbtn`) → popover « rendu ffmpeg (local) · publication · brouillon Scheduler · gratuit » → après le rendu FINAL, le **client** fait `POST /api/schedule` avec `run_at` = **demain 09:00**, `channels` = **["x","telegram"]** en dur, `caption` = nom + « 🐙 », `job_id`, puis `props.go("scheduler")` à 900 ms ; la Bibliothèque a son propre « 📅 Scheduler — brouillon de post » (+2 h, ["x"]) | pas de bouton **« Envoyer vers le Scheduler »** séparé du rendu ; pas de rendu master SANS brouillon ; navigation forcée ; tout dans le poll client (onglet fermé = aucun brouillon) ; deux conventions (09:00/x+telegram vs +2 h/x) ; `project_id` non transmis |

> **État au 22/09 soir : E-A livré** (E-1, E-3, E-4 — `bdc0ccf` … `9ff00c2`, clôture `6bf0244` restes de revue, `c9ffd99` mutations + banc croisé, et cette conception). Preuves écran (Playwright, contrôleur) citées ligne à ligne au §1 ; bancs ea 31/0 (dont le croisé `_CLIENT_DEFAULT_TRACKS` == `DZM_DEFAULT_TRACKS`), edition 169/0, bundle 1685/0, 107 ancres, sonde dzcout 99 ; campagne `backend/tests/mutations_montage_ea.py` : 18/18 rouges, 0 survivant, 0 mort (`c9ffd99`). Les écarts assumés sont dans chaque ligne ; E-2 et E-5 restent le lot E-B.

## 1. Tableau d'écart — entrée, projet, sortie (le cœur de la demande)

| Chez eux (Resolve, vu à l'écran) | Aujourd'hui chez nous | Décision proposée | Prio |
|---|---|---|---|
| **Project Manager** (⌂, Shift+0) : fenêtre modale à cartes (vignette + nom, contour rouge = sélection), onglets Local/Network/Cloud, tri, recherche, vue grille/liste, pied **Export · Import · New Project · Close** ; « New Project » = timeline VIDE | panneau **Projets** dans le transport (liste, « enregistrer sous… ») ; aucun « nouveau » ; réinitialiser = 4 derniers rendus posés | **E-1 — « Nouveau montage »** : bouton dans le panneau Projets ET dans la barre de titre ; route `POST /api/montage/projects` acceptant `{name, vide:true}` (tracks par défaut, `clips:[]`, plus de 400) et `POST /projects/{pid}/open` le chargeant tel quel — **`GET /project` ne pose plus les jobs récents quand le projet ouvert est marqué `vide`** (le remplissage automatique reste pour l'ancien chemin sans sauvegarde) ; le panneau Projets devient une **grille de cartes** (vignette = première image V1 par `/strip`, nom, date) avec « Nouveau » / « Dupliquer » / « Ouvrir » / « Supprimer » ; « ouvrir » garde sa confirmation mais **enregistre d'abord un instantané** du montage courant sous « (non nommé) » au lieu de le perdre — **exécuté 22/09/2026** : `POST /projects {name, vide:true, tracks?}` (drapeau opt-in strict, `_CLIENT_DEFAULT_TRACKS` miroir bancé de `DZM_DEFAULT_TRACKS`, plafond 2 Mo dans `_save_record`), `GET /project` et `open` le respectent (`bdc0ccf`, `a64345b`) ; « nouveau » et « ouvrir » armés, `dzmProjetNeuf`/`dzmInstantaneNom`, copie de sûreté du montage non nommé avant d'ouvrir (`f4b288e`, `5ef8b03`) ; preuve écran : « nouveau » crée un montage vide (0 clip, 7 pistes, 0:30, `vide:true`) qui survit à F5, le montage non nommé est mis à l'abri en projet avant l'ouverture, « ouvrir » le restaure sans copie supplémentaire ; **écarts** : le panneau Projets reste une **liste** (pas de grille de cartes, pas de vignette) ; le bouton `bibliothèque` (réinitialiser) est inchangé ; `vide` est **persistant** — un montage neuf reste marqué vide tant que le client renvoie le drapeau, même après ajout d'audio : voulu, c'est le repli Bibliothèque qu'il désarme, pas un état « sans clip » | P0 |
| **Media Pool** = un panneau permanent (Cut/Edit/Media), bins, Smart Bins, vue grille/liste, recherche, tri ; import par glisser depuis Media Storage ; « Add clips from Media Storage to get started » | sélecteur `openPicker` en popover (12 rendus, 3 sections), Bibliothèque = un AUTRE écran | **E-2 — « Médias » : le sélecteur devient un tiroir** (comme Sons / Sous-titres / Narration, `.svm-mid`), ouvert par un onglet « Médias » et par le « + » des pistes : **chips de provenance** en tête (« Tout · Studio · Chapitres · Quick · News · Templates · Importés »), recherche, tri par date, **pas de plafond à 12** (pagination `limit/offset` sur `/api/jobs`), vignette + durée + provenance ; un seul juge « est-ce une vidéo » (extension ET provider, servi par `/media-rules`) ; glisser sur la piste voulue, clic = « Ajouter sur la piste V1 » comme aujourd'hui — **exécuté 23/09/2026** : `GET /api/jobs?limit&offset&providers&q&video=1` paginé et filtré côté serveur (`2849626`, jokers LIKE échappés `ce8b412`), couche `dzmProvGroupe`/`dzmProvChips`/`dzmMediaFiltre`/`DzmMediaDrawer` (`46a40dd`, `dfc1239`, `94b176c`), onglet « médias » dans la barre des tiroirs, tiroir dans `.svm-mid`, « + » vidéo → tiroir (`2a7ce7d`, `3e9a878`) ; preuve écran : tiroir ouvert, chips dérivées, « Plus », clic pose à la tête de lecture ; **écarts** : les chips sont **dérivées** des providers reçus par la table `DZM_PROV_LBL` (aucun provider ne s'appelle Studio/Chapitres/Quick : la liste figée ci-contre est remplacée, un provider inconnu s'affiche tel quel) ; le tiroir = rendus **vidéo** seulement, Images et Sons restent dans `ovPicker` (Maj+clic sur le « + » vidéo, « lier ») ; `video=1` juge l'**extension** côté serveur (`media_rules()` unique, `/media-rules` ne porte que `video_exts`, pas de liste de providers) et la couche juge le **statut** `done` et exclut `_preview` (`dzmIsVideoJob`) ; filtre groupe **local** aux pages chargées, recherche serveur dès 2 caractères (repos 250 ms), dédoublonnage par `job_id` entre pages, « Plus » rejoue le même offset après une erreur HTTP ; vignette `strip n=1 w=96 h=54` masquée sur erreur ; tri par date déjà serveur ; garde `proj.demo` sur le tiroir (openPicker la portait, `addAsset` non) ; le « + » audio ouvre `ovPicker` sans fermer le tiroir (comme Sons/Narration) ; piste verrouillée refusée au clic sur une rangée ; lecteur ≈ 370 px à 1280 px avec inspecteur ; tiroir non persisté (F5 → fermé) ; `video=true` → 422 (le client envoie `1`) ; `q` > ~64 Ko refusé par le transport ; la recherche `q` n'est insensible à la casse que pour l'**ASCII** (le `lower()` de SQLite laisse « Éclair » intact — écart daté 23/09/2026) | P0 |
| Import direct depuis une autre page : rien de tel (Resolve n'a pas de « Studio »), mais chaque page garde le média dans le pool partagé | Studio et Chapitres n'ont **aucun bouton** vers le Montage ; seul le détour par Bibliothèque → Renders → « Envoyer vers… » existe, et il pose sur `v2` | **E-3 — « Ouvrir dans le Montage »** sur un rendu terminé de Studio ET sur un épisode terminé de Chapitres (même mécanisme `__dzMontageAdd`), qui pose **sur V1 à la tête** (`dzmPickTrack`, jamais `v2` en dur) **avec le jumeau A1** (`twinPlan` existant) ; pour Chapitres, transmettre `scenes[]` → **un clip par scène** (`srcIn/srcOut` = bornes des scènes dans le `.mp4`) avec un marqueur par scène (D-5) — option « un seul plan » conservée ; corriger la greffe `libsend` existante (`v2` → piste résolue) — **exécuté 22/09/2026** : « Ouvrir dans le Montage » sur un épisode terminé de Chapitres (EA2) et un rendu terminé de Studio (EA3), même boîte aux lettres `__dzMontageAdd` ; la porte Bibliothèque vidéo réécrite en aval sur `v1` (EA1) avec le jumeau A1 (`f510e87`) ; preuve écran : « Envoyer vers… → Montage — clip vidéo » pose sur V1 (0–6 s) avec le jumeau A1 « · son du plan », le bundle servi porte les deux boutons « Ouvrir dans le Montage » ; **écarts** : **un seul plan par épisode** (le job ne stocke aucune durée de scène : rien à découper), pas de marqueur par scène ; l'**image** « Envoyer vers… » reste sur `v2` (incrustation, voulu) ; les portes Scheduler propres à Chapitres/Studio gardent leurs conventions (+1 j 09:00) ; « déjà sur le Montage » impossible à dire (l'écran est démonté hors de sa vue, la boîte aux lettres est lue à sa monture) | P0 |
| **Deliver** : page dédiée, `Preset` (Custom, H.264/H.265 Master, ProRes, **YouTube · Vimeo · TikTok · Dropbox** avec sous-menus, Presentations, Audio Only…), `File Name`, `Location`, Video/Audio/File, résolution, cadence, `Add to Render Queue`, **Render Queue** à droite, `Render All` ; **Quick Export** partout en haut à droite | « Preview 480p (gratuit) » et « Rendre & publier → » (rendu + brouillon Scheduler automatique demain 09:00, x+telegram, navigation forcée) | **E-4 — séparer rendre et publier** : popover « Rendre » = **preset** (Master 1080 · YouTube · TikTok/Reels 9:16 · X · Audio seul — table `_DELIVER` de D-35) + nom de fichier ; à la fin du rendu final, **bandeau « Rendu terminé »** avec trois boutons : **« Envoyer vers le Scheduler »** (ouvre un mini-formulaire : canaux cochés, date/heure, légende pré-remplie), **« Voir dans la Bibliothèque »**, **« Fermer »** — plus de navigation forcée ; le brouillon est créé **par le backend** à la demande (`POST /api/montage/publish {job_id, project_id, channels, run_at, caption}` → `create_scheduled_post`), donc robuste à l'onglet fermé ; une seule convention partagée avec « Envoyer vers… » de la Bibliothèque (défaut : +2 h, canaux de la dernière fois) ; `project_id` transmis au post — **exécuté 22/09/2026** : `POST /api/montage/publish {job_id, channels?, run_at?, caption?, title?, project_id?}` → `create_scheduled_post` (liste blanche `_CHANNELS`, +2 h à défaut, fuseau ramené en UTC naïf, 409 si le fichier a disparu, `brief.project_id`) (`5cbb45e`, `d9810e4`) ; le rendu final ne poste plus et ne navigue plus (EA4), quatre libellés (EA5a-d), bandeau `DzmFinBandeau` « Rendu terminé » (Envoyer vers le Scheduler · Voir dans la Bibliothèque · Fermer), canaux mémorisés `dz_montage_channels`, heure locale → ISO « Z », fermé au lancement d'un rendu (EA6) (`b7b6fed`, `9ff00c2`) ; preuve écran : le rendu réel ne crée aucun post, le bandeau propose x coché, +2 h arrondi, légende = nom du projet, l'envoi avec Telegram crée un seul brouillon `x,telegram` `run_at` 20:00Z (locale 22:00 convertie), `brief.project_id`, bouton désarmé, aucune navigation, canaux mémorisés ; **écarts** : presets de sortie = **D-35 (L4)** ; pas de file (D-36) ; le bandeau ne mémorise que les canaux (heure et légende recalculées à chaque rendu) ; `proj` (nom, project_id) lu dans le poll est celui du lancement du rendu (un « enregistrer sous » pendant le rendu ne le suit pas) ; le `POST /schedule` du Scheduler garde son défaut de fuseau (seule notre route convertit) ; un refus non-JSON affiche le statut HTTP ; le commentaire d'en-tête du bundle l. 352 (« Rendre & publier » → … brouillon Scheduler) est **préexistant**, injecté par le maillon amont `sonvfx` (`son-vfx-montage.js:15`) : non touché | P0 |
| **Render Queue** : file visible, plusieurs jobs, `Render All` | un job à la fois, popover fermé pendant le rendu | **Conservé** pour ce lot ; la file locale est **D-36 (L4)** | — |
| **Quick Export** (bouton haut droite + menu File) | « Preview 480p » dans la barre de titre | **E-5** : renommer/regrouper la barre de titre en « **Preview · Rendre · Publier** » (trois verbes, trois boutons), Preview restant gratuit et local — **exécuté 23/09/2026** : barre « Preview · Rendre · Publier » (`4199cbe`), « Publier » rouvre le bandeau E-4 sur le dernier rendu **final** mémorisé côté client (`dz_montage_lastfin` par `project_id`, `dzmFinStore`/`dzmFinOf` purs) ; **écarts** : aucun lien job↔projet en base (`JobRecord` sans `project_id`), donc mémoire **client** ; Preview n'arme pas Publier ; store jamais purgé (≈ 70 o/projet, `at` posé pour une purge future ≤ 50) ; `project_id`/`name` = ceux du lancement (limite E-4 héritée) ; rendu supprimé → « Envoi impossible : Rendu introuvable. » sans purge ; projets non nommés partagent la clé `"_"` ; bandeau déjà ouvert + Publier = pas de remontage ; `.svm-secbtn:disabled` neuf (grise aussi « Plus ») | P1 |

## 2. Tableau d'écart — ergonomie et design de l'écran

| Chez eux | Chez nous | Décision proposée | Prio |
|---|---|---|---|
| **Barre de menus** de 13 menus (File… Help), raccourcis affichés en regard, entrées désactivées visibles en gris | aucune barre de menus ; 37 raccourcis dans un panneau « ? » ; le seul clic droit retire un losange | **E-6 — menu « ☰ » unique** dans la barre de titre (Projet · Édition · Timeline · Marqueurs · Affichage · Aide) listant les actions existantes AVEC leur raccourci (lu depuis la table des raccourcis remappables) ; **menu contextuel** sur un clip (couper, supprimer, remplacer la source, effets, vitesse, transition) et sur une piste (verrou, muet, solo, supprimer) — les actions existent déjà, seul le point d'entrée manque — **exécuté 23/09/2026** : `3208dea`, `c28c059`, `fb44056`, `a3bd684`, `ae7e31e` — couche `dzmComboToKey` / `dzmMenuModel` / `DzmCtxMenu`, hôte `dzFire` / `dzMenuProps` / `dzReplaceArm` (EC1), bouton ☰ (EC2), voile porte `dzMenu` (R_EB5A), clic droit clip (EC4) et piste (EC5), Échap dans R_K7 ; **écarts** : dispatch par **KeyboardEvent synthétique sur `window`** (aucun `dispatch(id)` n'existe ; `onKey` n'y lit ni `code` ni `isTrusted` ; 45/45 aller-retour combo → touche → combo, banc croisé) ; six rubriques par la table `DZM_MENU_RUB` (32 ids) + repli Timeline pour les 13 gestes de tête ; jog / step / play présents dans le ☰ (pas de masque, la table est celle du panneau « ? ») ; pas d'entrée Guide (aucun lien dans le rail) ; « Remplacer la source… » grisée sans `src` seulement (le geste accepte l'audio) ; suppression de piste sans armement (Ctrl+Z en un cran) ; coordonnées ramenées au repère `.dzsvm` ; `max-height:calc(100% - 16px)` sur `.svm-menu` (bloc conteneur, pas `100vh`) ; pas de navigation clavier dans le menu ; « Projet » = Projets… / Preview 480p / Rendre… / Publier ; `svmKeyLabel` rend "" sans surcharge (repli sur la combo de table) ; `bubbles` inerte pour un écouteur sur `window` (pinné, pas un comportement) | P1 |
| **Une page = un espace** (Media · Cut · Edit · Deliver), barre des pages en bas avec icône + libellé, page active soulignée rouge | un seul écran, tout à la fois : lecteur, inspecteur, timeline, tiroirs | **E-7 — trois vues du même écran** par une barre basse « **Médias · Montage · Livraison** » (Médias = tiroir E-2 grand ouvert + lecteur source ; Montage = l'écran actuel ; Livraison = presets + bandeau E-4 + historique des rendus du projet) ; pas de nouveau composant lourd, un `data-view` sur la racine `.dzsvm` qui montre/masque — **exécuté 23/09/2026** : `898b3b1`, `261a2ef` — couche `dzmJobsTri` / `DzmDeliver`, hôte `view` / `dzJobs` / `dzSetView` (repli R_M16REF), `data-view` (EC7), barre `.svm-views` (EC8), panneau premier enfant de `.svm-mid` (EC9) ; **écarts** : la vue est un **état de session** (pas de localStorage, F5 → Montage) ; historique **par TITRE** (`GET /api/jobs?providers=montage&q=<nom>&limit=24`, un nom court attrape les titres qui le prolongent, nom vide = tous) avec finals / aperçus séparés par le suffixe « (aperçu 480p) » du service (banc croisé) — aucun `project_id` en base ; Livraison ferme les quatre tiroirs (`min-width:320px` sur le panneau, sinon 150 px mesurés) et le tiroir Narration ne rouvre qu'au prochain montage (`dz_narr_open` non réécrit, comportement de toute bascule croisée) ; presets = D-35 ; « Voir dans la Bibliothèque » par `deepotus:navigate` (`props.go` n'existe pas dans DzMontage) ; l'effet de fetch dépend de `[view, proj.name]` (revue : renommer en place) | P1 |
| **Inspector** à bascule (bouton haut droite), panneau 300–400 px **fermé par défaut**, presets d'inspecteur (21.1) | `aside.svm-insp` **300 px fixes**, toujours ouvert, non redimensionnable | **E-8** : bouton « Inspecteur » à bascule + **poignée de redimensionnement** (260–480 px, mémorisée en local) ; la timeline reprend la largeur — **exécuté 23/09/2026** : `95f3040` — chip « inspecteur » à bascule, `aside` conditionné par `inspOn`, poignée `.svm-insphandle` (pointer events sur `window`), `dzmClamp`/`dzmInspW` 260–480 défaut 300 (= `.svm-insp{width:300px}` amont), clé `dz_svm_insp {on,w}` ; **écarts** : « la timeline reprend la largeur » était **déjà vrai** (frères flex de `.svm-mid`) ; la fermeture de l'aside est l'ancre de M13 (repli, forme `]}):null]}),`) ; setState par pixel = motif maison (`dzmTbSaisie`), React non contourné ; `isPrimary` non testé (second doigt) ; poignée non accessible au clavier ; `rootRef` existant non utilisé (`document.querySelector(".dzsvm")`) | P1 |
| Timeline redimensionnable (séparateur viewer/timeline), **double timeline** en Cut, timecode géant 01:00:00:00, options « durée des clips », « échelle auto des formes d'onde » | `.svm-tl` plafonné à **48 vh** (`montage.css:11-18`), pas de séparateur ; mini-carte = **D-7 (L7)** | **E-9** : **séparateur vertical** lecteur/timeline (glisser, 30–70 %) à la place du plafond ; option « durée sur les clips » (chip) ; D-7 remonté de L7 à ce lot — **exécuté 23/09/2026** : `80c79dd` — poignée `.svm-tlhandle` au-dessus de `.svm-tl` (tirer vers le haut agrandit), `dzmTlH` 30–70 % de `.dzsvm` mesurée, `[data-h]` + hauteur inline, chip « durées » (`dzmDurLbl` par `dzmDurTxt` = `svmRuler`, aucun second formateur), clés `dz_svm_tlh`/`dz_svm_showdur` ; **écarts** : le plafond 48 vh est **conservé** tant qu'aucune hauteur n'est choisie, `[data-h]` lève `max-height` et `min-height` ; `tlH` lu brut au montage puis reborné par effet (hauteur inconnue au premier rendu) ; resize fenêtre après choix non suivi (guérit au remontage ou au geste) ; petite fenêtre + 30 % déborde ; label vide → durée seule | P1 |
| Barre d'outils de timeline **fixe** au-dessus des pistes (sélection, trim, dynamic trim, lame, insert/overwrite/replace, courbes, lien, verrou, drapeau, marqueur, zoom) | barre OUTILS flottante `#dzm-toolbar` (D-1 l'a sortie des pistes), 8 groupes | **Conservé** (D-1 exécuté) ; **E-10** : option « ancrer la barre » qui la pose dans le transport, à gauche des boutons, comme Resolve — un `data-docked` sur `.dzm-tbar` — **exécuté 23/09/2026** : `9048044`, `6c9af1e` — CSS seule (six règles `[data-docked]` dans montage.css, première règle `.dzsvm .dzm-tbar{` intacte), état `dzTbDock` / clé `dz_svm_tb_dock` dans R_M11, prop `docked` → `data-docked` sur la barre seule, entrée ☰ › Affichage › « Ancrer la barre d'outils » ; **écarts** : l'état vit dans R_M11 (pas R_M16REF, dont le pin E-7 compte `localStorage` ×1) ; `data-off` garde son sens ancrée (repli à largeur 0, aucun `display:none`) ; compacité mesurée (flottante ≥ 80 px, ancrée ≈ 39 px, `--lbl:none` seul levier) ; poignée inerte ancrée (`saisir` et `clavier` sortent en tête) ; déclarations mortes sous `static` (`bottom:auto;left:auto;transform:none`) laissées et pinnées ; `margin-right:8px` s'ajoute au gap 12 px du bandeau, hors de l'arithmétique du §5.2 ; décalage de 12 px ancrée-repliée (le gap reste) ; spécificité des règles de compacité non pinnée | P2 |
| Popovers **modaux** avec voile (Project Manager, Quick Export, listes déroulantes) | `.svm-pop` `position:absolute; top:52px; right:18px` **sans voile** : tout reste cliquable sous un popover armé (mode remplacement, rendu) | **E-11** : voile discret (`rgba(0,0,0,.35)`) sous les popovers qui **arment un mode** (remplacement, rendu, projets) ; les popovers d'inspection (effets, transitions) restent sans voile — **exécuté 23/09/2026** : `1abe5bd` — `.svm-modescrim` monté entre `transPopover()` et `kbPanel()` sur `pop||dzFin`, clic = `setPop("")`+`setDzFin(null)`, racine `.svm-pop` avec `stopPropagation` ; **écarts** : voile `z-index:19` sous `.svm-pop` 20 (ordre DOM : le voile **suit** les popovers) ; SEULEMENT popover preview/rendu + bandeau de fin ; `ovPicker` **sans** voile (le glisser vers les bandes vit) ; opacité `.38` et variante claire `rgba(23,21,18,.22)` = celles de `.svm-kbscrim` (deux règles jumelles, feuille amont non patchable) et non `.35` ; Échap via `dzScrimRef` dans R_K7 (l'index des marqueurs se ferme d'abord) ; `stopPropagation` défensif (le voile est un frère, pas un parent) ; progression invisible hors popover (préexistant) ; statut « envoyé » du bandeau perdu à la fermeture ; voile/Échap ne purgent pas un job en échec (« Fermer » le fait) | P1 |
| Boutons « pilule » (rayon 12 px), états désactivés gris jamais masqués, infobulle partout | `.svm-secbtn`, `.svm-goldbtn`, chips ; certains boutons disparaissent selon l'état | **E-12** : règle d'audit (bancable) « un bouton se grise, ne disparaît pas » + infobulle obligatoire sur `.svm-tbtn`/`.dzm-tbb` — même mécanique que l'audit d'ergonomie du Vectorlab — **exécuté 23/09/2026** : `dc03a69`, `1f62a2b`, `072741c` — `backend/tests/test_montage_ergonomie.py` (36/0), onze sections EC15a..k + replis R_EB4 / R_EA5D, 21 titres ajoutés, bouton or et « bibliothèque » grisés sur la démo au lieu de disparaître ; **écarts** : audit par **scan des SOURCES** (DzMontage → en-tête SFX Studio, puis la couche ; SFX Studio, rack VFX et sous-titres hors audit) et non du DOM ; règle 1 = fenêtre de 400 c. coupée au prochain `r.jsx` / `r.jsxs` ; règle 2 avec liste TOLERES datée (2 bascules, 9 contextuels, 4 composants de la couche rendant null — et le banc croisé E-C vérifie que la liste == les conditionnels réellement présents, 11) ; « Rendre → » n'a jamais été absent (c'est le bouton or du popover qui disparaissait) ; « Oui » `svm-nbgold` hors classes auditées ; le titre du bandeau de fin fait 76 c. ; `.svm-libbtn:hover` s'applique encore au bouton grisé | P2 |
| Tête de lecture indiquée dans l'inspecteur et le viewer (21.1) ; presets d'inspecteur | inspecteur sans tête ; pas de presets | **E-13** : ligne « tête à HH:MM:SS·ii dans le plan » en tête de l'inspecteur ; « presets d'inspecteur » = **écarté** (peu de réglages par plan avant L3) — **exécuté 23/09/2026** : `6138fd1` — couche `dzmTeteTxt` (pure, formateur passé), section EC12 `.svm-insphead` entre la poignée et « Clip sélectionné » ; **écarts** : le format est **HH:MM:SS:FF à 30 i/s** (`svmTcFF`, celui de `.svm-tcmain`), pas « ·ii » ; intervalle fermé-ouvert (à `ph == end` la tête est « hors du plan ») ; section EC12 sur l'ancre libre « Clip sélectionné » et non un repli dans R_EB6A (pinné octet pour octet par le banc bundle) | P3 |
| Couper / copier / coller des **trous** (21.1) | supprimer un clip laisse un trou ; pas de « coller un trou » | **E-14** : « supprimer en ripple » existe (Maj+X sur plage) ; ajouter « sélectionner le trou » (clic sur le vide entre deux clips → trou sélectionné, Suppr = ripple) — **exécuté 23/09/2026** : `6138fd1`, `cc34c2c` — couche `dzmTrou` / `dzmTrouRipple` (pures), état `gapSel` + `gapSelRef` (R_M16REF), clic dans le vide d'une lane (EC10), rendu `.svm-gapsel` (EC11), Suppr (EC13), clic sur un clip (EC14), Échap (R_K7) ; **écarts** : le ripple agit sur **la piste du trou seule** (le jumeau A1 et les marqueurs D-5 ne suivent pas, comme D-4) ; la queue de piste n'est pas un trou (aucun clip suivant → null) ; seuil 0,05 s **flottant** (6,05−6 < 0,05 en IEEE : un tel trou est refusé, assumé) ; le temps est lu sur le rect de la lane (pas `phFromEvent`, qui déduirait la gouttière deux fois) ; le clic dans le vide ne déplace pas la tête et ne désélectionne pas le clip ; `gapSel` effacé par `useEffect([clips])` (revue : bornes périmées après tout `setClips`) ; Suppr traite le trou AVANT le losange et le clip, avec le verrou de piste | P3 |
| Transitions sur les clips d'ajustement (21.1) | piste d'ajustement = **D-9 (L3)** | à traiter dans D-9 : les clips `j1` acceptent `transition` (fondu du post-pass par `fade_in/fade_out` de l'effet) | — |

## 3. Ce que cette conception ne fait pas

- Pas de multi-timeline dans un projet, pas de bins/Smart Bins, pas de Cloud/Network : un projet reste une timeline, la « Bibliothèque » reste l'unique pool.
- Pas d'upload direct vers YouTube/TikTok depuis le Montage : la publication reste le Scheduler (E-4 lui donne un vrai bouton et un vrai formulaire).
- Pas de refonte des CSS `son-vfx-montage.css` (intouchable) : toutes les règles neuves vont dans `montage.css` ; les vues E-7 sont des états d'affichage, pas des écrans neufs.
- Aucun plan ni code avant la validation de ce tableau.

## 4. Ordre proposé si tout est validé

**Lot E-A (P0, ≈ 2,5 j)** : E-1 nouveau montage vide + grille de projets · E-3 « Ouvrir dans le Montage » depuis Studio et Chapitres (V1 + A1, scènes) + correction `v2` de la greffe existante · E-4 rendre/publier séparés + bandeau + `POST /publish` backend.
**Lot E-B (P0–P1, ≈ 3 j)** : E-2 tiroir Médias avec chips de provenance et pagination · E-5 barre de titre à trois verbes · E-11 voile des modes · E-8 inspecteur à bascule redimensionnable · E-9 séparateur + D-7. — **exécuté 23/09/2026** (branche `chantier/montage-eb`, plan `864e376`, T1–T8 `2849626` … `7b584f6`, clôture = mutations `backend/tests/mutations_montage_eb.py` 23/23 rouges 0 survivant 0 mort + banc croisé `test_montage_eb_croise.py` 35/0 + cette datation) ; bancs eb 24/0, ea 31/0, l3 86/0, projets 159/0, historique 51/0, coût 31/0, édition 245/0, bundle 1892/0, 141 ancres, sonde dzcout 123 ; **écarts transverses** : `.bak_dzcout` plus récent ⇒ toujours `repatch_all --from montage` ; un commit avec `-c core.autocrlf=false` met des CRLF dans l'index ; garde morte de `list_jobs` retirée ; tri `created_at` seul (rowid en cas d'égalité, 0 cas réel) ; deux formes de « `q` sensible à la casse » survivent aux mutations (LIKE SQLite insensible à la casse ASCII : ceinture, pas comportement observable) ; **déploiement et PR à l'utilisateur**
**Lot E-C (P1–P3, ≈ 2 j)** : E-6 menu ☰ + menus contextuels · E-7 trois vues · E-10 ancrage de la barre · E-12 audit · E-13/E-14. — **exécuté 23/09/2026** (branche `chantier/montage-ec`, plan `dbdc718`, T1–T6 `3208dea` … `072741c`, clôture = mutations `backend/tests/mutations_montage_ec.py` 21/21 rouges 0 survivant 0 mort + banc croisé `test_montage_ec_croise.py` 26/0 + cette datation) ; bancs ergonomie 36/0, édition 278/0 (277 + la sonde `rip_autre` de la clôture), bundle 2055/0, historique 51/0, projets 159/0, coût 31/0, eb 24/0, ea 31/0, l3 86/0, croisés eb 35/0 et l3 25/0, 164 ancres, sonde dzcout 132 ; **écarts transverses** : `.bak_dzcout` plus récent ⇒ toujours `repatch_all --from montage` ; un sélecteur de preuve `/lame|couper/i` tombe sur « plage : couper » (les libellés de menu concatènent la combo) ; le banc croisé E-B rougissait 32/3 AU REPOS après T2/T4 (le ☰ écrit `dz_svm_insp` et `dz_svm_showdur` une fois de plus, `dz_svm_tb_dock` est une clé neuve) — réaligné et daté par la clôture ; la mutation « ripple sur les autres pistes » survivait sur le comportement (aucun clip d'une autre piste ne commençait après le trou dans le jeu du banc) — sonde ajoutée ; **déploiement et PR à l'utilisateur**
Le lot L3 (propriétés de plan, plan du 22/09) reste indépendant et peut passer avant ou après E-A.
