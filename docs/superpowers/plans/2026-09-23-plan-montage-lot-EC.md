# Lot E-C du Montage — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal :** livrer E-6 (menu « ☰ » + menus contextuels clip/piste), E-7 (trois vues Médias · Montage · Livraison), E-10 (ancrage de la barre d'outils), E-12 (audit bancable des boutons et infobulles), E-13 (tête de lecture dans l'inspecteur) et E-14 (sélectionner un trou, Suppr = ripple) de `docs/superpowers/specs/2026-09-22-montage-vs-resolve-app-design.md` §2 (validée le 22/09/2026), sans complexifier : chaque geste réutilise un mécanisme mesuré.

**Architecture :** couche `frontend/patches/montage.js` = fonctions pures + UN composant de menu générique `DzmCtxMenu` (servi par ☰ et par les deux menus contextuels) + `DzmDeliver` (vue Livraison) ; patcher `scripts/patch_bundle_montage.py` = sections `EC1…` en queue, replis dans les remplacements hôtes ; CSS `frontend/dist/shared/montage.css` ; **aucun backend** (E-7 lit `GET /api/jobs?providers=montage` livré par E-B). Les actions à raccourci sont déclenchées par un `KeyboardEvent` synthétique rejouant leur combo (le dispatch reste `onKey`, unique) ; les actions sans raccourci appellent les fonctions de l'hôte.

**Tech :** bundle React patché en octets, bancs Python maison lancés par le python embarqué, Playwright pour les preuves.

---

## Conventions valables pour tout le plan

```powershell
$PY = "$env:LOCALAPPDATA\DeepotusVideoGen\runtime\python\python.exe"
Set-Location backend
& $PY tests\test_montage_edition.py      # 245/0 au départ
& $PY tests\test_montage_bundle.py       # 1892/0
& $PY tests\test_montage_ergonomie.py    # NEUF (T6)
& $PY tests\test_montage_historique.py   # 51/0
& $PY tests\test_montage_projets.py      # 159/0
& $PY tests\test_montage_eb.py           # 24/0
& $PY tests\test_cout_pastille.py        # 31/0
Set-Location ..
& $PY scripts\patch_bundle_montage.py --check --force-unchained   # 141 ancres au départ (140 triplets + ANCHOR_INJECT)
& $PY scripts\repatch_all.py --from montage ; & $PY scripts\repatch_all.py --list
node --check frontend\dist\assets\index-BEOJX8L5.js ; node --check frontend\patches\montage.js
```

- Un banc = un processus, jamais `pytest`. `check(label, cond, detail)`, fin `=== N passed, M failed ===`, code de sortie. Règle des assertions négatives (témoin positif dans la même expression), état vide construit (shim sur fichier vide pour la couche, `.bak_montage` pour le bundle), faute n°6 (aucune lecture nue avant un check).
- Ancres comptées sur `.bak_montage` (1 dans `.bak`, 0 dans le patcher hors sa section) ; ancre consommée → repli dans le remplacement hôte ; sections nouvelles EN QUEUE après `EB8b-fenetre-visible-de-la-mini-carte`. **Préfixe de ce lot : `EC`.** Le `.bak_dzcout` est plus récent : TOUJOURS `repatch_all --from montage`. Bundle en OCTETS ; après rejeu : `--list`, `node --check`, CRLF == LF, `git hash-object`, sonde `("montage","DzTracks",123)` (`scripts/patch_bundle_dzcout.py:348`, `str.count` brut du jeton nu) remesurée et justifiée ligne à ligne. Commits `--only`, config git normale (jamais `-c core.autocrlf=false`), première ligne sans accent, trailer EXACT `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`, push de `chantier/montage-ec`. Pas de heredoc bash pour du code. `localStorage` : clés `dz_svm_*`, try/catch. Chaque agent CONTESTE le plan si la mesure le contredit (écart daté).
- Preuve à l'écran par le contrôleur (backend du worktree sur 8799, projet réel « preuve e3 », Playwright + Chrome).

## Faits mesurés le 23/09/2026 (relevé lecture seule, état cd2d529)

- **Raccourcis** : table `SVM_ACTIONS` (`.bak:1522-1557`, niveau module ; entrée `{id,sec,lbl,combo}`, PAS de champ action), `SVM_KEY_SECTIONS=["Lecture","Montage","Affichage","Audio"]`, `SVM_ACTION_BY_ID` ; `R_R1` ajoute 12 actions (`range_in/out/clear/cut`, `marker_toggle/prev/next/index`, `swap_left/right`, `title_add`, `adjust_add`) + `toolbar` ; keymap `svmKmLoad/Save/Merge` (`dz_svm_keymap`, `{byId,toAct}`), `svmKeyLabel(id)` DANS `DzMontage` (lit `km.byId`), `svmKeyLabelNow(id)` au module. **Dispatch = `onKey` dans un `useEffect` (écouteur `window`), aucun `dispatch(id)` n'en sort** ; précédent pour une action à double entrée = fonction de l'hôte (`dzTtAdd`, `dzAjAdd`, `dzMkToggle`). `Delete`/`Backspace` lues comme « Suppr » (`.bak:1580`) ; branche `if(id==="delete"){` → `vpDel()` puis `        delClip();return}` (ancres libres).
- **Handlers de clip** : `blade()` (`useCallback`, `.bak:2902`), `delClip()`/`delClipById(id)` (`.bak:1902/1916`), remplacement de source = geste dans `R_M16` (`dzmReplaceRef.current=…;setDzmArm(…)` puis sélecteur ; `DzTracks.replaceBtn(sel,onArm)` rend null sans `sel.src`), effets `setFxPick(!fxPick)` (`.bak:4028`), vitesse `svmSetV1Speed(id,v)` V1 seulement 0,25..4 (`.bak:3158`), transition `openTransPopAt(id,cx0)` id = clip de DROITE (`.bak:3501`). **Piste** : `svmTrackLock(trId)` (`.bak:3329`), `svmTrackMute` (`:3315`), `svmTrackSolo(trId,additive)` (`:3337`) — muet/solo inertes sans bus (vidéo) ; `trackSt` `{l,m,s}` ; suppression = `del()` INTERNE à `DzmTrackBtns` (couche :813, armement deux clics) ; exposé : `DzTracks.remove(ts,id)` + `svmTracksSet(ts2)` + filtrage des clips.
- **Clic droit** existant : un seul, `.svm-vph` `onContextMenu:{preventDefault;stopPropagation;svmVpRemove}` (`.bak:5538`). Popovers maison : `.svm-pop` (racines `B:4637/4675/4939`), voile E-11 `(pop||dzFin)?…svm-modescrim` (`R_EB5A`), témoin `dzScrimRef`, Échap dans `R_K7`. `dzm-menu`/`dzm-ctx` = 0 partout.
- **Barre de titre** `.svm-titlebar` (`.bak:4940`) : `svm-title` « Montage » (ancre libre) → `svm-projmeta` → `svm-libbtn` → badge → bloc droit `marginLeft:auto` (format, Preview, Rendre →, Publier, médias, inspecteur, durées, sons, narration, thème).
- **Racine** `.dzsvm.svm-col` : ligne `  return r.jsxs("div",{className:"dzsvm svm-col",ref:rootRef,"data-svm-theme":theme==="light"?"light":void 0,children:[` (1/0/1) ; enfants : titlebar, popover(), bandeau, fxPicker, ovPicker + MarkerIndex, transPopover, voile, kbPanel, `.svm-mid` (tiroirs, MediaDrawer, `.svm-playerzone`, aside), `.svm-tlhandle`, `.svm-tl` (`.svm-trans` avec ToolDock M19, mini-carte, `.svm-scroll`). Fin de `.dzsvm` : `r.jsx("div",{className:"svm-translabel",ref:transLabelRef})]})})]})]})}` (1/0/1 ; le dernier `]})` ferme `.dzsvm`). `data-view` = 0 partout.
- **Historique des rendus** : `GET /api/jobs?providers=montage&q=&limit=&offset=` (E-B) ; `title` d'un job montage = `str(body.name or "montage")[:60]` + « (aperçu 480p) » pour une preview (`montage_service.py:3222-3229`) ; aucun `project_id` en base ; `dz_montage_lastfin` = dernier rendu final par projet (`{job_id,name,at}`).
- **Barre d'outils** : `DzmToolDock(o)` (couche :4267) rend `DzmToolBar` (:4231) `div#DZM_TB_ID.dzm-tbar` avec `data-off/data-noanim/data-drag`, `style:{--tbx,--tby}` ; montée par `R_M19` en TÊTE de `.svm-trans` (ancre consommée) ; CSS `.dzsvm .dzm-tbar{position:absolute;left:14px;top:auto;bottom:calc(100% + 8px);z-index:8}` (`montage.css:552`), `[data-off]` `translateY(-6px)` (:573), `.svm-trans{position:relative}` (:511) ; clés `dz_svm_tb_open`, `dz_svm_tb_off` ; pins à ne PAS casser : `D1_la_barre_flotte_AU_DESSUS…` (bundle :1302) et `tb_la_geometrie…` (:11112) lisent la PREMIÈRE règle `.dzsvm .dzm-tbar{` — une règle séparée `[data-docked]` ne les touche pas. Transport `.svm-trans` : `.svm-tcmain` (ancre libre, `title:"position / durée totale — HH:MM:SS:image (30 i/s)"`), `svm-spdchip`, `svm-solochip`, premier `.svm-transbtns` (ancre libre).
- **Audit** : précédents = `frontend/vectorlab/qa/ergonomie.test.mjs` (banc node de la fonction pure `auditer(mesures)` de `mod-controles.js:38`) et `backend/tests/test_subs_regle_des_gestes.py` (règle verrouillée par scan des SOURCES). Boutons qui DISPARAISSENT (13 sites, `B:4650, 4717, 5030, 5600, 5657, 5817, 6031, 6265, 6270, 14626, 14665, 19372` + 4 composants de la couche rendant null). Infobulles : `.svm-tbtn` 23/23 titrés ; `.dzm-tbb` fabrique unique `title:o.title||lbl` ; `svm-secbtn` **10 sans `title`** : `B:4649, 4690, 4976, 5083 (Fermer), 5633 (Preview), 18340, 19044, 19045, 19363, 19372` ; précédent de grisé `data-off` sur annuler/rétablir (`.bak:5245-5247`).
- **Inspecteur** : `R_EB6A` écrit `inspOn?r.jsxs("aside",{…children:[r.jsx("div",{className:"svm-insphandle",…}),` ; premier enfant d'origine `        r.jsx(SvmLabel,{children:"Clip sélectionné"}),` (1/0/1) ; `ph` (état `.bak:1694`) + `phRef` ; `sel` (`.bak:2098`, `sel.start/end/srcIn`) ; formateur `svmTcFF(s)` HH:MM:SS:FF à 30 i/s (`.bak:363`, celui de `.svm-tcmain`).
- **Bande vide** `.svm-lane` (`.bak:5412`) : NI `onPointerDown` NI `onClick` (seulement `onDragOver`/`onDrop` — ancre libre `                onDrop:function(e){dropOnTrack(e,tr.id,e.currentTarget)},`) ; la tête ne se pose que par la règle (`rulerDown`, `phFromEvent(e,el)` gouttière 88 px déduite) ; `svmV1Gaps(clips,dur)` dessine `.svm-gap` sur V1 seulement (ancre libre `                tr.id==="v1"?svmV1Gaps(clips,dur):null,`) ; `dzmVoisins` ne trouve que les voisins AU CONTACT (0,1 s) — pas les bords d'un trou ; `DzTracks.rippleCut(clips,a,b,DzTracks.cutOpts(dzProjRef.current,trackStRef.current))` → `{clips,removed}` agit sur TOUTES les pistes (chemin de `range_cut`, `R_R2`) ; `.dzm-range` (`montage.css:943`) = bande de plage sur la règle.
- **Sonde** 123 ; `PATCHES` = 140 triplets ; préfixes pris `M E H R T K X V TT EA DZ KF AJ EB`.

### Décisions prises sur les démentis (datées à la clôture)

1. **Pas de `dispatch(id)`** → les entrées de menu à raccourci rejouent la combo par `window.dispatchEvent(new KeyboardEvent("keydown", dzmComboToKey(combo)))` : `onKey` reste l'unique dispatch, zéro duplication ; si `onKey` filtre par `e.target` (mesurer : `tagName`/`isContentEditable`), un événement dont la cible est `window`/`document.body` passe. Les entrées SANS raccourci (Projets…, Preview, Rendre, Publier, effets, vitesse, transition, remplacer, verrou, muet, solo, supprimer la piste, ancrer la barre, aide) appellent les fonctions de l'hôte. Le geste « remplacer la source » de `R_M16` est EXTRAIT en `dzReplaceArm()` (fonction de l'hôte) appelée par le bouton existant ET le menu.
2. **Six rubriques** (Projet · Édition · Timeline · Marqueurs · Affichage · Aide) ≠ quatre `sec` : table `DZM_MENU_RUB` id → rubrique pour les ids connus, repli sur `sec` (Lecture/Montage → Timeline, Audio → Édition), rubriques rendues dans l'ordre fixe, vides omises.
3. **Un seul composant** `DzmCtxMenu({x,y,items,onClose})` sert ☰ (ancré sous le bouton) et les deux menus contextuels (au pointeur) ; `items` = `[{lbl, combo?, run, off?, sep?}]` ; voile E-11 étendu (`pop||dzFin||dzMenu`), Échap ferme (repli `R_K7`).
4. **E-7** : `view` = état de session (`"montage"` au montage, pas de localStorage — chaque ouverture repart sur l'écran complet), `data-view` sur la racine, barre basse `.svm-views` insérée avant la fermeture de `.dzsvm` ; « Médias » = `data-view` + `setMedOn(!0)` (garde démo `_EB_GARDE`, autres tiroirs fermés) ; « Livraison » = `.svm-tl` masquée + panneau `DzmDeliver` DANS `.svm-mid` (boutons Preview/Rendre/Publier réutilisant les handlers, historique `GET /api/jobs?providers=montage&q=<nom>&limit=24` avec les aperçus séparés des rendus finaux par le suffixe « (aperçu 480p) », « Voir dans la Bibliothèque ») ; presets = D-35 (écart).
5. **E-10** : `docked` = état lu de `dz_svm_tb_dock`, passé en prop `docked` à `DzmToolDock` qui pose `"data-docked":""` sur `.dzm-tbar` ; CSS `.dzsvm .dzm-tbar[data-docked]{position:static;bottom:auto;transform:none;margin-right:8px}` (règle SÉPARÉE, la première règle reste intacte pour les pins D-1) ; la barre étant déjà en tête de `.svm-trans` (R_M19), elle se pose « à gauche des boutons » par le flux ; bascule dans ☰ › Affichage.
6. **E-12** : audit = banc de SOURCES `backend/tests/test_montage_ergonomie.py` (règle 1 : dans `DzMontage` et la couche, tout `r.jsx("button",{className:"svm-tbtn|dzm-tbb|svm-secbtn|svm-minibtn…` porte `title:` — corriger les 10 `svm-secbtn` sans titre, y compris ceux de la couche ; règle 2 : un bouton `.svm-tbtn`/`.dzm-tbb`/`.svm-secbtn` ne disparaît pas selon l'état — pin sur l'absence de `?r.jsx("button",{className:"svm-tbtn` / `svm-secbtn` conditionnel dans `DzMontage`, avec les 13 sites existants convertis quand ils sont des boutons de ces classes (`4650` goldbtn/failed, `5600` libbtn → `disabled` + `title`), les chips (`svm-sfxchip`, `svm-duckbtn`, M/S de piste sans bus, `svm-minibtn` overlay, kbreset, projets, medplus, composants de la couche qui rendent null) restant DATÉS comme écarts (ce sont des contrôles contextuels, pas des boutons d'outil).
7. **E-13** : pure `dzmTeteTxt(ph, sel, fmt)` → « tête à HH:MM:SS:FF » + « · +HH:MM:SS:FF dans le plan » si `sel` et `sel.start ≤ ph < sel.end`, sinon « · hors du plan » ; `svmTcFF` réutilisé (pas de « ·ii »).
8. **E-14** : pure `dzmTrou(clips, tr, t)` → `{a,b}` (fin du clip précédent, début du suivant sur la piste ; `null` si `t` est dans un clip, ou sans clip suivant — la queue de piste n'est pas un trou) ; `dzmTrouRipple(clips, tr, a, b)` → décale de `-(b-a)` les clips de CETTE piste dont `start ≥ b` (pure, autres pistes intactes — écart daté : le jumeau A1 ne suit pas, comme D-4) ; hôte : `onPointerDown` sur `.svm-lane` (cible = la lane elle-même, bouton 0, hors démo) → `setGapSel({tr,a,b}|null)` ; rendu `.svm-gapsel` dans la lane (position % comme les clips) ; `Suppr` : si `gapSel` → `pushHistory()` + `setClips(dzmTrouRipple(...))` + note « trou de N s refermé » + `setGapSel(null)` (repli sur la branche `if(id==="delete"){` : AVANT `vpDel()`), Échap efface (repli `R_K7`), clic sur un clip efface (`clipDown`).

## Fichiers

| Fichier | Rôle |
|---|---|
| `frontend/patches/montage.js` | `dzmComboToKey`, `DZM_MENU_RUB`, `dzmMenuModel`, `DzmCtxMenu` (T1) ; `DzmDeliver`, `dzmJobsTri` (T3) ; `dzmTeteTxt`, `dzmTrou`, `dzmTrouRipple` (T5) ; titres des boutons de la couche (T6) |
| `scripts/patch_bundle_montage.py` | sections `EC1…` ; replis `R_K7` (Échap), `R_EB5A` (voile), `R_M16` (`dzReplaceArm`), `R_M19` (`docked`), `R_EB6A` (E-13), `R_EB2` (rien : ☰ à gauche du titre) |
| `frontend/dist/shared/montage.css` | `.svm-menu*`, `.svm-views`, `[data-view]`, `.svm-deliver*`, `.dzm-tbar[data-docked]`, `.svm-insphead`, `.svm-gapsel` |
| `backend/tests/test_montage_edition.py` | section `[20]` couche E-C |
| `backend/tests/test_montage_bundle.py` | section `[EC]` |
| `backend/tests/test_montage_ergonomie.py` | NEUF (T6) : règles 1 et 2 par scan des sources |
| `scripts/patch_bundle_dzcout.py` | sonde |
| `backend/tests/mutations_montage_ec.py` | harnais (T7) |
| conceptions | lignes E-6, E-7, E-10, E-12, E-13, E-14, §4 Lot E-C datées (T7) |

---

## Tâche 1 — E-6 couche : combo → touche, modèle de menu, `DzmCtxMenu`

**Files :** `frontend/patches/montage.js`, `backend/tests/test_montage_edition.py` `[20]`.

- [ ] **Banc rouge [20]** (shim vide = état vide ; sonde node) :

```js
out.combo = [T.comboToKey("Alt+C"), T.comboToKey("Ctrl+Maj+X"), T.comboToKey("Suppr"), T.comboToKey("Maj+M"), T.comboToKey(""), T.comboToKey("Ctrl+←")];
out.menu = T.menuModel([{id:"blade",sec:"Montage",lbl:"lame",combo:"Alt+C"},{id:"undo",sec:"Montage",lbl:"annuler",combo:"Ctrl+Z"},{id:"marker_toggle",sec:"Montage",lbl:"marqueur",combo:"Maj+M"},{id:"zoom_in",sec:"Affichage",lbl:"zoom",combo:"Ctrl+="},{id:"gain_up",sec:"Audio",lbl:"gain",combo:"Alt+↑"}], function(id){return id==="blade"?"Alt+C*":null}).map(function(r){return [r.rub, r.items.map(function(i){return i.id+":"+(i.combo||"")})]});
out.menu_vide = T.menuModel([], null).length;
out.ctx_pure = typeof T.CtxMenu;
```

Checks : `comboToKey("Alt+C")=={key:"c",altKey:true,ctrlKey:false,shiftKey:false,metaKey:false}` (clé en minuscule, `Maj`→shift, `Suppr`→`"Delete"`, `←`→`"ArrowLeft"`, `""`→`null`) ; `menu` = rubriques dans l'ordre fixe `Projet, Édition, Timeline, Marqueurs, Affichage, Aide` vides omises : `[["Édition",["undo:Ctrl+Z","gain_up:Alt+↑"]],["Timeline",["blade:Alt+C*"]],["Marqueurs",["marker_toggle:Maj+M"]],["Affichage",["zoom_in:Ctrl+="]]]` (le libellé de combo vient de `keyLabel(id)` quand il rend non-null, sinon `combo` de la table) ; `menu_vide==0` ; `ctx_pure=="function"` ; cœur pur (`comboToKey`, `menuModel` sans `r.jsx`/`x.use`/`window`), composant lit `r`/`x` à l'appel ; état vide.

- [ ] **Implémenter** : `dzmComboToKey(combo)` (parse `+`, jetons `Ctrl|Maj|Alt|Cmd`, clé finale : lettre → minuscule, `Suppr→Delete`, `Échap→Escape`, `Espace→" "`, `←↑→↓→Arrow*`, `Entrée→Enter`, `=`/`-` tels quels ; vide → null) ; `DZM_MENU_RUB={undo:"Édition",redo:"Édition",delete:"Édition",ripple:"Édition",range_cut:"Édition",swap_left:"Édition",swap_right:"Édition",gain_up:"Édition",gain_down:"Édition",marker_toggle:"Marqueurs",marker_prev:"Marqueurs",marker_next:"Marqueurs",marker_index:"Marqueurs",toolbar:"Affichage",zoom_in:"Affichage",zoom_out:"Affichage",…}` (lire les ids réels de `SVM_ACTIONS` + `R_R1` dans le bundle et remplir la table : Lecture/Montage → « Timeline » par défaut, Affichage → « Affichage », Audio → « Édition ») ; `dzmMenuModel(actions, keyLabel)` → `[{rub, items:[{id,lbl,combo}]}]` ordre `DZM_MENU_ORDRE=["Projet","Édition","Timeline","Marqueurs","Affichage","Aide"]` ; `DzmCtxMenu(o)` : `o.items=[{lbl,combo,run,off,sep}]`, `o.x,o.y` (ou `o.anchor` rect) → `div.svm-pop.svm-menu` positionné `style:{left,top}` (borné à la fenêtre : `Math.min(x, innerWidth-260)`), rangée `button.svm-menuitem` (`disabled:off`, `title`), `span.svm-menukey` pour la combo, `div.svm-menusep`, en-tête `div.svm-menurub` si `o.rubs` (pour ☰ : `[{rub,items}]`), `onClick` → `run()` puis `o.onClose()`, `stopPropagation` sur la racine ; hooks : aucun. Exports `comboToKey, menuModel, CtxMenu`.
- [ ] Vert, `node --check`, commit `montage : E-6 - combo vers touche, modele de menu et DzmCtxMenu dans la couche`, push.

---

## Tâche 2 — E-6 bundle : bouton ☰, menu, clic droit clip et piste

**Files :** `scripts/patch_bundle_montage.py`, `frontend/dist/shared/montage.css`, `backend/tests/test_montage_bundle.py` `[EC]`, `scripts/patch_bundle_dzcout.py`.

- [ ] **Banc rouge [EC]** puis implémenter :
  - **EC1** état : `var stMn=x.useState(null),dzMenu=stMn[0],setDzMenu=stMn[1];` (`{kind:"main"|"clip"|"track",x,y,id}`) sur une ancre libre (ex. `  var delClip=x.useCallback(…)` reprise) ; `dzFire(id)` = `var k=DzTracks.comboToKey(svmKeyLabel(id)||(SVM_ACTION_BY_ID[id]||{}).combo);if(k)window.dispatchEvent(new KeyboardEvent("keydown",Object.assign({bubbles:!0,cancelable:!0},k)))` — MESURER d'abord que `onKey` accepte un événement dont `target===window` (lire son filtre `e.target.tagName`/`isContentEditable`) ; sinon dispatcher sur `document.body`.
  - **EC2** bouton ☰ : ancre `      r.jsx("span",{className:"svm-title",children:"Montage"}),` → insère AVANT `      r.jsx("button",{className:"svm-secbtn svm-menubtn",title:"Menu (actions et raccourcis)","aria-haspopup":"menu",onClick:function(e){var b=e.currentTarget.getBoundingClientRect();setDzMenu(dzMenu&&dzMenu.kind==="main"?null:{kind:"main",x:b.left,y:b.bottom+4})},children:"☰"}),`.
  - **EC3** rendu : `dzMenu?r.jsx(DzTracks.CtxMenu,dzMenuProps()):null,` inséré à côté du voile (ancre `    transPopover(),\n    kbPanel(),` est CONSOMMÉE par EB5a → repli dans `R_EB5A` ; le voile devient `(pop||dzFin||dzMenu)?…onClick:function(){setPop("");setDzFin(null);setDzMenu(null)}`) ; `dzScrimRef.current=!!(pop||dzFin||dzMenu)` (repli `R_M16REF`) ; Échap : `R_K7` → `setDzMenu(null)` ajouté à la branche du voile.
  - `dzMenuProps()` (fonction de l'hôte, EC1) construit les items : `main` → `rubs = DzTracks.menuModel(SVM_ACTIONS.concat(<actions R_R1 si elles sont dans une autre table : mesurer>), svmKeyLabel)` avec `run:function(){dzFire(id)}`, PLUS entrées sans raccourci : Projet → « Projets… » (`setDzProjReq(+1)` — mesurer le déclencheur réel du popover projets), « Preview 480p » (`setPop("preview")`), « Rendre… » (`setPop("render")`), « Publier » (`off:!dzLast`, `setDzFin(Object.assign({project_id:…},dzLast))`) ; Affichage → « Inspecteur » (bascule E-8), « Médias » (E-2, garde démo), « Durées sur les clips » (E-9), « Ancrer la barre d'outils » (T4, `off` tant que T4 n'est pas livré : NON — T4 l'ajoute) ; Aide → « Raccourcis (?) » (`setKbOn(!0)`), « Guide » (lien existant : mesurer `svm-guide`/href du Guide dans le rail, sinon omettre et dater). `clip` → items : « Couper à la tête » (`dzFire("blade")`, `off` si la tête est hors du clip), « Supprimer » (`delClipById(id)`), « Remplacer la source… » (`dzReplaceArm()` extrait de `R_M16`, `off` si pas `src` ou pas V1), « Effets… » (`setFxPick(!0)`), sep, « Vitesse » ×6 (`25/50/75/100/150/200 %` → `svmSetV1Speed(id,v)`, `off` hors V1 ; entrées à plat préfixées « Vitesse 100 % »), sep, « Transition… » (`openTransPopAt(id,x)`, `off` si le clip est premier de sa piste : `DzTracks.voisins` `g` null). `track` → « Verrouiller/Déverrouiller » (`svmTrackLock`), « Muet »/« Solo » (`off` sans bus), sep, « Supprimer la piste » (`svmTracksSet(DzTracks.remove(tracks,id))` + `setClips(clips.filter(c=>c.tr!==id))` + `pushHistory` — reprendre EXACTEMENT ce que fait `del()` de la couche après armement : mesurer et copier, `off` sur les pistes non supprimables comme `del()` le décide).
  - **EC4** clic droit clip : ancre `                    onPointerDown:function(e){clipDown(e,c,e.currentTarget.parentElement)},` → ajoute `onContextMenu:function(e){e.preventDefault();e.stopPropagation();setSelId(c.id);setDzMenu({kind:"clip",id:c.id,x:e.clientX,y:e.clientY})},` ; **EC5** piste : ancre `              r.jsxs("div",{className:"svm-thead"+(bus?" svm-thead-a":""),children:` → ajoute `onContextMenu:function(e){e.preventDefault();setDzMenu({kind:"track",id:tr.id,x:e.clientX,y:e.clientY})},`.
  - CSS : `.dzsvm .svm-menu{width:260px;padding:6px;z-index:20}`, `.svm-menurub{font:600 10px/1 …;opacity:.6;padding:6px 8px 2px;text-transform:uppercase}`, `.svm-menuitem{display:flex;width:100%;justify-content:space-between;gap:12px;padding:6px 8px;border:0;background:transparent;color:inherit;border-radius:6px;cursor:pointer;text-align:left}` `:hover{background:color-mix(in srgb,var(--accent) 12%,transparent)}` `:disabled{opacity:.45;cursor:not-allowed}`, `.svm-menukey{opacity:.6;font-family:ui-monospace,monospace;font-size:11px}`, `.svm-menusep{height:1px;background:var(--stroke);margin:4px 0}`.
  - Pins : `svm-menubtn` ×1, `DzTracks.CtxMenu` ×1, `DzTracks.menuModel(` ×1, `DzTracks.comboToKey(` ×1 (sonde +3 → 126, mesure), `onContextMenu` 1 → 3, `pop||dzFin||dzMenu` ×2 (voile + ref), `setDzMenu(null)` ≥ 3, `dzReplaceArm` défini ×1 et appelé ×2 (bouton + menu), `.bak` à 0 ; assertions négatives, faute n°6.
- [ ] Chaîne, bancs (bundle, édition, historique, projets, coût), commit `montage : E-6 - menu principal et menus contextuels clip et piste`, push. Preuve écran : ☰ → six rubriques max avec combos affichées, « Couper à la tête » depuis ☰ coupe réellement (clic → 2 clips V1), Échap/voile ferment ; clic droit clip → menu, « Supprimer » supprime, « Vitesse 200 % » persiste ; clic droit piste → « Verrouiller » verrouille (🔒).

---

## Tâche 3 — E-7 : trois vues Médias · Montage · Livraison

**Files :** `frontend/patches/montage.js` (`DzmDeliver`, `dzmJobsTri`), `scripts/patch_bundle_montage.py`, `frontend/dist/shared/montage.css`, bancs `[20]`/`[EC]`.

- [ ] **Couche** : `dzmJobsTri(jobs, nom)` pure → `{finals:[…], previews:[…]}` (jobs `provider==="montage"`, titre commençant par `nom` ; `previews` = titre contenant « (aperçu 480p) » ; tri `created_at` décroissant conservé) ; `DzmDeliver(o)` `{nom, onPreview, onRender, onPublish, publishOn, lastFin, jobs, st, onOpenLib}` → `div.svm-deliver` : titre « Livraison », trois boutons (`svm-secbtn` avec `title`, `svm-goldbtn` pour Rendre), « Dernier rendu final : <name> · <date>» ou « aucun », liste `.svm-delrow` (titre, date locale, durée `dzmDurTxt(duration_s)`, badge « aperçu »/« final »), bouton « Voir dans la Bibliothèque » (`onOpenLib`) ; hooks : aucun (le fetch est dans l'hôte). Banc [20] : `jobsTri` (deux finals, une preview, un job d'un autre projet exclu, un job non-montage exclu), état vide, pureté, composant.
- [ ] **Bundle** : **EC6** état `view` (`x.useState("montage")`) + `dzJobs` (`useState([])`) + effet `[view]` : si `view==="livraison"` → `fetch("/api/jobs?providers=montage&limit=24&q="+encodeURIComponent(proj.name||""))` → `setDzJobs` (vivant/démonté comme le tiroir) ; `dzSetView(v)` : `if(v==="medias"){<garde démo>;setMedOn(!0);setSfxOn(!1);setSubsOn(!1);setNarrOn(!1)}` `setView(v)` ; **EC7** racine : `"data-svm-theme":…` → ajoute `"data-view":view,` ; **EC8** barre basse : ancre de fin de `.dzsvm` → insère avant le dernier `]})` : `r.jsxs("div",{className:"svm-views",role:"tablist",children:[["medias","Médias"],["montage","Montage"],["livraison","Livraison"]].map(function(v){return r.jsx("button",{className:"svm-viewbtn",role:"tab","aria-selected":view===v[0],"data-on":view===v[0]?"":void 0,title:"Vue "+v[1],onClick:function(){dzSetView(v[0])},children:v[1]},v[0])})})` ; **EC9** panneau Livraison dans `.svm-mid` : ancre `    r.jsxs("div",{className:"svm-mid",children:[` (1/0/1) → ajoute après : `      view==="livraison"?r.jsx(DzTracks.Deliver,{nom:proj.name,onPreview:function(){setPop("preview")},onRender:function(){setPop("render")},onPublish:function(){…dzLast…},publishOn:!!dzLast,lastFin:dzLast,jobs:dzJobs,onOpenLib:function(){props.go&&props.go("library")}}):null,` (mesurer le nom réel de la navigation vers la Bibliothèque : `props.go("library")` est le précédent E-4 « Voir dans la Bibliothèque » du bandeau — copier). CSS : `.dzsvm .svm-views{display:flex;gap:4px;justify-content:center;padding:4px;border-top:1px solid var(--stroke);flex:none}`, `.svm-viewbtn{…}` `[data-on]{color:var(--accent);box-shadow:inset 0 -2px 0 var(--accent)}` ; `.dzsvm[data-view="medias"] .svm-tlhandle,.dzsvm[data-view="medias"] .svm-tl,.dzsvm[data-view="livraison"] .svm-tlhandle,.dzsvm[data-view="livraison"] .svm-tl{display:none}` ; `.dzsvm[data-view="medias"] .svm-meddrawer{width:520px;min-width:520px}` ; `.dzsvm .svm-deliver{flex:1;padding:16px;overflow:auto}` + `.svm-delrow`.
  - Pins : `data-view` ×1 racine + CSS ≥ 4, `svm-views` ×1/×1, `DzTracks.Deliver` ×1 (sonde +1), `providers=montage` ×1, `view==="livraison"` ≥ 2, `.bak` 0.
- [ ] Chaîne, bancs, commit `montage : E-7 - trois vues Medias Montage Livraison`, push. Preuve : barre basse, « Médias » → tiroir 520 px + timeline masquée, « Livraison » → panneau avec l'historique des rendus « preuve e3 » (finals/aperçus séparés), « Montage » → écran complet ; F5 → « Montage ».

---

## Tâche 4 — E-10 : ancrer la barre d'outils

**Files :** couche (`DzmToolDock`/`DzmToolBar` : prop `docked` → `"data-docked":o.docked?"":void 0`), patcher (`R_M19` : `docked:dzTbDock` ; état `dzTbDock` lu de `dz_svm_tb_dock` ; entrée ☰ › Affichage « Ancrer la barre d'outils » qui bascule + persiste), CSS, bancs.

- [ ] CSS : `.dzsvm .dzm-tbar[data-docked]{position:static;bottom:auto;left:auto;transform:none;margin-right:8px;flex:none}` (règle SÉPARÉE après la première ; vérifier `[data-off]` : quand ancrée, `data-off` ne doit plus la faire glisser hors vue : `.dzsvm .dzm-tbar[data-docked][data-off]{transform:none}` — mesurer ce que fait `data-off` exactement). Pins : `data-docked` ×1 couche ×1 CSS, `dz_svm_tb_dock` ×2, la PREMIÈRE règle `.dzsvm .dzm-tbar{` inchangée (pins D-1 verts), `.bak` 0.
- [ ] Chaîne, bancs, commit `montage : E-10 - ancrage de la barre d outils dans le transport`, push. Preuve : ☰ › Affichage › Ancrer → `.dzm-tbar` `position:static` mesuré, dans `.svm-trans` avant `.svm-tcmain`, F5 conservé ; désancrer → flottante.

---

## Tâche 5 — E-13 tête dans l'inspecteur + E-14 sélectionner le trou

**Files :** couche (`dzmTeteTxt`, `dzmTrou`, `dzmTrouRipple`), patcher (repli `R_EB6A` pour E-13 ; sections EC10 lane, EC11 rendu du trou, repli branche `delete`, repli `R_K7` Échap, repli `clipDown` ? non : `setGapSel(null)` dans `clipDown` = ancre `  function clipDown(` à mesurer), CSS, bancs.

- [ ] **Couche** : `dzmTeteTxt(ph, sel, fmt)` → `"tête à "+fmt(ph)+(sel?(sel.start<=ph&&ph<sel.end?" · +"+fmt(ph-sel.start)+" dans le plan":" · hors du plan"):"")` ; `dzmTrou(clips, tr, t)` → parcourt les clips de `tr` triés par `start` : si un clip contient `t` → null ; `a` = max `end` des clips finissant ≤ t (0 si aucun), `b` = min `start` des clips commençant > t (null si aucun → null) ; retourne `{a,b}` si `b-a ≥ 0.05` ; `dzmTrouRipple(clips, tr, a, b)` → nouveau tableau où les clips de `tr` avec `start ≥ b` sont décalés de `-(b-a)` (start/end), autres intacts. Banc [20] : tête dans/hors plan, sans sel ; trou entre 2 clips, en tête de piste (a=0), dans un clip (null), en queue (null), autre piste ignorée ; ripple décale les bons clips seulement, non-mutation.
- [ ] **Bundle** : E-13 : repli `R_EB6A` → après la poignée : `r.jsx("div",{className:"svm-insphead",children:DzTracks.teteTxt(ph,sel,svmTcFF)}),` ; E-14 : état `gapSel` (EC1 ou ancre libre) ; **EC10** ancre `                onDrop:function(e){dropOnTrack(e,tr.id,e.currentTarget)},` → ajoute `onPointerDown:function(e){if(e.button!==0||e.target!==e.currentTarget||proj.demo)return;var t=phFromEvent(e,e.currentTarget);var g=DzTracks.trou(clipsRef.current,tr.id,t);setGapSel(g?{tr:tr.id,a:g.a,b:g.b}:null)},` (mesurer `phFromEvent` signature et `clipsRef`) ; **EC11** rendu : ancre `                tr.id==="v1"?svmV1Gaps(clips,dur):null,` → ajoute `gapSel&&gapSel.tr===tr.id?r.jsx("div",{className:"svm-gapsel",style:{left:(gapSel.a/dur*100)+"%",width:((gapSel.b-gapSel.a)/dur*100)+"%"},title:"Trou sélectionné — Suppr le referme (ripple sur cette piste)"}):null,` ; Suppr : ancre `      if(id==="delete"){` → `      if(id==="delete"){if(gapSelRef.current){var gs=gapSelRef.current;pushHistory();setClips(function(cs){return DzTracks.trouRipple(cs,gs.tr,gs.a,gs.b)});setGapSel(null);fireNote("Trou de "+svmShort(gs.b-gs.a)+" refermé sur "+gs.tr.toUpperCase());return}` (ref `gapSelRef` posé à chaque rendu, motif `dzScrimRef` ; `svmShort` mesuré) ; Échap : `R_K7` → `if(gapSelRef.current){setGapSel(null);return}` avant le voile ; `clipDown` → `setGapSel(null)` en tête (ancre libre à mesurer, sinon repli). CSS : `.dzsvm .svm-gapsel{position:absolute;top:2px;bottom:2px;border:1px dashed var(--accent);background:color-mix(in srgb,var(--accent) 10%,transparent);pointer-events:none;border-radius:4px}`, `.dzsvm .svm-insphead{font-size:11px;opacity:.75;margin-bottom:6px;font-variant-numeric:tabular-nums}`.
  - Pins : `svm-insphead` ×1/×1, `DzTracks.teteTxt(` ×1, `DzTracks.trou(` ×1, `DzTracks.trouRipple(` ×1 (sonde +3), `svm-gapsel` ×1/×1, `gapSelRef` ≥ 3, `e.target!==e.currentTarget` ×1, `.bak` 0.
- [ ] Chaîne, bancs, commit `montage : E-13 tete dans l inspecteur et E-14 selection du trou avec Suppr ripple`, push. Preuve : inspecteur « tête à 00:00:08:00 · +00:00:02:00 dans le plan » ; clic dans le vide de V1 entre deux clips → `.svm-gapsel` aux bornes ; Suppr → le clip droit avance de la largeur du trou, A1 intact (écart), Ctrl+Z ; Échap efface.

---

## Tâche 6 — E-12 : audit des boutons et infobulles

**Files :** `backend/tests/test_montage_ergonomie.py` (NEUF), couche + patcher (10 `title` manquants, `disabled` sur `svm-goldbtn` failed et `svm-libbtn` démo), bancs.

- [ ] Banc (scan des SOURCES : bundle patché entre `function DzMontage(` et la fin de la couche, lu en octets) : règle 1 « tout `r.jsx("button",{className:"svm-tbtn|dzm-tbb|svm-secbtn|svm-minibtn|svm-goldbtn…` porte `title:` dans les 300 caractères qui suivent » → liste des manquants = [] avec témoin (≥ 30 boutons scannés) ; règle 2 « aucun `?r.jsx("button",{className:"svm-tbtn` / `svm-secbtn` / `svm-goldbtn` conditionnel (`?` ou `&&` juste avant) » → 0 avec témoin, ET la liste DATÉE des contrôles contextuels tolérés (chips, M/S sans bus, minibtn overlay, kbreset, projets, medplus, composants null) pinnée telle quelle (si elle bouge, le banc rougit : on date à nouveau). Corriger : `title` sur les 10 `svm-secbtn` ; `svm-goldbtn` (`B:4650` `proj.demo?null:failed?`) → rendu toujours, `disabled:proj.demo`, `title` ; `svm-libbtn` (`B:5600`) → `disabled:proj.demo` + `title` (mesurer que le handler tolère le disabled).
- [ ] Commit `montage : E-12 - audit des boutons et infobulles bancable`, push. Preuve : sur la démo, « Rendre → » grisé avec infobulle au lieu d'absent.

---

## Tâche 7 — clôture E-C

- [ ] `backend/tests/mutations_montage_ec.py` (modèle `mutations_montage_eb.py`, `N_ROUGES` comparé, rc) ≥ 12 : `dzmComboToKey` sans `Maj→shift` ; `dzmMenuModel` sans ordre fixe ; rubrique par défaut fausse ; `DzmCtxMenu` sans `disabled:off` ; `dzFire` sans `bubbles` (si ça rougit — sinon écarter et dire) ; voile sans `dzMenu` ; clic droit clip sans `preventDefault` ; `dzmJobsTri` qui garde les aperçus dans `finals` ; `data-view` retiré ; `[data-docked]` sans `position:static` ; `dzmTeteTxt` sans « hors du plan » ; `dzmTrou` qui accepte la queue de piste ; `dzmTrouRipple` qui décale les autres pistes ; Suppr sans `pushHistory` ; règle 1 de l'audit : un `title` retiré → le banc ergonomie rougit.
- [ ] Banc croisé `test_montage_ec_croise.py` : chaque id de `DZM_MENU_RUB` existe dans `SVM_ACTIONS` ∪ actions `R_R1` (lues dans le bundle), et chaque combo de la table est parsable par `dzmComboToKey` (non null) ; clés localStorage `dz_svm_tb_dock` ×2.
- [ ] Comptes, `--list`, node --check, hash, sonde ; conceptions datées « exécuté <date> » + écarts (E-6 : dispatch par événement synthétique, six rubriques par table, Guide omis si non trouvé, suppression de piste sans armement dans le menu ; E-7 : vue de session non persistée, presets = D-35, historique par titre (pas de project_id), 24 derniers ; E-10 : CSS seul ; E-12 : contrôles contextuels tolérés listés ; E-13 : `svmTcFF` 30 i/s, pas de « ·ii » ; E-14 : ripple sur la piste seule, jumeau A1 ne suit pas, queue de piste n'est pas un trou, 0,05 s min) ; mémoire tenue par le contrôleur ; commit, push.

## Relecture du plan
- Couverture : E-6 (T1–T2), E-7 (T3), E-10 (T4), E-13/E-14 (T5), E-12 (T6), clôture (T7). Huit démentis tranchés en tête.
- Noms : `dzmComboToKey/comboToKey`, `dzmMenuModel/menuModel`, `DzmCtxMenu/CtxMenu`, `dzmJobsTri/jobsTri`, `DzmDeliver/Deliver`, `dzmTeteTxt/teteTxt`, `dzmTrou/trou`, `dzmTrouRipple/trouRipple` ; hôte : `dzMenu/setDzMenu`, `dzFire`, `dzMenuProps`, `dzReplaceArm`, `view/dzSetView`, `dzJobs`, `dzTbDock`, `gapSel/gapSelRef` ; clés `dz_svm_tb_dock`.
