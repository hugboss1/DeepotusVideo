# News — RSS → script → reel : plan d'implémentation

> **Pour les agents :** SOUS-COMPÉTENCE REQUISE : utiliser
> `superpowers:subagent-driven-development` (recommandé) ou
> `superpowers:executing-plans` pour exécuter ce plan tâche par tâche. Les
> étapes sont des cases à cocher (`- [ ]`).

**But :** faire du flux News un pipeline qui trie tout seul (filtre gratuit
puis score), se souvient de ce qu'il a déjà dit, cite ses sources dans la
légende et choisit son mode de voix — puis, au lot 2, va de l'article au post
programmé sans intervention, en quatre formes de reel, avec les tendances
croisées entre flux.

**Architecture :** cinq modules backend NEUFS et sans état partagé
(`news_filter`, `news_rank`, `news_memory`, `news_caption`, `news_voice`),
chacun une responsabilité, tous appelés depuis `news_service` et les routes
`/news/*` existantes ; le lot 2 ajoute `news_forms`, `news_chain`,
`news_trends`. L'écran News vit dans le bundle minifié : il se patche par deux
patchers chaînés (`newsrank` au lot 1, `newschain` au lot 2), jamais
autrement. Aucune dépendance nouvelle : `difflib`, `unicodedata`, `json`,
`datetime` sont dans la stdlib du python EMBARQUÉ.

**Pile :** Python 3.13 embarqué (stdlib + Pillow), `feedparser`, `httpx`,
`trafilatura` (déjà dans `backend/requirements.txt`, section « News / RSS
pipeline (v1.7) »), FastAPI, SQLAlchemy/aiosqlite, ffmpeg, bundle React
minifié `frontend/dist/assets/index-BEOJX8L5.js`.

---

## Périmètre

Ce plan exécute EXACTEMENT les bacs de `### R8. News — réponses (03/09/2026)`
de `docs/superpowers/plans/2026-09-02-balayage-meilleur-de-sa-classe.md`.

**Lot 1 — parité, dans cet ordre :**

| Bac | Titre | Tâches |
| --- | --- | --- |
| P1 | Filtre gratuit puis score | 1, 2, 3, 4, 5, 6 |
| P2 | Historique et équilibre des sources | 7, 8 |
| P3 | Sources en légende | 9 |
| P4 | Modes de voix par sujet | 10 |

**Lot 2 — différenciant :**

| Bac | Titre | Tâches |
| --- | --- | --- |
| D2 | Quatre formes de reel | 11 |
| D1 | Chaîne article → post sans intervention | 12, 13 |
| D3 | Tendances locales | 14 |
| — | Le patch du bundle du lot 2 | 15 |

**Tâche 16 :** la campagne de mutations `backend/tests/mutations_news.py`.

**Ce que ce plan NE fait PAS, parce qu'un autre plan le porte :** le brief de
campagne persistant et la validation par lot (**R6 D2** et **R6 P5**) — la
tâche 12 les LIT et s'y branche, elle ne les réécrit pas ; les sous-titres
animés mot par mot (**R5 P2**) — la forme 4 de la tâche 11 les APPELLE ; le
registre des modèles vidéo et son estimé de coût (**R1**, `pricing.estimate`)
— la tâche 11 le réutilise. Si l'un de ces trois n'est pas livré au moment
d'exécuter, la tâche concernée dit pourquoi elle refuse et s'arrête : elle ne
réimplémente rien.

**Ce que le code fait aujourd'hui, relu le 03/09 — les faits qui fondent le
plan :**

- `backend/app/services/news_service.py:396-405` — le dédoublonnage est **par
  identifiant seulement** (`_hid(source_id, link|title)`), puis tri
  décroissant sur `published`, puis coupe à `MAX_ITEMS = 300`
  (`news_service.py:28`). Deux médias qui reprennent la même dépêche donnent
  deux entrées.
- `frontend/dist/assets/index-BEOJX8L5.js` — l'écran News est le composant
  minifié `pm({variant,go})`. Son tri « By relevance » est
  `shown.slice().sort((a,b)=>(b.summary||"").length-(a.summary||"").length)` :
  **la longueur du résumé**, pas une pertinence. Son sélecteur « Illustration
  / Style » (`sty`, quatre valeurs) n'est envoyé nulle part : `buildIllu`
  poste `{items:payload(),per_card_s:3.5,engine:"ffmpeg"}`. Son bouton « Send
  to Studio » appelle `Go("studio")` sans emporter la sélection. `payload()`
  n'emporte ni `id` ni `published`.
- `backend/app/personas/deepotus.json` — les clés sont exactement
  `persona_id, display_name, vibe_keywords, brand_colors,
  default_negative_prompt, default_hashtags_pool, templates`. Il **n'y a pas**
  de bloc `voice_modes`, donc `prompt_engine._voice_mode_block`
  (`prompt_engine.py:101-112`) rend toujours `None` et les quatre modes de
  `VoiceMode` (`schemas.py:71-81`) ne pilotent que les crochets et signatures
  codés en dur de `prompt_engine.py:643-681`.
- `backend/app/services/prompt_engine.py:689` — sans `voice_mode` explicite le
  ton est `"prophet"` ; l'UI, elle, envoie toujours `voice:"oracle"` (état
  initial `x.useState("oracle")`). Le mode « choisi selon le sujet » n'existe
  pas.
- `backend/app/services/summarizer.py:94-116` — `summarize()` rend `None` sur
  absence de clé, texte sous 80 caractères, ou échec de tous les fournisseurs ;
  `_chat_dispatch()` (`summarizer.py:156-175`) rend `(None, "")` dans les mêmes
  cas. Tout ce plan traite ce `None` comme un cas NORMAL, jamais comme une
  erreur.
- `backend/app/services/marketing.py:255-296` — `materialize_plan()` sait déjà
  créer des `ScheduledPost` (`storage.py:82-121`) à partir de blocs de plan.
  La tâche 13 s'y branche au lieu d'ouvrir une seconde porte vers le
  Scheduler.
- `backend/app/services/news_illustration.py:130-157` — le reel « news » est
  une suite de cartes ffmpeg muettes (titre, index · source, ticker) ; c'est
  la forme 1 d'aujourd'hui, la tâche 11 en ajoute trois autres.

---

## Coût de patch

L'interface compilée se patche ; c'est le poste de coût de cette catégorie.
Règles non négociables (piège hérité « L'interface compilée se patche ») :

1. **Tag NEUF** par patcher, jamais réutilisé : `newsrank` (lot 1),
   `newschain` (lot 2).
2. **Backup dédié** `index-BEOJX8L5.js.bak_<tag>`, créé par `shutil.copy2`.
3. **EN QUEUE de chaîne** : `guard_downstream()` refuse de tourner si un
   `.bak_*` porte un mtime postérieur. La chaîne du dépôt au 03/09, dans
   l'ordre des mtimes : `dzrailmotion`, `version`, `dznodecat`, `seedance25`.
   `newsrank` se pose APRÈS `seedance25`, `newschain` APRÈS `newsrank`.
4. **Ancre unique** : `apply()` compte l'ancre et abandonne si le compte n'est
   pas 1. Les neuf ancres retenues par ce plan ont été comptées le 03/09,
   toutes à 1.
5. **Rejouer la chaîne** : `python scripts/repatch_all.py --from newsrank`
   après toute modification amont — jamais un patcher amont seul.
6. `newline=""` partout (le bundle est en CRLF homogène) ; vérification des
   fins de ligne AVANT et APRÈS ; parité de delta (caractères ET octets) ;
   restauration du backup si la vérification échoue.

**Coût, tâche par tâche :**

| Tâche | Bac | Bundle ? | Coût |
| --- | --- | --- | --- |
| 1 | P1 | non | backend pur — `news_filter.py` neuf |
| 2 | P1 | non | backend pur — `news_filter.py` (ajout) |
| 3 | P1 | non | `news_service.py:396-405`, un appel et un compteur |
| 4 | P1 | non | backend pur — `news_rank.py` neuf |
| 5 | P1 | non | 3 routes + 5 schémas |
| 6 | P1 | **OUI** | patcher `newsrank`, 5 sections |
| 7 | P2 | non | backend pur — `news_memory.py` neuf, JSON sous `news/` |
| 8 | P2 | non | `news_rank.py` (une pénalité) + la route de la tâche 5 |
| 9 | P3 | non | `news_caption.py` neuf + 6 lignes dans `routes.py` |
| 10 | P4 | non | `news_voice.py` neuf + bloc `voice_modes` dans la persona |
| 11 | D2 | non | `news_forms.py` neuf, réutilise `pricing.estimate` |
| 12 | D1 | non | `news_chain.py` neuf |
| 13 | D1 | non | `news_chain.py` + `marketing.materialize_plan` (appel) |
| 14 | D3 | non | `news_trends.py` neuf |
| 15 | D1·D2·D3 | **OUI** | patcher `newschain`, 4 sections |
| 16 | — | non | banc de mutations, aucun code de production |

Deux patches de bundle pour toute la catégorie, un par lot : c'est le minimum
atteignable. Un patch par bac ajouterait sept maillons à une chaîne qui en
compte déjà quatre, et chaque maillon supplémentaire est un `repatch_all` de
plus à rejouer au prochain changement amont.

---

## Références vérifiées

Seules les références **vérifiées et datées** de `### R8` figurent ici. Une
référence de mémoire n'est pas un argument (piège hérité « Le souvenir n'est
pas une mesure »).

- **Feedly** (docs.feedly.com, vérifié le 03/09/2026) : dédoublonnage quand le
  contenu se recouvre à **plus de 85 %** ; filtres de mute par mots-clés,
  entreprises, personnes, sujets, auteurs, sites ; priorisation par sujets.
  → fonde `SEUIL_CONTENU = 0.85` (tâche 2) et la forme des listes noires
  (tâche 1).
- **Inoreader** (inoreader.com, vérifié le 03/09/2026) : filtres de doublons
  par URL, par titre exact, par **titre presque identique** ; fenêtre de 6 h à
  1 mois ; règles (déclencheur, conditions, action) au palier Pro.
  → fonde le dédoublonnage par titre presque identique (tâche 2) et la borne
  haute de la fenêtre de fraîcheur (`FRAICHEUR_MAX_H = 24 * 30`, tâche 1).
- **Google Trends API** (developers.google.com, support.google.com, vérifié le
  03/09/2026) : **alpha** depuis juillet 2025, accès sur candidature, sans
  réponse garantie. → **écartée** (E2) ; la tendance de la tâche 14 est locale
  et gratuite.
- **X API** (docs.x.com, vérifié le 03/09/2026, relevé de R6) : palier gratuit
  **100 lectures par mois** par projet. → le signal X de la tâche 14 est borné
  à **3 lectures par jour** (3 × 31 = 93 < 100), coupé net au budget, et
  l'écran affiche le reste du quota.
- Opus Clip, Pictory, InVideo, Perplexity, Exploding Topics : **de mémoire,
  non vérifiés** — jamais utilisés comme argument dans ce plan.

**Mesures faites pour ce plan le 03/09/2026** (python embarqué, `difflib` +
`unicodedata`, tokeniseur unique de la tâche 1, 21 paires de titres et 4
paires de corps) :

| Famille | ratio (jetons triés) | Jaccard (jetons) |
| --- | --- | --- |
| Doublons réels (11 paires) | **min 0,753** — max 1,000 | **min 0,417** — max 1,000 |
| Suite d'un même sujet, pas doublon (6 paires) | max **0,711** | max **0,333** |
| Sans rapport (4 paires) | max 0,356 | 0,083 |

La paire la plus dure — « Ethereum Dencun upgrade goes live on mainnet » vs
« Ethereum gas fees fall after Dencun upgrade » — donne 0,711 / 0,333 : elle
est sous les **deux** seuils. D'où `SEUIL_TITRE = 0.735` et
`SEUIL_JETONS = 0.375`, les milieux des deux intervalles vides
]0,711 ; 0,753[ et ]0,333 ; 0,417[.

**Un tronçonnage léger a été ajouté au tokeniseur après une première mesure
ratée.** Sans lui, « Oil prices climb on supply fears » contre « Oil price
climbs on supply fear » donnait ratio 0,947 mais Jaccard 0,250 — un doublon
évident MANQUÉ, parce que `prices/price`, `climb/climbs` et `fears/fear` sont
trois jetons distincts pour un ensemble alors qu'ils se ressemblent au
caractère près. Retirer les suffixes `ing`, `ed`, `es`, `s` (au-delà de deux
lettres de racine) remonte cette paire à 0,981 / 0,667 et « Argentina peso
plunges after vote » contre « Argentine peso plunged after the vote » à
0,960 / 0,600, **sans** rapprocher aucune des paires non-doublons : le vide
s'élargit au lieu de se réduire. C'est pourquoi les deux paires sont dans le
banc de la tâche 2 : elles gardent le tronçonnage honnête.

Sur les corps : reprise de dépêche **0,968**, article indépendant sur le même
événement **0,031**. Le seuil Feedly de 0,85 tombe dans un vide de 0,94 de
large.

**Ce que la conjonction fait vraiment.** Sur les 21 paires réelles, chaque
signal pris seul classe déjà tout correctement : le `and` n'y est PAS
discriminant. Il est une marge, pas un juge — et une marge non exercée est
une marge qu'un mutant traverse sans faire rougir personne. Le banc de la
tâche 2 porte donc deux paires SYNTHÉTIQUES construites exprès, l'une dans
chaque zone à une seule condition (ratio 0,839 / Jaccard 0,000 ; ratio
0,475 / Jaccard 0,600), et la campagne de mutations de la tâche 16 remplace
le `and` par un `or` pour vérifier qu'elles rougissent.

**Convention de banc, valable pour toutes les tâches :** un banc est un script
AUTONOME, lancé **un processus par fichier**, depuis `backend/`, par
`python tests/test_<x>.py` — jamais `pytest tests` (les bancs figent
`app.config` à l'import, le premier fuiterait dans tous les suivants ;
`scripts/run-tests.ps1` explique ce choix en tête de fichier). Chaque banc
force UTF-8 (`sys.stdout.reconfigure(encoding="utf-8", errors="replace")`),
finit par `raise SystemExit(pytest.main([__file__, "-q"]))`, et lit **ce qui a
été écrit** — le JSON du disque, le bundle, le fichier rendu — jamais le code
qui prétend le produire.

---

## Lot 1 — parité

### Tâche 1 : le filtre gratuit — mots-clés, listes noires, fraîcheur

**Fichiers :**
- Créer : `backend/app/services/news_filter.py`
- Test : `backend/tests/test_news_filter.py`

**Coût de patch :** nul, backend pur.

- [ ] **Étape 1 : écrire le banc qui échoue**

Créer `backend/tests/test_news_filter.py` :

```python
# -*- coding: utf-8 -*-
"""Le filtre gratuit du flux News (plan 2026-09-03, lot 1, P1).

Banc-miroir : il LIT le fichier de reglages ecrit sur le disque et la liste
rendue, jamais le code qui pretend les produire. Aucun reseau, aucun LLM.

Run (depuis backend/) : python tests/test_news_filter.py
"""
import json
import os
import pathlib
import sys
import tempfile
from datetime import datetime, timedelta, timezone

import pytest

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

_tmp = tempfile.mkdtemp()
os.environ["DATABASE_URL"] = \
    f"sqlite+aiosqlite:///{pathlib.Path(_tmp, 't.db').as_posix()}"
os.environ.setdefault("FAL_KEY", "test-key")
os.environ["IMAGES_FOLDER"] = str(pathlib.Path(_tmp, "images"))
os.environ["OUTPUTS_FOLDER"] = str(pathlib.Path(_tmp, "outputs"))
pathlib.Path(_tmp, "images").mkdir(exist_ok=True)
pathlib.Path(_tmp, "outputs").mkdir(exist_ok=True)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

MAINTENANT = "2026-09-03T12:00:00+00:00"


def _item(titre, source="CoinDesk", heures=1, resume="", sid="s1"):
    t = datetime(2026, 9, 3, 12, 0, tzinfo=timezone.utc) - timedelta(hours=heures)
    return {"id": titre[:8], "title": titre, "source_name": source,
            "source_id": sid, "summary": resume,
            "link": "https://x/" + titre[:4],
            "published": t.isoformat(timespec="seconds")}


def test_les_mots_cles_gardent_et_le_reste_tombe():
    from app.services import news_filter as F
    items = [_item("Bitcoin ETF outflows accelerate"),
             _item("Storm floods southern Spain")]
    reglages = dict(F.REGLAGES_DEFAUT, mots_cles=["bitcoin", "solana"])
    gardes, motifs = F.filtrer(items, reglages, maintenant=MAINTENANT)
    assert [i["title"] for i in gardes] == ["Bitcoin ETF outflows accelerate"]
    assert motifs["Storm fl"] == "hors mots-cles"


def test_une_source_noire_tombe_meme_si_elle_a_le_mot_cle():
    from app.services import news_filter as F
    items = [_item("Bitcoin ETF outflows accelerate", source="Spammy Feed")]
    reglages = dict(F.REGLAGES_DEFAUT, mots_cles=["bitcoin"],
                    sources_noires=["spammy feed"])
    gardes, motifs = F.filtrer(items, reglages, maintenant=MAINTENANT)
    assert gardes == []
    assert motifs[items[0]["id"]] == "source sur liste noire"


def test_un_mot_noir_dans_le_titre_ou_le_resume_tombe():
    from app.services import news_filter as F
    items = [_item("Bitcoin giveaway airdrop now"),
             _item("Bitcoin ETF flows", resume="a free giveaway inside")]
    reglages = dict(F.REGLAGES_DEFAUT, mots_noirs=["giveaway"])
    gardes, motifs = F.filtrer(items, reglages, maintenant=MAINTENANT)
    assert gardes == []
    assert set(motifs.values()) == {"mot sur liste noire"}


def test_la_fenetre_de_fraicheur_coupe_le_vieux():
    from app.services import news_filter as F
    items = [_item("Frais assez recent", heures=5),
             _item("Vieux de quatre jours", heures=100)]
    reglages = dict(F.REGLAGES_DEFAUT, fraicheur_h=48)
    gardes, motifs = F.filtrer(items, reglages, maintenant=MAINTENANT)
    assert [i["title"] for i in gardes] == ["Frais assez recent"]
    assert motifs["Vieux de"] == "hors fenetre de fraicheur"


def test_une_date_illisible_ne_fait_pas_tomber_l_article():
    from app.services import news_filter as F
    it = _item("Sans date lisible")
    it["published"] = "pas une date"
    gardes, _ = F.filtrer([it], dict(F.REGLAGES_DEFAUT), maintenant=MAINTENANT)
    assert len(gardes) == 1


def test_les_reglages_sont_relus_du_disque_tels_qu_ecrits():
    from app.services import news_filter as F
    F.ecrire_reglages({"mots_cles": ["Solana", "  BCE  "], "fraicheur_h": 12,
                       "sources_noires": [], "mots_noirs": ["giveaway"]})
    sur_disque = json.loads(F.chemin_reglages().read_text(encoding="utf-8"))
    assert sur_disque["mots_cles"] == ["solana", "bce"]
    assert sur_disque["fraicheur_h"] == 12
    relu = F.lire_reglages()
    assert relu["mots_cles"] == ["solana", "bce"]
    assert relu["mots_noirs"] == ["giveaway"]


def test_un_fichier_de_reglages_corrompu_rend_les_defauts():
    from app.services import news_filter as F
    F.chemin_reglages().write_text("{ pas du json", encoding="utf-8")
    assert F.lire_reglages() == F.REGLAGES_DEFAUT


def test_une_fraicheur_absurde_est_bornee_a_l_ecriture():
    from app.services import news_filter as F
    F.ecrire_reglages(dict(F.REGLAGES_DEFAUT, fraicheur_h=100000))
    assert F.lire_reglages()["fraicheur_h"] == F.FRAICHEUR_MAX_H


if __name__ == "__main__":
    raise SystemExit(pytest.main([__file__, "-q"]))
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_filter.py
```

Attendu : `8 errors`, chacune
`ModuleNotFoundError: No module named 'app.services.news_filter'`.

- [ ] **Étape 3 : écrire le module**

Créer `backend/app/services/news_filter.py` :

```python
# -*- coding: utf-8 -*-
"""Le filtre gratuit du flux News (plan 2026-09-03, lot 1, P1).

Rien ne part vers un fournisseur avant que ce module ait parle : mots-cles,
listes noires (sources, mots), fenetre de fraicheur — puis, tache 2, le
dedoublonnage par titre presque identique et par recouvrement de contenu.

Tout est stdlib (`difflib`, `re`, `unicodedata`, `json`) : le python EMBARQUE
n'a ni numpy ni scikit-learn, et un tri qui coute un appel LLM par article
n'est pas un tri « gratuit ».

Les reglages vivent a cote des sources et du cache, sous le dossier `news/`
des donnees utilisateur : ils survivent aux mises a jour.
"""
import difflib
import json
import re
import unicodedata
from datetime import datetime, timezone
from pathlib import Path

from loguru import logger

from app.config import settings

FRAICHEUR_MAX_H = 24 * 30      # un mois : la borne haute d'Inoreader
FRAICHEUR_MIN_H = 1

REGLAGES_DEFAUT: dict = {
    "mots_cles": [],           # vide = tout passe l'etape mots-cles
    "sources_noires": [],
    "mots_noirs": [],
    "fraicheur_h": 48,
}

# Mots vides FR + EN. Volontairement courte : elle sert a comparer des TITRES,
# pas a faire de la linguistique.
MOTS_VIDES = {
    "the", "a", "an", "of", "to", "in", "on", "for", "and", "or", "as", "at",
    "by", "with", "from", "after", "over", "is", "are", "was", "were", "be",
    "been", "has", "have", "had", "that", "this", "it", "its", "his", "her",
    "they", "their", "he", "she", "said", "says", "but", "not", "also", "new",
    "up", "out", "into", "than", "then", "who", "will",
    "le", "la", "les", "de", "des", "du", "un", "une", "et", "en", "pour",
    "sur", "dans", "que", "qui", "au", "aux", "ses", "son", "sa", "est",
    "sont", "ont", "plus", "ne", "pas",
}


_SUFFIXES = ("ing", "ed", "es", "s")


def _tronc(mot: str) -> str:
    """Tronconnage LEGER : `prices` -> `price`, `climbs` -> `climb`.

    Mesure du 03/09 : sans lui, « Oil prices climb on supply fears » contre
    « Oil price climbs on supply fear » donnait un Jaccard de 0,250 — un
    doublon evident manque, parce que trois jetons singulier/pluriel sont
    trois jetons distincts pour un ensemble. Avec lui la paire monte a 0,667,
    et AUCUNE paire non-doublon ne se rapproche.

    Une racine de deux lettres est laissee intacte : `es` -> `es`, pas `""`.
    """
    for suf in _SUFFIXES:
        if len(mot) > len(suf) + 2 and mot.endswith(suf):
            return mot[:-len(suf)]
    return mot


def jetons(texte: str) -> list[str]:
    """Jetons normalises : sans accents, en minuscules, sans mots vides,
    tronconnes.

    UN SEUL tokeniseur pour tout le module — titres et corps —, sinon les
    seuils mesures dans le plan ne veulent plus rien dire. Les mots vides sont
    filtres AVANT le tronconnage : la liste est ecrite en formes pleines.
    """
    t = unicodedata.normalize("NFKD", texte or "")
    t = "".join(c for c in t if not unicodedata.combining(c)).lower()
    return [_tronc(w) for w in re.findall(r"[a-z0-9]+", t)
            if w not in MOTS_VIDES and len(w) > 1]


def chemin_reglages() -> Path:
    d = settings.outputs_path.parent / "news"
    d.mkdir(parents=True, exist_ok=True)
    return d / "filtre.json"


def _normaliser_liste(v) -> list[str]:
    if not isinstance(v, list):
        return []
    out: list[str] = []
    for x in v:
        s = " ".join(str(x or "").split()).strip().lower()
        if s and s not in out:
            out.append(s)
    return out


def _propre(brut: dict) -> dict:
    h = brut.get("fraicheur_h", REGLAGES_DEFAUT["fraicheur_h"])
    try:
        h = int(h)
    except (TypeError, ValueError):
        h = REGLAGES_DEFAUT["fraicheur_h"]
    h = max(FRAICHEUR_MIN_H, min(FRAICHEUR_MAX_H, h))
    return {
        "mots_cles": _normaliser_liste(brut.get("mots_cles")),
        "sources_noires": _normaliser_liste(brut.get("sources_noires")),
        "mots_noirs": _normaliser_liste(brut.get("mots_noirs")),
        "fraicheur_h": h,
    }


def lire_reglages() -> dict:
    p = chemin_reglages()
    if not p.is_file():
        return dict(REGLAGES_DEFAUT)
    try:
        brut = json.loads(p.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError):
        logger.warning("news filtre.json illisible - retour aux defauts")
        return dict(REGLAGES_DEFAUT)
    if not isinstance(brut, dict):
        return dict(REGLAGES_DEFAUT)
    return _propre(brut)


def ecrire_reglages(brut: dict) -> dict:
    """Ecrit les reglages NORMALISES (minuscules, sans doublons, fraicheur
    bornee) et rend ce qui a ete ecrit. L'ecriture passe par un fichier
    temporaire : une coupure ne laisse pas un JSON tronque."""
    propre = _propre(brut if isinstance(brut, dict) else {})
    p = chemin_reglages()
    tmp = p.with_name(p.name + ".tmp")
    tmp.write_text(json.dumps(propre, indent=2, ensure_ascii=False),
                   encoding="utf-8")
    tmp.replace(p)
    return propre


def _quand(item: dict) -> datetime | None:
    try:
        d = datetime.fromisoformat(str(item.get("published") or ""))
    except ValueError:
        return None
    return d if d.tzinfo else d.replace(tzinfo=timezone.utc)


def _texte(item: dict) -> str:
    return " ".join([str(item.get("title") or ""),
                     str(item.get("summary") or ""),
                     str(item.get("essence") or "")])


def motif_de_rejet(item: dict, reglages: dict,
                   maintenant: datetime) -> str | None:
    """Le PREMIER motif qui fait tomber l'article, ou None s'il passe.

    L'ordre est celui du cout : source (une comparaison), mots noirs (un
    parcours), mots-cles (un parcours), fraicheur (une date). Le motif est
    rendu tel quel a l'ecran : un article ecarte doit dire pourquoi.
    """
    src = " ".join(str(item.get("source_name") or "").split()).lower()
    if any(n in src for n in reglages["sources_noires"]):
        return "source sur liste noire"
    corpus = _texte(item).lower()
    if any(n in corpus for n in reglages["mots_noirs"]):
        return "mot sur liste noire"
    cles = reglages["mots_cles"]
    if cles and not any(c in corpus for c in cles):
        return "hors mots-cles"
    d = _quand(item)
    if d is not None:
        age_h = (maintenant - d).total_seconds() / 3600.0
        if age_h > reglages["fraicheur_h"]:
            return "hors fenetre de fraicheur"
    return None


def filtrer(items: list[dict], reglages: dict | None = None, *,
            maintenant: str | None = None) -> tuple[list[dict], dict]:
    """Rend (gardes, {id: motif de rejet}). Ne mute jamais les articles."""
    reglages = _propre(reglages) if reglages else lire_reglages()
    if maintenant:
        ref = datetime.fromisoformat(maintenant)
        if not ref.tzinfo:
            ref = ref.replace(tzinfo=timezone.utc)
    else:
        ref = datetime.now(timezone.utc)
    gardes: list[dict] = []
    motifs: dict = {}
    for it in items:
        m = motif_de_rejet(it, reglages, ref)
        if m:
            motifs[it.get("id")] = m
        else:
            gardes.append(it)
    return gardes, motifs
```

- [ ] **Étape 4 : lancer le banc pour le voir vert**

Depuis `backend/` :

```
python tests/test_news_filter.py
```

Attendu : `8 passed`.

- [ ] **Étape 5 : commit**

```
git add backend/app/services/news_filter.py backend/tests/test_news_filter.py
git commit -m "news : le filtre gratuit avant tout appel paye" -m "Mots-cles, listes noires de sources et de mots, fenetre de fraicheur bornee a un mois (parite Inoreader), reglages persistes sous news/filtre.json et normalises a l ecriture. Un article ecarte porte son motif, affichable tel quel." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 2 : le dédoublonnage par titre presque identique et par contenu

**Fichiers :**
- Modifier : `backend/app/services/news_filter.py` (ajout en fin de fichier)
- Test : `backend/tests/test_news_filter.py` (ajout avant le bloc `__main__`)

**Coût de patch :** nul, backend pur.

- [ ] **Étape 1 : écrire le banc qui échoue**

Insérer dans `backend/tests/test_news_filter.py`, JUSTE AVANT le bloc
`if __name__ == "__main__":` :

```python
# ── Tache 2 : le doublon, avec les couples qui ont FIXE les seuils ──────────

DOUBLONS = [
    ("Bitcoin tumbles below $60,000 as ETF outflows accelerate",
     "Bitcoin falls under $60,000 amid accelerating ETF outflows"),
    ("EU agrees landmark AI Act enforcement delay",
     "EU agrees to delay enforcement of landmark AI Act"),
    ("Fed holds rates steady, signals two cuts in 2026",
     "Federal Reserve holds rates steady and signals two cuts this year"),
    ("China unveils new stimulus package for property sector",
     "Beijing unveils fresh stimulus for the property sector"),
    ("Solana network hits record daily transactions",
     "Solana network hits record daily transactions"),
    ("Trump signs executive order on crypto reserve",
     "Trump signs an executive order creating a crypto reserve"),
    ("Ethereum Dencun upgrade goes live on mainnet",
     "Ethereum's Dencun upgrade is live on mainnet"),
    ("Nvidia beats earnings expectations, shares jump 8%",
     "Nvidia shares jump 8% after beating earnings expectations"),
    ("La BCE laisse ses taux inchanges et evoque deux baisses",
     "La BCE maintient ses taux inchanges, deux baisses evoquees"),
    # Les deux paires qui ont IMPOSE le tronconnage : sans lui, Jaccard 0,250
    # et 0,333, donc deux doublons evidents manques.
    ("Oil prices climb on supply fears",
     "Oil price climbs on supply fear"),
    ("Argentina peso plunges after vote",
     "Argentine peso plunged after the vote"),
]

PAS_DOUBLONS = [
    ("Bitcoin tumbles below $60,000 as ETF outflows accelerate",
     "Bitcoin ETF inflows hit record high in January"),
    ("EU agrees landmark AI Act enforcement delay",
     "EU parliament debates AI Act amendments next week"),
    ("Trump signs executive order on crypto reserve",
     "Crypto reserve order draws criticism from senators"),
    ("Nvidia beats earnings expectations, shares jump 8%",
     "Nvidia supplier warns of chip shortage in 2026"),
    ("Ethereum Dencun upgrade goes live on mainnet",
     "Ethereum gas fees fall after Dencun upgrade"),
    ("La BCE laisse ses taux inchanges et evoque deux baisses",
     "Les marches europeens saluent la decision de la BCE"),
    ("Bitcoin tumbles below $60,000 as ETF outflows accelerate",
     "EU agrees landmark AI Act enforcement delay"),
    ("Fed holds rates steady, signals two cuts in 2026",
     "China unveils new stimulus package for property sector"),
    ("Nvidia beats earnings expectations, shares jump 8%",
     "Storm Bernard floods southern Spain, thousands evacuated"),
    ("Tesla recalls 12,000 vehicles over brake fault",
     "Tesla plans a factory in Mexico next year"),
]

# Deux paires SYNTHETIQUES : la seule maniere d'exercer la CONJONCTION. Sur
# les 21 paires reelles, chaque signal pris seul classe deja tout
# correctement ; sans ces deux-la, remplacer le `and` par un `or` ne ferait
# rougir aucun test (mesure du 03/09).
UNE_SEULE_CONDITION = [
    # ratio 0,839 mais Jaccard 0,000 : les caracteres se ressemblent, aucun
    # jeton n'est commun.
    ("Bitcoin rally halts trading floor",
     "Bitcona rallx haltz tradinq floov"),
    # Jaccard 0,600 mais ratio 0,475 : les jetons communs sont noyes sous
    # deux mots tres longs.
    ("Bitcoin ETF outflow",
     "Bitcoin ETF outflow internationalization supercalifragilistic"),
]

DEPECHE = ("The European Central Bank held its benchmark rate at 2.75 percent "
           "on Thursday and signalled two cuts before the end of the year, "
           "citing slowing core inflation and weak industrial output across "
           "the euro area. President Christine Lagarde told reporters the "
           "council was unanimous and that incoming data would guide the pace.")
REPRISE = ("BRUSSELS - The European Central Bank held its benchmark rate at "
           "2.75 percent on Thursday and signalled two cuts before the end of "
           "the year, citing slowing core inflation and weak industrial "
           "output across the euro area. Lagarde told reporters the council "
           "was unanimous.")
INDEPENDANT = ("Frankfurt policymakers left borrowing costs untouched this "
               "week, a decision widely expected by economists, while opening "
               "the door to easing later in 2026. Markets rallied modestly; "
               "bank shares led the gains as traders priced in a softer path "
               "for deposit rates through the autumn.")


def test_les_seuils_separent_les_deux_familles_avec_de_la_marge():
    """Le banc MESURE au lieu de croire : il recalcule les deux extremes et
    verifie que les seuils tombent dans le vide entre eux."""
    from app.services import news_filter as F
    d_ratio = min(F.ratio_titres(a, b) for a, b in DOUBLONS)
    d_jac = min(F.jaccard_titres(a, b) for a, b in DOUBLONS)
    n_ratio = max(F.ratio_titres(a, b) for a, b in PAS_DOUBLONS)
    n_jac = max(F.jaccard_titres(a, b) for a, b in PAS_DOUBLONS)
    assert round(d_ratio, 3) == 0.753, d_ratio
    assert round(n_ratio, 3) == 0.711, n_ratio
    assert round(d_jac, 3) == 0.417, d_jac
    assert round(n_jac, 3) == 0.333, n_jac
    assert n_ratio < F.SEUIL_TITRE < d_ratio
    assert n_jac < F.SEUIL_JETONS < d_jac


def test_chaque_paire_est_classee_du_bon_cote():
    from app.services import news_filter as F
    for a, b in DOUBLONS:
        assert F.titres_presque_identiques(a, b), (a, b)
    for a, b in PAS_DOUBLONS:
        assert not F.titres_presque_identiques(a, b), (a, b)


def test_la_conjonction_est_bien_exigee():
    """Les deux paires synthetiques passent UNE condition et ratent l'autre :
    elles doivent etre refusees. Sans ce test, remplacer le `and` par un `or`
    ne ferait rougir personne."""
    from app.services import news_filter as F
    a, b = UNE_SEULE_CONDITION[0]
    assert F.ratio_titres(a, b) >= F.SEUIL_TITRE
    assert F.jaccard_titres(a, b) < F.SEUIL_JETONS
    assert not F.titres_presque_identiques(a, b)
    a, b = UNE_SEULE_CONDITION[1]
    assert F.ratio_titres(a, b) < F.SEUIL_TITRE
    assert F.jaccard_titres(a, b) >= F.SEUIL_JETONS
    assert not F.titres_presque_identiques(a, b)


def test_le_tronconnage_rapproche_le_singulier_du_pluriel():
    """La mesure qui a impose _tronc : sans lui, Jaccard 0,250 sur une paire
    que tout le monde lit comme un doublon."""
    from app.services import news_filter as F
    assert round(F.jaccard_titres("Oil prices climb on supply fears",
                                  "Oil price climbs on supply fear"), 3) \
        == 0.667
    # Le tronconnage est GROSSIER, et c'est assume : `prices` devient `pric`
    # tandis que `price` reste `price`. Les deux ne se rejoignent donc PAS —
    # ce sont `climbs`/`climb` et `fears`/`fear` qui font remonter la paire de
    # 0,250 a 0,667. Un vrai lemmatiseur est hors stdlib ; ce banc fige le
    # comportement reel plutot qu'un comportement souhaite.
    assert F._tronc("prices") == "pric"
    assert F._tronc("price") == "price"
    assert F._tronc("climbing") == "climb"
    assert F._tronc("climbs") == "climb"
    assert F._tronc("es") == "es"          # racine trop courte : intacte
    assert F._tronc("gas") == "gas"        # trois lettres : pas de coupe


def test_le_recouvrement_de_contenu_attrape_la_reprise_de_depeche():
    from app.services import news_filter as F
    assert round(F.recouvrement(DEPECHE, REPRISE), 3) == 0.968
    assert round(F.recouvrement(DEPECHE, INDEPENDANT), 3) == 0.031
    assert F.SEUIL_CONTENU == 0.85
    assert F.contenus_se_recouvrent(DEPECHE, REPRISE)
    assert not F.contenus_se_recouvrent(DEPECHE, INDEPENDANT)


def test_un_corps_trop_court_ne_declenche_jamais_le_recouvrement():
    from app.services import news_filter as F
    court = "BCE taux inchanges deux baisses evoquees"
    assert F.recouvrement(court, court) == 1.0
    assert not F.contenus_se_recouvrent(court, court)


def test_le_dedoublonnage_garde_le_plus_recent_et_dit_qui_il_a_fondu():
    from app.services import news_filter as F
    vieux = _item("Bitcoin tumbles below $60,000 as ETF outflows accelerate",
                  source="CoinDesk", heures=6, sid="s1")
    recent = _item("Bitcoin falls under $60,000 amid accelerating ETF outflows",
                   source="Decrypt", heures=1, sid="s2")
    autre = _item("Storm Bernard floods southern Spain", heures=2, sid="s3")
    gardes, fondus = F.dedoublonner([vieux, recent, autre])
    assert [i["title"] for i in gardes] == [recent["title"], autre["title"]]
    assert gardes[0]["doublons"] == [{"source_name": "CoinDesk",
                                      "title": vieux["title"],
                                      "link": vieux["link"]}]
    assert fondus[vieux["id"]] == recent["id"]


def test_le_dedoublonnage_ne_mute_pas_les_articles_d_entree():
    from app.services import news_filter as F
    a = _item("Solana network hits record daily transactions", heures=2)
    b = _item("Solana network hits record daily transactions", heures=1,
              sid="s2")
    b["id"] = "autre-id"
    F.dedoublonner([a, b])
    assert "doublons" not in a and "doublons" not in b


def test_deux_articles_de_la_meme_source_ne_sont_pas_fondus():
    """Un fil qui republie sa propre depeche n'est pas un recoupement entre
    medias : le fondre masquerait un vrai doublon d'identifiant, deja traite
    en amont par news_service."""
    from app.services import news_filter as F
    a = _item("Solana network hits record daily transactions", heures=2,
              sid="s1")
    b = _item("Solana network hits record daily transactions", heures=1,
              sid="s1")
    b["id"] = "autre-id"
    gardes, _ = F.dedoublonner([a, b])
    assert len(gardes) == 2
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_filter.py
```

Attendu : `8 passed, 9 failed` — les neuf nouveaux échouent avec
`AttributeError: module 'app.services.news_filter' has no attribute
'ratio_titres'` (sauf le test du tronçonnage, qui échoue sur
`titres_presque_identiques`).

- [ ] **Étape 3 : écrire le dédoublonnage**

Ajouter à la FIN de `backend/app/services/news_filter.py` :

```python
# ── Le doublon : titre presque identique (Inoreader) ou recouvrement de
#    contenu au-dela de 85 % (Feedly). Seuils MESURES le 03/09/2026 sur 21
#    paires de titres et 4 paires de corps (plan, « References verifiees ») :
#    le vide mesure est ]0,711 ; 0,753[ sur le ratio et ]0,333 ; 0,417[ sur
#    le Jaccard. La paire la plus dure (« Dencun goes live » contre « gas
#    fees fall after Dencun ») rate les deux.
#    Les deux conditions sont exigees ENSEMBLE. Sur les paires REELLES chaque
#    signal suffirait ; la conjonction est une marge contre une paire qui ne
#    tripperait qu'un signal, et le banc porte deux paires synthetiques pour
#    l'exercer — sans elles, remplacer ce `and` par un `or` ne ferait rougir
#    aucun test.

SEUIL_TITRE = 0.735      # SequenceMatcher sur les jetons TRIES
SEUIL_JETONS = 0.375     # Jaccard sur les jetons du titre
SEUIL_CONTENU = 0.85     # containment sur le corps (parite Feedly)
CORPS_MIN_JETONS = 25    # sous ce compte, un corps ne prouve rien


def ratio_titres(a: str, b: str) -> float:
    """Similarite de deux titres, ordre des mots neutralise.

    Les jetons sont TRIES avant comparaison : « X after Y » et « Y, X »
    doivent se ressembler, sinon une reprise de depeche remontee dans un
    autre ordre passerait au travers.
    """
    return difflib.SequenceMatcher(
        None, " ".join(sorted(jetons(a))), " ".join(sorted(jetons(b)))
    ).ratio()


def jaccard_titres(a: str, b: str) -> float:
    A, B = set(jetons(a)), set(jetons(b))
    if not A or not B:
        return 0.0
    return len(A & B) / len(A | B)


def titres_presque_identiques(a: str, b: str) -> bool:
    return (ratio_titres(a, b) >= SEUIL_TITRE
            and jaccard_titres(a, b) >= SEUIL_JETONS)


def recouvrement(a: str, b: str) -> float:
    """Containment : la part du PLUS COURT des deux corps que l'autre couvre.

    Pas un Jaccard : la reprise ajoute un chapeau et coupe une phrase, donc
    les deux corps n'ont pas la meme longueur et l'union punirait la reprise.
    """
    A, B = set(jetons(a)), set(jetons(b))
    if not A or not B:
        return 0.0
    return len(A & B) / min(len(A), len(B))


def contenus_se_recouvrent(a: str, b: str) -> bool:
    if len(set(jetons(a))) < CORPS_MIN_JETONS:
        return False
    if len(set(jetons(b))) < CORPS_MIN_JETONS:
        return False
    return recouvrement(a, b) >= SEUIL_CONTENU


def _corps(item: dict) -> str:
    return str(item.get("essence") or item.get("summary") or "")


def dedoublonner(items: list[dict]) -> tuple[list[dict], dict]:
    """Fond les articles qui racontent la meme chose.

    Rend (gardes, {id fondu: id gardant}). Le gardant est le PLUS RECENT du
    groupe ; il porte une cle `doublons` : la liste des articles fondus
    (media, titre, lien), qui alimente la ligne de sources de la legende
    (tache 9) et l'equilibre des sources (tache 8).

    Deux articles de la MEME source ne sont jamais fondus : un fil qui se
    republie releve du doublon d'identifiant, deja traite par
    `news_service._refresh_blocking`.
    """
    ordonnes = sorted(items, key=lambda x: str(x.get("published") or ""),
                      reverse=True)
    gardes: list[dict] = []
    fondus: dict = {}
    for it in ordonnes:
        cible = None
        for g in gardes:
            if g.get("source_id") and g.get("source_id") == it.get("source_id"):
                continue
            if titres_presque_identiques(g.get("title") or "",
                                         it.get("title") or ""):
                cible = g
                break
            if contenus_se_recouvrent(_corps(g), _corps(it)):
                cible = g
                break
        if cible is None:
            copie = dict(it)
            copie["doublons"] = []
            gardes.append(copie)
        else:
            cible["doublons"].append({
                "source_name": it.get("source_name") or "",
                "title": it.get("title") or "",
                "link": it.get("link") or "",
            })
            fondus[it.get("id")] = cible.get("id")
    return gardes, fondus
```

- [ ] **Étape 4 : lancer le banc pour le voir vert**

Depuis `backend/` :

```
python tests/test_news_filter.py
```

Attendu : `17 passed`.

- [ ] **Étape 5 : commit**

```
git add backend/app/services/news_filter.py backend/tests/test_news_filter.py
git commit -m "news : le doublon se voit au titre presque identique et au recouvrement" -m "difflib sur jetons tries plus Jaccard, exiges ensemble ; seuils 0,735 et 0,375 poses dans le vide MESURE entre 18 paires de titres (doublons a partir de 0,752, suites d un meme sujet jusqu a 0,718). Le corps se compare par containment, seuil 0,85 de Feedly, mesure a 0,968 sur une reprise de depeche contre 0,031 sur un article independant. Le gardant est le plus recent et porte la liste de ce qu il a fondu." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 3 : brancher le dédoublonnage sur le rafraîchissement

**Fichiers :**
- Modifier : `backend/app/services/news_service.py:396-422`
- Test : `backend/tests/test_news_service_dedup.py`

**Coût de patch :** nul, backend pur.

- [ ] **Étape 1 : écrire le banc-miroir qui échoue**

Créer `backend/tests/test_news_service_dedup.py` :

```python
# -*- coding: utf-8 -*-
"""Le rafraichissement News fond les doublons (plan 2026-09-03, tache 3).

Banc-miroir : il LIT le fichier `cache.json` ecrit sur le disque, pas la
valeur rendue par la methode. Le reseau est remplace : `_fetch_rss` est
monkeypatche, aucune requete ne sort.

Run (depuis backend/) : python tests/test_news_service_dedup.py
"""
import json
import os
import pathlib
import sys
import tempfile

import pytest

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

_tmp = tempfile.mkdtemp()
os.environ["DATABASE_URL"] = \
    f"sqlite+aiosqlite:///{pathlib.Path(_tmp, 't.db').as_posix()}"
os.environ.setdefault("FAL_KEY", "test-key")
os.environ["IMAGES_FOLDER"] = str(pathlib.Path(_tmp, "images"))
os.environ["OUTPUTS_FOLDER"] = str(pathlib.Path(_tmp, "outputs"))
pathlib.Path(_tmp, "images").mkdir(exist_ok=True)
pathlib.Path(_tmp, "outputs").mkdir(exist_ok=True)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

FLUX = {
    "s1": [{"id": "a1", "source_id": "s1", "source_name": "CoinDesk",
            "title": "Bitcoin tumbles below $60,000 as ETF outflows accelerate",
            "summary": "", "link": "https://c/1",
            "published": "2026-09-03T08:00:00+00:00", "image": None}],
    "s2": [{"id": "b1", "source_id": "s2", "source_name": "Decrypt",
            "title": "Bitcoin falls under $60,000 amid accelerating ETF outflows",
            "summary": "", "link": "https://d/1",
            "published": "2026-09-03T11:00:00+00:00", "image": None}],
    "s3": [{"id": "c1", "source_id": "s3", "source_name": "BBC World",
            "title": "Storm Bernard floods southern Spain",
            "summary": "", "link": "https://b/1",
            "published": "2026-09-03T10:00:00+00:00", "image": None}],
}


def _service(monkeypatch):
    from app.services.news_service import NewsService
    svc = NewsService()
    svc._save_sources([
        {"id": k, "type": "rss", "url": f"https://{k}/rss", "name": k,
         "enabled": True} for k in FLUX
    ])
    monkeypatch.setattr(svc, "_fetch_rss", lambda src: list(FLUX[src["id"]]))
    return svc


def test_le_cache_ecrit_ne_contient_plus_le_doublon(monkeypatch):
    svc = _service(monkeypatch)
    svc._refresh_blocking()
    cache = json.loads(svc.cache_path.read_text(encoding="utf-8"))
    titres = [i["title"] for i in cache["items"]]
    assert len(titres) == 2, titres
    assert "Bitcoin falls under $60,000 amid accelerating ETF outflows" in titres
    assert "Bitcoin tumbles below $60,000 as ETF outflows accelerate" \
        not in titres


def test_le_gardant_porte_le_media_fondu_dans_le_cache(monkeypatch):
    svc = _service(monkeypatch)
    svc._refresh_blocking()
    cache = json.loads(svc.cache_path.read_text(encoding="utf-8"))
    garde = [i for i in cache["items"] if i["source_name"] == "Decrypt"][0]
    assert garde["doublons"] == [{"source_name": "CoinDesk",
                                  "title": FLUX["s1"][0]["title"],
                                  "link": "https://c/1"}]


def test_le_rapport_de_rafraichissement_compte_les_fondus(monkeypatch):
    svc = _service(monkeypatch)
    rapport = svc._refresh_blocking()
    assert rapport["item_count"] == 2
    assert rapport["merged_count"] == 1


def test_le_plafond_de_300_s_applique_apres_le_dedoublonnage(monkeypatch):
    from app.services.news_service import MAX_ITEMS
    faux = [{"id": f"x{i}", "source_id": "s1", "source_name": "CoinDesk",
             "title": f"Depeche numero {i} sur un sujet parfaitement distinct",
             "summary": "", "link": f"https://c/{i}",
             "published": f"2026-09-0{1 + i % 3}T0{i % 10}:00:00+00:00",
             "image": None}
            for i in range(MAX_ITEMS + 40)]
    svc = _service(monkeypatch)
    monkeypatch.setattr(svc, "_fetch_rss",
                        lambda src: faux if src["id"] == "s1" else [])
    svc._refresh_blocking()
    cache = json.loads(svc.cache_path.read_text(encoding="utf-8"))
    assert len(cache["items"]) == MAX_ITEMS


if __name__ == "__main__":
    raise SystemExit(pytest.main([__file__, "-q"]))
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_service_dedup.py
```

Attendu : `4 failed` — le premier sur `assert len(titres) == 2` avec trois
entrées, le troisième sur `KeyError: 'merged_count'`.

- [ ] **Étape 3 : brancher**

Dans `backend/app/services/news_service.py`, remplacer les lignes 396-405
(du commentaire `# dedupe by id...` jusqu'à `deduped = deduped[:MAX_ITEMS]`)
par :

```python
        # dedupe by id, keep first occurrence, newest-first by published
        seen: set[str] = set()
        deduped: list[dict] = []
        for it in items:
            if it["id"] in seen:
                continue
            seen.add(it["id"])
            deduped.append(it)
        deduped.sort(key=lambda x: x.get("published") or "", reverse=True)
        # Puis le doublon SÉMANTIQUE (plan 2026-09-03, P1) : titre presque
        # identique (Inoreader) ou recouvrement de contenu > 85 % (Feedly).
        # Il vient APRÈS le doublon d'identifiant et AVANT le plafond : dans
        # l'autre ordre, les 300 places partiraient en reprises de dépêche.
        from app.services.news_filter import dedoublonner
        avant_fusion = len(deduped)
        deduped, fondus = dedoublonner(deduped)
        deduped = deduped[:MAX_ITEMS]
```

puis remplacer le bloc `cache = {...}` … `return {...}` (lignes 406-422) par :

```python
        cache = {
            "fetched_at": _now_iso(),
            "items": deduped,
            "errors": errors,
            "source_count": len(sources),
            "merged_count": len(fondus),
        }
        self.cache_path.write_text(
            json.dumps(cache, ensure_ascii=False), encoding="utf-8")
        logger.info(
            f"news refresh: {len(deduped)} items from {len(sources)} sources, "
            f"{len(fondus)} merged (of {avant_fusion}), {len(errors)} errors")
        return {
            "fetched_at": cache["fetched_at"],
            "item_count": len(deduped),
            "source_count": len(sources),
            "merged_count": len(fondus),
            "errors": errors,
        }
```

- [ ] **Étape 4 : lancer les deux bancs**

Depuis `backend/` :

```
python tests/test_news_service_dedup.py
python tests/test_news_filter.py
```

Attendu : `4 passed`, puis `17 passed`.

- [ ] **Étape 5 : commit**

```
git add backend/app/services/news_service.py backend/tests/test_news_service_dedup.py
git commit -m "news : le rafraichissement fond les reprises avant de plafonner" -m "Le doublon semantique passe apres le doublon d identifiant et AVANT la coupe a 300 : dans l autre ordre les places du cache partent en reprises de depeche. Le cache et le rapport disent combien d articles ont ete fondus, et le gardant porte les medias fondus." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 4 : le score — LLM quand il y a une clé, déterministe sinon

**Fichiers :**
- Créer : `backend/app/services/news_rank.py`
- Test : `backend/tests/test_news_rank.py`

**Coût de patch :** nul, backend pur.

- [ ] **Étape 1 : écrire le banc qui échoue**

Créer `backend/tests/test_news_rank.py` :

```python
# -*- coding: utf-8 -*-
"""Le score de pertinence News (plan 2026-09-03, lot 1, P1 puis P2).

Aucun reseau : `summarizer._chat_dispatch` est monkeypatche, y compris dans
le cas ou il rend (None, "") — le cas NORMAL quand aucune cle n'est reglee.

Run (depuis backend/) : python tests/test_news_rank.py
"""
import json
import os
import pathlib
import sys
import tempfile

import pytest

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

_tmp = tempfile.mkdtemp()
os.environ["DATABASE_URL"] = \
    f"sqlite+aiosqlite:///{pathlib.Path(_tmp, 't.db').as_posix()}"
os.environ.setdefault("FAL_KEY", "test-key")
os.environ["IMAGES_FOLDER"] = str(pathlib.Path(_tmp, "images"))
os.environ["OUTPUTS_FOLDER"] = str(pathlib.Path(_tmp, "outputs"))
pathlib.Path(_tmp, "images").mkdir(exist_ok=True)
pathlib.Path(_tmp, "outputs").mkdir(exist_ok=True)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))


def _it(titre, source="CoinDesk", sid="s1", resume=""):
    return {"id": titre[:10], "title": titre, "source_name": source,
            "source_id": sid, "summary": resume, "link": "https://x/1",
            "published": "2026-09-03T11:00:00+00:00", "doublons": []}


def test_sans_llm_le_score_est_deterministe_et_borne(monkeypatch):
    from app.services import news_rank as R, summarizer
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (None, ""))
    items = [_it("Solana network hits record daily transactions"),
             _it("Local bakery wins regional prize", source="NPR", sid="s9")]
    classes = R.classer(items, brief="crypto Solana deepotus")
    assert all(0 <= i["score"] <= 100 for i in classes)
    assert classes[0]["title"].startswith("Solana")
    assert classes[0]["score_origine"] == "deterministe"
    assert "solana" in classes[0]["score_pourquoi"]


def test_le_meme_flux_donne_deux_fois_le_meme_score_sans_llm(monkeypatch):
    from app.services import news_rank as R, summarizer
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (None, ""))
    items = [_it("Bitcoin ETF outflows accelerate"),
             _it("AI Act delayed once more", sid="s2")]
    a = [i["score"] for i in R.classer(items, brief="crypto")]
    b = [i["score"] for i in R.classer(items, brief="crypto")]
    assert a == b


def test_le_llm_donne_le_score_et_son_motif(monkeypatch):
    from app.services import news_rank as R, summarizer
    reponse = json.dumps([{"id": "Solana net", "score": 93,
                           "pourquoi": "sujet coeur de la communaute"},
                          {"id": "Local bake", "score": 4,
                           "pourquoi": "hors sujet"}])
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (reponse, "anthropic"))
    items = [_it("Local bakery wins regional prize", source="NPR", sid="s9"),
             _it("Solana network hits record daily transactions")]
    classes = R.classer(items, brief="crypto Solana deepotus")
    assert classes[0]["score"] == 93
    assert classes[0]["score_origine"] == "anthropic"
    assert classes[0]["score_pourquoi"] == "sujet coeur de la communaute"
    assert classes[1]["score"] == 4


def test_un_llm_qui_bavarde_autour_du_json_est_quand_meme_lu(monkeypatch):
    from app.services import news_rank as R, summarizer
    # La cloture est ecrite par concatenation : trois accents graves ecrits
    # tels quels ici couperaient le bloc de code du plan en deux.
    cloture = "`" * 3
    bavard = ("Voici mon analyse :\n" + cloture + "json\n"
              '[{"id": "Solana net", "score": 70, "pourquoi": "ok"}]\n'
              + cloture + "\nEn esperant que cela aide.")
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (bavard, "openai"))
    classes = R.classer([_it("Solana network hits record daily transactions")],
                        brief="crypto")
    assert classes[0]["score"] == 70
    assert classes[0]["score_origine"] == "openai"


def test_un_json_illisible_retombe_sur_le_deterministe(monkeypatch):
    from app.services import news_rank as R, summarizer
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: ("je ne sais pas faire", "gemini"))
    classes = R.classer([_it("Solana network hits record daily transactions")],
                        brief="crypto Solana")
    assert classes[0]["score_origine"] == "deterministe"


def test_un_fournisseur_qui_leve_ne_casse_pas_l_ecran(monkeypatch):
    from app.services import news_rank as R, summarizer

    def _boum(p, s, m):
        raise RuntimeError("socket ferme")

    monkeypatch.setattr(summarizer, "_chat_dispatch", _boum)
    classes = R.classer([_it("Solana network hits record daily transactions")],
                        brief="crypto Solana")
    assert classes[0]["score_origine"] == "deterministe"


def test_un_id_inconnu_rendu_par_le_llm_est_ignore(monkeypatch):
    from app.services import news_rank as R, summarizer
    monkeypatch.setattr(
        summarizer, "_chat_dispatch",
        lambda p, s, m: (json.dumps([{"id": "fantome", "score": 99,
                                      "pourquoi": "invente"}]), "anthropic"))
    classes = R.classer([_it("Solana network hits record daily transactions")],
                        brief="crypto")
    assert classes[0]["score_origine"] == "deterministe"
    assert classes[0]["score"] != 99


def test_un_score_hors_bornes_est_ramene_dans_0_100(monkeypatch):
    from app.services import news_rank as R, summarizer
    monkeypatch.setattr(
        summarizer, "_chat_dispatch",
        lambda p, s, m: (json.dumps([{"id": "Solana net", "score": 4000,
                                      "pourquoi": "trop"}]), "anthropic"))
    classes = R.classer([_it("Solana network hits record daily transactions")],
                        brief="crypto")
    assert classes[0]["score"] == 100


def test_les_cinq_premiers_sont_marques_en_tete(monkeypatch):
    from app.services import news_rank as R, summarizer
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (None, ""))
    items = [_it(f"Solana sujet numero {n} de la journee", sid=f"s{n}")
             for n in range(9)]
    classes = R.classer(items, brief="crypto Solana")
    en_tete = [i for i in classes if i["en_tete"]]
    assert R.EN_TETE_MAX == 5
    assert len(en_tete) == 5
    assert classes[:5] == en_tete


def test_une_liste_vide_ne_leve_pas():
    from app.services import news_rank as R
    assert R.classer([], brief="crypto") == []


if __name__ == "__main__":
    raise SystemExit(pytest.main([__file__, "-q"]))
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_rank.py
```

Attendu : `10 errors`, chacune
`ModuleNotFoundError: No module named 'app.services.news_rank'`.

- [ ] **Étape 3 : écrire le module**

Créer `backend/app/services/news_rank.py` :

```python
# -*- coding: utf-8 -*-
"""Le score de pertinence News (plan 2026-09-03, lot 1, P1 puis P2).

Deuxieme etage, apres le filtre gratuit : ce qui a survecu recoit un score
0-100 — « ce sujet merite-t-il le reel du jour ». UN SEUL appel LLM pour tout
le lot (pas un par article), sur des titres deja filtres.

Trois garanties, dans cet ordre :
  1. sans cle, si le LLM rend None, s'il leve, ou si sa reponse n'est pas
     lisible, le score DETERMINISTE prend la main — l'ecran reste trie ;
  2. un score est toujours dans [0, 100] et porte son origine et son motif ;
  3. `classer` ne leve jamais.
"""
import json
import unicodedata

from loguru import logger

from app.services.news_filter import jetons

EN_TETE_MAX = 5          # « les 3 a 5 meilleurs du jour en tete » (R8 P1)
LOT_MAX = 40             # articles envoyes au LLM en un appel
TITRE_MAX = 140

_SYSTEME = (
    "You score news headlines for a deep-sea themed crypto brand called "
    "deepotus, which publishes one short vertical video per day. Reply with "
    "JSON ONLY: a list of {\"id\": str, \"score\": int 0-100, \"pourquoi\": "
    "str}, one entry per headline given, no preamble, no code fence."
)


def _sans_accents(t: str) -> str:
    t = unicodedata.normalize("NFKD", t or "")
    return "".join(c for c in t if not unicodedata.combining(c)).lower()


def score_deterministe(item: dict, mots_du_brief: list[str]) -> tuple[int, str]:
    """Score sans fournisseur. Volontairement simple et STABLE : deux passages
    sur le meme flux rendent le meme nombre.

    Bareme, somme bornee a 100 :
      +18 par mot du brief present dans le titre ou le resume (plafond 54) ;
      +12 par media distinct ayant couvert le meme sujet — les `doublons` du
          dedoublonnage (plafond 24) : un sujet repris est un sujet qui compte ;
      +12 si l'article a un corps exploitable (au moins 40 jetons distincts) ;
      +10 de socle, pour qu'un article hors brief ne soit pas a zero.
    """
    corpus = _sans_accents(f"{item.get('title', '')} {item.get('summary', '')} "
                           f"{item.get('essence', '')}")
    touches = [m for m in mots_du_brief if m and m in corpus]
    pts_mots = min(54, 18 * len(touches))
    reprises = len(item.get("doublons") or [])
    pts_reprises = min(24, 12 * reprises)
    corps = len(set(jetons(item.get("essence") or item.get("summary") or "")))
    pts_corps = 12 if corps >= 40 else 0
    total = min(100, 10 + pts_mots + pts_reprises + pts_corps)
    bouts = []
    if touches:
        bouts.append("brief : " + ", ".join(touches[:3]))
    if reprises:
        bouts.append(f"{reprises} media(s) sur le meme sujet")
    if pts_corps:
        bouts.append("article lisible")
    return total, " ; ".join(bouts) or "aucun signal, socle seulement"


def _mots_du_brief(brief: str) -> list[str]:
    return [_sans_accents(m) for m in jetons(brief or "")]


def _extraire_json(texte: str):
    """Le premier tableau JSON du texte, ou None. Un LLM qui entoure sa
    reponse d'explications ou d'une cloture en triples accents graves reste
    lisible."""
    if not texte:
        return None
    depart = texte.find("[")
    fin = texte.rfind("]")
    if depart < 0 or fin <= depart:
        return None
    try:
        d = json.loads(texte[depart:fin + 1])
    except json.JSONDecodeError:
        return None
    return d if isinstance(d, list) else None


def _scores_llm(items: list[dict], brief: str) -> tuple[dict, str]:
    """Rend ({id: (score, pourquoi)}, fournisseur) — ({}, "") si indisponible."""
    from app.services import summarizer
    lignes = "\n".join(
        f'- id={it.get("id")} | {str(it.get("title") or "")[:TITRE_MAX]}'
        for it in items[:LOT_MAX])
    prompt = (
        f"Brand brief: {brief or 'deep-sea crypto brand, community of traders'}"
        f"\n\nHeadlines:\n{lignes}\n\n"
        "Score each headline 0-100 for how much it deserves today's video.")
    try:
        brut, fournisseur = summarizer._chat_dispatch(
            prompt, _SYSTEME, 200 + 40 * min(len(items), LOT_MAX))
    except Exception as e:  # noqa: BLE001 - un fournisseur ne casse pas l'ecran
        logger.warning(f"news_rank: dispatch en echec ({e}) - deterministe")
        return {}, ""
    if not brut:
        return {}, ""
    lu = _extraire_json(brut)
    if not lu:
        logger.warning("news_rank: reponse LLM illisible - deterministe")
        return {}, ""
    connus = {str(it.get("id")) for it in items}
    out: dict = {}
    for ligne in lu:
        if not isinstance(ligne, dict):
            continue
        ident = str(ligne.get("id") or "")
        if ident not in connus:
            continue
        try:
            score = int(round(float(ligne.get("score"))))
        except (TypeError, ValueError):
            continue
        out[ident] = (max(0, min(100, score)),
                      str(ligne.get("pourquoi") or "")[:200])
    return out, (fournisseur if out else "")


def classer(items: list[dict], *, brief: str = "",
            penalites: dict | None = None) -> list[dict]:
    """Rend une COPIE des articles, du meilleur au pire, chacun portant
    `score`, `score_origine`, `score_pourquoi` et `en_tete`.

    `penalites` : {source_id: points a retrancher} — rempli par la tache 8
    (equilibre des sources) ; absent au bac P1.
    """
    if not items:
        return []
    mots = _mots_du_brief(brief)
    llm, fournisseur = _scores_llm(items, brief)
    penalites = penalites or {}
    sortie: list[dict] = []
    for it in items:
        copie = dict(it)
        ident = str(it.get("id"))
        if ident in llm:
            score, pourquoi = llm[ident]
            origine = fournisseur
        else:
            score, pourquoi = score_deterministe(it, mots)
            origine = "deterministe"
        malus = int(penalites.get(it.get("source_id"), 0))
        if malus:
            pourquoi = f"{pourquoi} ; -{malus} source sur-representee"
        copie["score"] = max(0, min(100, score - malus))
        copie["score_origine"] = origine
        copie["score_pourquoi"] = pourquoi
        copie["en_tete"] = False
        sortie.append(copie)
    # tri stable : score decroissant, puis le plus recent d'abord
    sortie.sort(key=lambda x: str(x.get("published") or ""), reverse=True)
    sortie.sort(key=lambda x: x["score"], reverse=True)
    for c in sortie[:EN_TETE_MAX]:
        c["en_tete"] = True
    return sortie
```

- [ ] **Étape 4 : lancer le banc pour le voir vert**

Depuis `backend/` :

```
python tests/test_news_rank.py
```

Attendu : `10 passed`.

- [ ] **Étape 5 : commit**

```
git add backend/app/services/news_rank.py backend/tests/test_news_rank.py
git commit -m "news : le score, un seul appel pour le lot et un repli qui tient" -m "Le LLM note tout le lot en un appel sur des titres deja filtres ; sa reponse est lue meme entouree de bavardage, ses identifiants inconnus sont ignores, ses scores hors bornes ramenes, et une exception du fournisseur ne casse pas l ecran. Sans cle, reponse vide ou JSON illisible, le score deterministe prend la main : le meme flux rend deux fois le meme nombre. Les cinq premiers sont marques en tete." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 5 : les routes — réglages du filtre et classement du jour

**Fichiers :**
- Modifier : `backend/app/api/routes.py:1944-1946` (juste après `list_news_items`)
- Modifier : `backend/app/models/schemas.py:585` (fin du bloc News)
- Test : `backend/tests/test_news_routes.py`

**Coût de patch :** nul, backend pur.

- [ ] **Étape 1 : écrire le banc qui échoue**

Créer `backend/tests/test_news_routes.py` :

```python
# -*- coding: utf-8 -*-
"""Les routes News du lot 1 (plan 2026-09-03, taches 5, 8, 9, 10).

Aucun reseau : le cache est ECRIT a la main sur le disque et le LLM est
monkeypatche. Banc-miroir : on verifie le JSON rendu par la route et le
fichier de reglages ecrit sur le disque.

Run (depuis backend/) : python tests/test_news_routes.py
"""
import json
import os
import pathlib
import sys
import tempfile

import pytest

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

_tmp = tempfile.mkdtemp()
os.environ["DATABASE_URL"] = \
    f"sqlite+aiosqlite:///{pathlib.Path(_tmp, 't.db').as_posix()}"
os.environ.setdefault("FAL_KEY", "test-key")
os.environ["IMAGES_FOLDER"] = str(pathlib.Path(_tmp, "images"))
os.environ["OUTPUTS_FOLDER"] = str(pathlib.Path(_tmp, "outputs"))
pathlib.Path(_tmp, "images").mkdir(exist_ok=True)
pathlib.Path(_tmp, "outputs").mkdir(exist_ok=True)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

from fastapi.testclient import TestClient  # noqa: E402

from app.main import app  # noqa: E402

client = TestClient(app)

ARTICLES = [
    {"id": "a1", "source_id": "s1", "source_name": "CoinDesk",
     "title": "Solana network hits record daily transactions", "summary": "",
     "link": "https://c/1", "published": "2026-09-03T11:00:00+00:00",
     "doublons": []},
    {"id": "a2", "source_id": "s2", "source_name": "Spammy Feed",
     "title": "Free giveaway airdrop inside", "summary": "",
     "link": "https://s/1", "published": "2026-09-03T10:00:00+00:00",
     "doublons": []},
]


def _poser_cache(items=None):
    from app.services.news_service import news_service
    news_service.cache_path.write_text(
        json.dumps({"fetched_at": "2026-09-03T11:30:00+00:00",
                    "items": ARTICLES if items is None else items,
                    "errors": [], "source_count": 2, "merged_count": 0},
                   ensure_ascii=False),
        encoding="utf-8")


def test_les_reglages_se_lisent_et_s_ecrivent_par_la_route():
    r = client.get("/api/news/filter")
    assert r.status_code == 200
    assert "fraicheur_h" in r.json()
    r = client.put("/api/news/filter", json={
        "mots_cles": ["Solana"], "sources_noires": [],
        "mots_noirs": ["giveaway"], "fraicheur_h": 48})
    assert r.status_code == 200
    assert r.json()["mots_cles"] == ["solana"]
    from app.services import news_filter as F
    sur_disque = json.loads(F.chemin_reglages().read_text("utf-8"))
    assert sur_disque["mots_noirs"] == ["giveaway"]


def test_le_classement_rend_les_gardes_les_ecartes_et_leurs_motifs(monkeypatch):
    from app.services import summarizer
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (None, ""))
    _poser_cache()
    client.put("/api/news/filter", json={
        "mots_cles": [], "sources_noires": [], "mots_noirs": ["giveaway"],
        "fraicheur_h": 24 * 30})
    r = client.post("/api/news/rank", json={"brief": "crypto Solana"})
    assert r.status_code == 200
    j = r.json()
    assert [i["id"] for i in j["items"]] == ["a1"]
    assert j["items"][0]["en_tete"] is True
    assert j["items"][0]["score_origine"] == "deterministe"
    assert j["ecartes"] == [{"id": "a2",
                             "title": "Free giveaway airdrop inside",
                             "motif": "mot sur liste noire"}]
    assert j["compte"] == {"lus": 2, "gardes": 1, "ecartes": 1}


def test_le_classement_sur_un_cache_vide_rend_une_liste_vide(monkeypatch):
    from app.services import summarizer
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (None, ""))
    _poser_cache(items=[])
    r = client.post("/api/news/rank", json={"brief": ""})
    assert r.status_code == 200
    assert r.json()["items"] == []
    assert r.json()["compte"]["lus"] == 0


def test_une_fraicheur_hors_bornes_est_refusee_par_le_schema():
    r = client.put("/api/news/filter", json={"fraicheur_h": 0})
    assert r.status_code == 422


if __name__ == "__main__":
    raise SystemExit(pytest.main([__file__, "-q"]))
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_routes.py
```

Attendu : `4 failed` — les appels rendent `404`, sauf le dernier qui rend
`404` au lieu de `422`.

- [ ] **Étape 3 : ajouter les schémas**

Dans `backend/app/models/schemas.py`, ajouter à la FIN du fichier (après
`NewsIllustrationResponse`, ligne 585) :

```python
# ---- 03/09/2026 : filtre gratuit puis score (plan News, P1 et P2) ----

class NewsFilterSettings(BaseModel):
    mots_cles: List[str] = []
    sources_noires: List[str] = []
    mots_noirs: List[str] = []
    fraicheur_h: int = Field(48, ge=1, le=24 * 30)


class NewsRankRequest(BaseModel):
    brief: str = Field("", max_length=2000)


class NewsRankedItem(BaseModel):
    id: str
    title: str
    source_name: str = ""
    source_id: str = ""
    link: str = ""
    published: str = ""
    summary: str = ""
    score: int = 0
    score_origine: str = ""
    score_pourquoi: str = ""
    en_tete: bool = False
    doublons: List[dict] = []
    deja_couvert: Optional[str] = None


class NewsDropped(BaseModel):
    id: str
    title: str = ""
    motif: str = ""


class NewsRankResponse(BaseModel):
    items: List[NewsRankedItem] = []
    ecartes: List[NewsDropped] = []
    compte: dict = {}
    fetched_at: Optional[str] = None
```

- [ ] **Étape 4 : ajouter les routes**

Dans `backend/app/api/routes.py`, JUSTE APRÈS la fonction `list_news_items`
(ligne 1946), insérer :

```python
@router.get("/news/filter", response_model=NewsFilterSettings)
async def get_news_filter():
    """Les réglages du filtre gratuit (mots-clés, listes noires, fraîcheur)."""
    from app.services import news_filter
    return NewsFilterSettings(**news_filter.lire_reglages())


@router.put("/news/filter", response_model=NewsFilterSettings)
async def put_news_filter(request: NewsFilterSettings):
    """Écrit les réglages NORMALISÉS et rend ce qui a été écrit."""
    from app.services import news_filter
    return NewsFilterSettings(
        **news_filter.ecrire_reglages(request.model_dump()))


@router.post("/news/rank", response_model=NewsRankResponse)
async def rank_news(request: NewsRankRequest):
    """Le tri du jour : filtre gratuit, puis score sur ce qui reste.

    Aucun appel payant n'est fait sur un article que le filtre a écarté —
    c'est l'ordre exigé par le plan (P1). La réponse dit AUSSI ce qui est
    tombé et pourquoi : un article écarté sans motif est un article perdu.
    """
    from app.services import news_filter, news_rank
    cache = news_service.get_items()
    bruts = cache.get("items") or []
    gardes, motifs = news_filter.filtrer(bruts, news_filter.lire_reglages())
    classes = await asyncio.to_thread(
        news_rank.classer, gardes, brief=request.brief)
    par_id = {str(i.get("id")): i for i in bruts}
    ecartes = [
        NewsDropped(
            id=str(k),
            title=str((par_id.get(str(k)) or {}).get("title") or ""),
            motif=v)
        for k, v in motifs.items()
    ]
    champs = set(NewsRankedItem.model_fields)
    return NewsRankResponse(
        items=[NewsRankedItem(**{k: v for k, v in it.items() if k in champs})
               for it in classes],
        ecartes=ecartes,
        compte={"lus": len(bruts), "gardes": len(classes),
                "ecartes": len(ecartes)},
        fetched_at=cache.get("fetched_at"),
    )
```

Puis compléter l'import des schémas en tête de `routes.py` : ajouter
`NewsFilterSettings`, `NewsRankRequest`, `NewsRankedItem`, `NewsDropped` et
`NewsRankResponse` à la liste importée depuis `app.models.schemas`.

- [ ] **Étape 5 : lancer le banc pour le voir vert**

Depuis `backend/` :

```
python tests/test_news_routes.py
```

Attendu : `4 passed`.

- [ ] **Étape 6 : commit**

```
git add backend/app/api/routes.py backend/app/models/schemas.py backend/tests/test_news_routes.py
git commit -m "news : trois routes pour le filtre et le classement du jour" -m "GET et PUT sur les reglages du filtre, POST rank qui filtre puis score. Aucun appel paye sur un article deja tombe. La reponse porte les ecartes et leur motif, pour que l ecran puisse les montrer au lieu de les faire disparaitre." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 6 : le patch du bundle `newsrank` — le tri devient vrai

**Fichiers :**
- Créer : `scripts/patch_bundle_newsrank.py`
- Modifier : `frontend/dist/assets/index-BEOJX8L5.js` (par le patcher, jamais à la main)
- Test : `backend/tests/test_news_bundle.py`

**Coût de patch :** tag NEUF `newsrank`, backup `.js.bak_newsrank`, position
EN QUEUE (après `seedance25`), **huit ancres comptées à 1 le 03/09** :
`function pm({variant:e,go:Go}){` ·
`const[sty,setSty]=x.useState("deep-sea");` · le tri par longueur de résumé ·
la ligne de titre de la carte d'article · `function payload(){...}` ·
`r.jsxs(te,{tone:"cyan",children:[sel.size," selected"]})` ·
`{value:"relevance",label:"By relevance"}` · le bloc JSX « Illustration /
Style ».

**Attention mesurée :** `sty` apparaît DEUX fois dans l'écran News — l'état
(offset 450032) et le JSX qui le lit (offset 460332). Retirer l'un sans
l'autre casse le rendu par `ReferenceError` sans erreur de compilation. Les
sections S2 et S8 vont donc toujours ensemble.

- [ ] **Étape 1 : écrire le banc-miroir qui échoue**

Créer `backend/tests/test_news_bundle.py` :

```python
# -*- coding: utf-8 -*-
"""L'ecran News patche (plan 2026-09-03, taches 6 et 15).

Banc-MIROIR : il lit le BUNDLE ECRIT, pas le patcher qui pretend l'ecrire.
Il verifie en plus que le patcher porte ses gardes de chaine.

Run (depuis backend/) : python tests/test_news_bundle.py
"""
import pathlib
import sys

import pytest

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

_RACINE = pathlib.Path(__file__).resolve().parents[2]
_BUNDLE = _RACINE / "frontend" / "dist" / "assets" / "index-BEOJX8L5.js"


def _src():
    return _BUNDLE.read_text("utf-8")


def test_le_tri_par_longueur_de_resume_a_disparu():
    assert '(b.summary||"").length-(a.summary||"").length' not in _src()


def test_l_ecran_appelle_les_routes_de_filtre_et_de_classement():
    s = _src()
    assert s.count("/api/news/rank") == 1
    assert s.count("/api/news/filter") == 2      # lecture + ecriture


def test_le_score_son_motif_et_la_tete_de_liste_sont_affiches():
    s = _src()
    assert s.count("__dzNewsRank") == 2          # definition + appel
    assert "score_pourquoi" in s and "score_origine" in s
    assert s.count("en_tete") >= 2


def test_les_ecartes_sont_comptes_et_leurs_motifs_lisibles_au_survol():
    """« motif » existait deja 9 fois dans le bundle avant le patch : le banc
    ne peut pas se contenter de sa presence, il compte la CHAINE du patch."""
    s = _src()
    assert s.count("ecartes") == 2               # l'etat + le remplissage
    assert s.count("z.motif") == 1               # l'infobulle de la puce
    assert "ecartes ce jour" in s


def test_l_option_de_tri_dit_ce_qu_elle_fait():
    s = _src()
    assert '{value:"relevance",label:"By relevance"}' not in s
    assert '{value:"relevance",label:"Score du jour"}' in s


def test_le_selecteur_de_style_mort_a_ete_debranche_des_DEUX_cotes():
    """Le select « Illustration / Style » n'etait envoye a aucune route. Il
    apparaissait DEUX fois : l'etat et le JSX qui le lit. Retirer l'un sans
    l'autre casserait le rendu par ReferenceError, sans erreur de
    compilation — le banc verifie donc les deux."""
    s = _src()
    assert 'const[sty,setSty]=x.useState("deep-sea");' not in s
    assert 'options:["deep-sea","cinematic","glitch","documentary"]' not in s
    assert 'r.jsx(ie,{label:"Brief de campagne"' in s


def test_le_payload_emporte_l_identifiant_et_la_date():
    s = _src()
    assert 'id:i.id,published:i.published||""' in s


def test_le_patcher_newsrank_est_garde():
    p = (_RACINE / "scripts" / "patch_bundle_newsrank.py").read_text("utf-8")
    assert "guard_downstream" in p
    assert "SPEC_CHAR_DELTA" in p and "SPEC_BYTE_DELTA" in p
    assert 'newline=""' in p
    assert "--force-unchained" in p
    assert ".bak_newsrank" in p


def test_le_backup_du_patcher_est_bien_en_queue_de_chaine():
    baks = sorted(_BUNDLE.parent.glob(_BUNDLE.name + ".bak_*"),
                  key=lambda p: p.stat().st_mtime)
    tags = [p.name.rsplit(".bak_", 1)[1] for p in baks]
    assert "newsrank" in tags, tags
    assert tags.index("newsrank") > tags.index("seedance25")


if __name__ == "__main__":
    raise SystemExit(pytest.main([__file__, "-q"]))
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_bundle.py
```

Attendu : `9 failed` — le premier parce que le tri par longueur est encore là,
l'avant-dernier sur `FileNotFoundError` du patcher.

- [ ] **Étape 3 : écrire le patcher**

Créer `scripts/patch_bundle_newsrank.py` :

```python
# -*- coding: utf-8 -*-
# scripts/patch_bundle_newsrank.py
"""Patcher assert-garde : le tri REEL de l'ecran News (plan 2026-09-03, P1).

BASELINE : bundle POST-patch seedance25 (queue de chaine au 03/09).
Backup dedie : `.js.bak_newsrank`. Position : EN QUEUE, apres seedance25.

Ce que le bundle faisait, MESURE le 03/09 :
  - « By relevance » triait par LONGUEUR DE RESUME
    (`(b.summary||"").length-(a.summary||"").length`) ;
  - le select « Illustration / Style » (`sty`) n'etait envoye a aucune route ;
  - `payload()` n'emportait ni `id` ni `published`, donc le backend ne
    pouvait ni marquer l'historique ni equilibrer les sources.

Sept sections :
  S1  `__dzNewsRank` et `__dzNewsFilter` : les ponts vers POST
      /api/news/rank et GET/PUT /api/news/filter, poses avant le composant ;
  S2  l'etat du composant : brief, liste classee, ecartes, repli ;
  S3  le tri : « By relevance » consomme le classement au lieu de compter
      des caracteres, et replie sur les cinq en tete ;
  S4  la carte d'article : score, motif, marque « en tete », marque « deja
      couvert » (remplie par la tache 7) ;
  S5  `payload()` emporte `id` et `published` ;
  S6  la barre d'outils : la puce « N ecartes ce jour » (motifs au survol)
      et le bouton qui deplie la liste au-dela des cinq en tete ;
  S7  le libelle du tri : « By relevance » devient « Score du jour » ;
  S8  le bloc « Illustration / Style » (le select mort) devient le champ
      « Brief de campagne » qui pilote le score. S8 est INSEPARABLE de S2 :
      `sty` est declare en S2 et LU en S8, retirer l'un sans l'autre casse le
      rendu par ReferenceError, sans erreur de compilation.

DANGERS : lancement SEUL ; jamais `repatch_all --from` un tag amont sans
rejouer toute la queue ; `newline=""` partout (CRLF) ; jamais d'ancre
imprimee (cp1252 sur la console Windows).

Run :
    python scripts/patch_bundle_newsrank.py            # applique
    python scripts/patch_bundle_newsrank.py --check    # n'ecrit rien
    python scripts/patch_bundle_newsrank.py --deltas   # affiche les deltas
"""
import os
import pathlib
import shutil
import sys
import time

REL_BUNDLE = pathlib.Path("frontend/dist/assets/index-BEOJX8L5.js")
TAG = "newsrank"
MARKER = "__dzNewsRank"
MARKER_ATTENDU = 2

# Deltas figes a l'etape 4 de la tache : `--deltas` les imprime, on les
# recopie ici, et main() refuse de tourner si le calcul ne les retrouve pas.
SPEC_CHAR_DELTA = 0
SPEC_BYTE_DELTA = 0

# Comptes releves sur le bundle du depot le 03/09/2026.
STABLE_PROBES = [
    ("news-items", "/api/news/items", 1),
    ("news-script", "/api/news/script", 2),
    ("news-illu", "/api/news/illustration", 1),
    ("news-sources", "/api/news/sources", 10),
    ("seedance25", "seedance-2.5", 1),
]

POST_COUNTS = [
    ("/api/news/rank", 1),
    ("/api/news/filter", 2),
    ("score_pourquoi", 1),
    ("deja_couvert", 2),
    ("ecartes", 2),
    ("z.motif", 1),
    ('{value:"relevance",label:"Score du jour"}', 1),
    ('r.jsx(ie,{label:"Brief de campagne"', 1),
    ('options:["deep-sea","cinematic","glitch","documentary"]', 0),
]

# ── S1 : les ponts, poses juste avant la fonction de l'ecran News ───────────
PONT = (
    "async function __dzNewsRank(brief){try{"
    'var R=await fetch("/api/news/rank",{method:"POST",'
    'headers:{"Content-Type":"application/json"},'
    'body:JSON.stringify({brief:brief||""})});'
    "if(!R.ok)return null;return await R.json()}catch(e){return null}}"
    "async function __dzNewsFilter(patch){try{"
    'var R=patch?await fetch("/api/news/filter",{method:"PUT",'
    'headers:{"Content-Type":"application/json"},body:JSON.stringify(patch)})'
    ':await fetch("/api/news/filter");'
    "if(!R.ok)return null;return await R.json()}catch(e){return null}}"
)

_A1 = "function pm({variant:e,go:Go}){"
_R1 = PONT + _A1

# ── S2 : l'etat (remplace le select mort) ──────────────────────────────────
_A2 = 'const[sty,setSty]=x.useState("deep-sea");'
_R2 = ('const[brief,setBrief]=x.useState("");'
       "const[ranked,setRanked]=x.useState(null);"
       "const[ecartes,setEcartes]=x.useState([]);"
       "const[plie,setPlie]=x.useState(!0);"
       "x.useEffect(function(){var on=!0;"
       "__dzNewsRank(brief).then(function(j){if(on&&j){setRanked(j);"
       "setEcartes(j.ecartes||[])}});"
       "return function(){on=!1}},[brief]);")

# ── S3 : le tri ────────────────────────────────────────────────────────────
_A3 = ('if(sort==="relevance")shown=shown.slice().sort'
       '((a,b)=>(b.summary||"").length-(a.summary||"").length);')
_R3 = ('if(sort==="relevance"&&ranked&&ranked.items){'
       "var ord={},sc={};ranked.items.forEach(function(z,ix){"
       "ord[z.id]=ix;sc[z.id]=z});"
       "shown=shown.filter(function(z){return ord[z.id]!=null})"
       ".slice().sort(function(a,b){return ord[a.id]-ord[b.id]})"
       ".map(function(z){var m=sc[z.id];return Object.assign({},z,"
       "{score:m.score,score_pourquoi:m.score_pourquoi,"
       "score_origine:m.score_origine,en_tete:m.en_tete,"
       "deja_couvert:m.deja_couvert})});"
       "if(plie)shown=shown.filter(function(z){return z.en_tete})}")

# ── S4 : la carte d'article ────────────────────────────────────────────────
_A4 = ('r.jsx("div",{style:{fontSize:13,color:"var(--ink-strong)",'
       'fontWeight:500,marginBottom:4},children:i.title})')
_R4 = (_A4 + ',i.score!=null?r.jsxs("div",{style:{display:"flex",gap:8,'
       'alignItems:"center",flexWrap:"wrap",fontSize:10.5,marginBottom:4},'
       'children:[r.jsx("span",{className:"mono",style:{color:i.en_tete?'
       '"var(--cyan)":"var(--ink-muted)"},children:i.score+"/100"}),'
       'r.jsx("span",{style:{color:"var(--ink-soft)"},'
       'children:i.score_pourquoi||i.score_origine}),'
       'i.deja_couvert?r.jsx("span",{style:{color:"var(--amber)"},'
       'children:"deja couvert : "+i.deja_couvert}):null]}):null')

# ── S5 : payload ───────────────────────────────────────────────────────────
_A5 = ("function payload(){return chosen().map(i=>({title:i.title,"
       "summary:i.summary,source_name:i.source,link:i.link}))}")
_R5 = ("function payload(){return chosen().map(i=>({title:i.title,"
       "summary:i.summary,source_name:i.source,link:i.link,"
       'id:i.id,published:i.published||""}))}')

# ── S6 : la barre d'outils — puce des ecartes + repli ──────────────────────
_A6 = 'r.jsxs(te,{tone:"cyan",children:[sel.size," selected"]})'
_R6 = (_A6 + ',ecartes.length?r.jsx(te,{tone:"amber",'
       'title:ecartes.map(function(z){return z.title+" : "+z.motif})'
       '.join("\\n"),children:ecartes.length+" ecartes ce jour"}):null,'
       'ranked&&ranked.items&&ranked.items.length>5?r.jsx(K,{variant:"ghost",'
       'size:"sm",onClick:function(){setPlie(!plie)},'
       'children:plie?"Tout voir":"En tete seulement"}):null')

# ── S7 : le libelle du tri ─────────────────────────────────────────────────
_A7 = '{value:"relevance",label:"By relevance"}'
_R7 = '{value:"relevance",label:"Score du jour"}'

# ── S8 : le select mort devient le champ de brief (INSEPARABLE de S2) ──────
_A8 = ('r.jsx(ie,{label:"Illustration",children:r.jsx(O,{label:"Style",'
       'children:r.jsx(re,{value:sty,options:["deep-sea","cinematic",'
       '"glitch","documentary"],onChange:v=>setSty(v)})})})')
_R8 = ('r.jsx(ie,{label:"Brief de campagne",children:r.jsx(O,'
       '{label:"Ce qui compte aujourd hui",children:r.jsx(le,'
       '{placeholder:"crypto, Solana, macro...",value:brief,'
       'onChange:function(v){setBrief(typeof v==="string"?v:'
       'v&&v.target?v.target.value:"")}})})})')

PATCHES = [
    ("S1-pont", _A1, _R1),
    ("S2-etat", _A2, _R2),
    ("S3-tri", _A3, _R3),
    ("S4-carte", _A4, _R4),
    ("S5-payload", _A5, _R5),
    ("S6-barre", _A6, _R6),
    ("S7-libelle", _A7, _R7),
    ("S8-brief", _A8, _R8),
]


def deltas():
    dc = sum(len(rp) - len(a) for _t, a, rp in PATCHES)
    db = sum(len(rp.encode("utf-8")) - len(a.encode("utf-8"))
             for _t, a, rp in PATCHES)
    return dc, db


def check_spec_parity():
    dc, db = deltas()
    if (dc, db) != (SPEC_CHAR_DELTA, SPEC_BYTE_DELTA):
        raise SystemExit(
            f"[{TAG}] parite spec rompue : calcule {dc} car / {db} o, "
            f"spec {SPEC_CHAR_DELTA} car / {SPEC_BYTE_DELTA} o. Aborting.")
    return dc, db


def guard_downstream(bak):
    if not bak.exists():
        return
    stem = bak.name.rsplit(".bak_", 1)[0]
    for other in bak.parent.glob(stem + ".bak_*"):
        if other != bak and other.stat().st_mtime > bak.stat().st_mtime:
            raise SystemExit(
                f"[garde-chaine] backup aval detecte : {other.name}. "
                f"{TAG} doit rester le DERNIER maillon.")


def ensure_tail_order(bak):
    stem = bak.name.rsplit(".bak_", 1)[0]
    autres = [p.stat().st_mtime for p in bak.parent.glob(stem + ".bak_*")
              if p != bak]
    if not autres:
        return False
    top = max(autres)
    if bak.stat().st_mtime > top:
        return False
    t = max(time.time(), top + 1.0)
    os.utime(bak, (t, t))
    return True


def apply(s, anchor, replacement, tag):
    n = s.count(anchor)
    if n != 1:
        raise SystemExit(f"[{tag}] anchor count={n} (want 1). Aborting.")
    return s.replace(anchor, replacement)


def eol_stats(data):
    crlf = data.count(b"\r\n")
    return crlf, data.count(b"\n") - crlf, data.count(b"\r") - crlf


def resolve_root(args):
    if "--root" in args:
        return pathlib.Path(args[args.index("--root") + 1]).resolve()
    ici = pathlib.Path(".").resolve()
    if (ici / REL_BUNDLE).is_file():
        return ici
    return pathlib.Path(__file__).resolve().parent.parent


def main():
    args = sys.argv[1:]
    if "--deltas" in args:
        dc, db = deltas()
        print(f"[{TAG}] delta +{dc} car / +{db} o")
        return
    dc, db = check_spec_parity()
    root = resolve_root(args)
    bundle = root / REL_BUNDLE
    if not bundle.is_file():
        raise SystemExit(f"[{TAG}] bundle introuvable : {bundle}")
    bak = bundle.with_name(bundle.name + ".bak_" + TAG)
    if "--force-unchained" not in args:
        guard_downstream(bak)

    if "--check" in args:
        src = bak if bak.exists() else bundle
        s = src.read_text(encoding="utf-8", newline="")
        if MARKER in s:
            raise SystemExit(f"[{TAG}] marqueur deja present dans {src.name}.")
        for tag, anchor, _r in PATCHES:
            n = s.count(anchor)
            if n != 1:
                raise SystemExit(f"[{tag}] anchor count={n} (want 1).")
        for nom, sonde, veut in STABLE_PROBES:
            if s.count(sonde) != veut:
                raise SystemExit(f"[sonde {nom}] count={s.count(sonde)} "
                                 f"(want {veut}).")
        crlf, lf, cr = eol_stats(src.read_bytes())
        print(f"[{TAG}] applicable sur {src}")
        print(f"[{TAG}] {len(PATCHES)} ancres OK, {len(STABLE_PROBES)} sondes")
        print(f"[{TAG}] CRLF={crlf} LF-isole={lf} CR-isole={cr} ; "
              f"delta +{dc} car / +{db} o")
        return

    if not bak.exists():
        if MARKER in bundle.read_text(encoding="utf-8", newline=""):
            raise SystemExit(f"[{TAG}] marqueur present sans {bak.name}.")
        shutil.copy2(bundle, bak)
        if ensure_tail_order(bak):
            print("mtime du backup pousse en queue de chaine")
        print("backup ->", bak.name)
    else:
        shutil.copy2(bak, bundle)
        print("restore <-", bak.name)

    avant = bundle.read_bytes()
    crlf0, lf0, cr0 = eol_stats(avant)
    if lf0 or cr0:
        raise SystemExit(f"[{TAG}] fins de ligne non homogenes. Aborting.")
    s = bundle.read_text(encoding="utf-8", newline="")
    car0 = len(s)
    for tag, anchor, repl in PATCHES:
        s = apply(s, anchor, repl, tag)
    with open(bundle, "w", encoding="utf-8", newline="") as fh:
        fh.write(s)

    apres = bundle.read_bytes()
    crlf1, lf1, cr1 = eol_stats(apres)
    soucis = []
    if (crlf1, lf1, cr1) != (crlf0, 0, 0):
        soucis.append("fins de ligne changees")
    if len(apres) != len(avant) + db:
        soucis.append(f"taille {len(apres)} o, attendu {len(avant) + db}")
    if len(s) != car0 + dc:
        soucis.append(f"caracteres {len(s)}, attendu {car0 + dc}")
    if s.count(MARKER) != MARKER_ATTENDU:
        soucis.append(f"marqueur x{s.count(MARKER)} (want {MARKER_ATTENDU})")
    for sonde, veut in POST_COUNTS:
        if s.count(sonde) != veut:
            soucis.append(f"post {sonde} x{s.count(sonde)} (want {veut})")
    if soucis:
        shutil.copy2(bak, bundle)
        raise SystemExit(f"[{TAG}] VERIFICATION ECHOUEE, bundle restaure :\n  "
                         + "\n  ".join(soucis))
    print("OK - bundle patche (tri par score, ecartes montres, payload "
          "complet).")
    print(f"   taille : {len(avant)} -> {len(apres)} o (+{db})")
    print("   suite  : copie .mjs + node --check, puis DEPLOYER le bundle")


if __name__ == "__main__":
    if hasattr(sys.stdout, "reconfigure"):
        sys.stdout.reconfigure(errors="backslashreplace")
    main()
```

- [ ] **Étape 4 : relever les deltas réels et les figer**

Depuis la racine du dépôt :

```
python scripts/patch_bundle_newsrank.py --deltas
```

Attendu : une ligne `[newsrank] delta +NNNN car / +NNNN o`. Recopier les deux
nombres dans `SPEC_CHAR_DELTA` et `SPEC_BYTE_DELTA` du patcher (ils valent 0
à l'écriture ; tant qu'ils valent 0, `main()` refuse de tourner, ce qui est
le comportement voulu).

- [ ] **Étape 5 : vérifier à blanc, puis appliquer**

Depuis la racine du dépôt :

```
python scripts/patch_bundle_newsrank.py --check
python scripts/patch_bundle_newsrank.py
```

Attendu : `--check` affiche `[newsrank] 8 ancres OK, 5 sondes` puis
`CRLF=<n> LF-isole=0 CR-isole=0` ; l'application affiche
`backup -> index-BEOJX8L5.js.bak_newsrank` puis `OK - bundle patche`.

- [ ] **Étape 6 : vérifier que le bundle reste du JavaScript valide**

Depuis la racine du dépôt :

```
cp frontend/dist/assets/index-BEOJX8L5.js /tmp/dz_newsrank_check.mjs
node --check /tmp/dz_newsrank_check.mjs
```

Attendu : aucune sortie, code de retour 0. Si `node` est absent de la machine,
le dire dans le rapport de tâche et ne PAS contourner la vérification.

- [ ] **Étape 7 : lancer le banc-miroir**

Depuis `backend/` :

```
python tests/test_news_bundle.py
```

Attendu : `9 passed`.

- [ ] **Étape 8 : commit**

```
git add scripts/patch_bundle_newsrank.py frontend/dist/assets/index-BEOJX8L5.js backend/tests/test_news_bundle.py
git commit -m "news : l ecran trie par le score, plus par la longueur du resume" -m "Le tri By relevance comptait des caracteres de resume ; il consomme desormais la route de classement, replie sur les cinq en tete, montre le score, son motif et son origine, et affiche ce qui a ete ecarte. Le select de style, qui n etait envoye a aucune route, disparait. payload emporte id et published, sans quoi l historique ne peut rien marquer." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 7 : P2 — la mémoire des sujets couverts

**Fichiers :**
- Créer : `backend/app/services/news_memory.py`
- Test : `backend/tests/test_news_memory.py`

**Coût de patch :** nul, backend pur. Le fichier de mémoire vit à côté de
`sources.json` et `cache.json`, sous `news/couverture.json` : pas de
migration de base, pas de table neuve.

- [ ] **Étape 1 : écrire le banc-miroir qui échoue**

Créer `backend/tests/test_news_memory.py` :

```python
# -*- coding: utf-8 -*-
"""La memoire des sujets deja couverts (plan 2026-09-03, lot 1, P2).

Banc-miroir : il relit `couverture.json` sur le disque apres chaque note.
Aucun reseau, aucun LLM.

Run (depuis backend/) : python tests/test_news_memory.py
"""
import json
import os
import pathlib
import sys
import tempfile

import pytest

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

_tmp = tempfile.mkdtemp()
os.environ["DATABASE_URL"] = \
    f"sqlite+aiosqlite:///{pathlib.Path(_tmp, 't.db').as_posix()}"
os.environ.setdefault("FAL_KEY", "test-key")
os.environ["IMAGES_FOLDER"] = str(pathlib.Path(_tmp, "images"))
os.environ["OUTPUTS_FOLDER"] = str(pathlib.Path(_tmp, "outputs"))
pathlib.Path(_tmp, "images").mkdir(exist_ok=True)
pathlib.Path(_tmp, "outputs").mkdir(exist_ok=True)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))


@pytest.fixture(autouse=True)
def _table_rase():
    from app.services import news_memory as M
    p = M.chemin_couverture()
    if p.is_file():
        p.unlink()
    yield


def _it(titre, source="CoinDesk", sid="s1"):
    return {"id": titre[:10], "title": titre, "source_name": source,
            "source_id": sid, "link": "https://x/1",
            "published": "2026-09-03T11:00:00+00:00"}


def test_une_note_arrive_sur_le_disque_avec_ses_jetons():
    from app.services import news_memory as M
    M.noter_couverture([_it("Solana network hits record daily transactions")],
                       post_id="p1", quand="2026-09-03T12:00:00+00:00")
    ecrit = json.loads(M.chemin_couverture().read_text("utf-8"))
    assert len(ecrit["sujets"]) == 1
    sujet = ecrit["sujets"][0]
    assert sujet["post_id"] == "p1"
    assert sujet["source_id"] == "s1"
    assert "solana" in sujet["jetons"]
    assert sujet["titre"].startswith("Solana")


def test_un_article_proche_d_un_sujet_couvert_est_marque():
    from app.services import news_memory as M
    M.noter_couverture([_it("Solana network hits record daily transactions")],
                       post_id="p1", quand="2026-09-03T12:00:00+00:00")
    marques = M.marquer([
        _it("Solana network hits a record in daily transactions", sid="s2"),
        _it("Storm Bernard floods southern Spain", sid="s3"),
    ], maintenant="2026-09-04T09:00:00+00:00")
    assert marques[0]["deja_couvert"].startswith("Solana network hits record")
    assert marques[1]["deja_couvert"] is None


def test_un_sujet_couvert_hors_fenetre_ne_marque_plus():
    from app.services import news_memory as M
    M.noter_couverture([_it("Solana network hits record daily transactions")],
                       post_id="p1", quand="2026-07-01T12:00:00+00:00")
    marques = M.marquer(
        [_it("Solana network hits a record in daily transactions", sid="s2")],
        maintenant="2026-09-04T09:00:00+00:00")
    assert marques[0]["deja_couvert"] is None


def test_l_equilibre_compte_les_posts_par_source_et_rend_des_malus():
    from app.services import news_memory as M
    for n in range(4):
        M.noter_couverture([_it(f"Sujet CoinDesk numero {n}", sid="s1")],
                           post_id=f"p{n}",
                           quand=f"2026-09-0{n + 1}T12:00:00+00:00")
    M.noter_couverture([_it("Un seul sujet BBC", source="BBC", sid="s2")],
                       post_id="p9", quand="2026-09-02T12:00:00+00:00")
    malus = M.penalites_de_source(maintenant="2026-09-04T09:00:00+00:00")
    assert malus["s1"] > malus.get("s2", 0)
    assert malus["s1"] <= M.MALUS_MAX
    assert all(v >= 0 for v in malus.values())


def test_la_memoire_est_bornee_et_jette_le_plus_vieux():
    from app.services import news_memory as M
    for n in range(M.SUJETS_MAX + 15):
        M.noter_couverture([_it(f"Sujet parfaitement distinct numero {n}")],
                           post_id=f"p{n}",
                           quand="2026-09-03T12:00:00+00:00")
    ecrit = json.loads(M.chemin_couverture().read_text("utf-8"))
    assert len(ecrit["sujets"]) == M.SUJETS_MAX


def test_un_fichier_corrompu_ne_casse_ni_la_lecture_ni_la_marque():
    from app.services import news_memory as M
    M.chemin_couverture().write_text("]]pas du json[[", encoding="utf-8")
    assert M.sujets_couverts() == []
    marques = M.marquer([_it("Solana network hits record daily transactions")],
                        maintenant="2026-09-04T09:00:00+00:00")
    assert marques[0]["deja_couvert"] is None


def test_marquer_ne_mute_pas_les_articles_d_entree():
    from app.services import news_memory as M
    it = _it("Solana network hits record daily transactions")
    M.marquer([it], maintenant="2026-09-04T09:00:00+00:00")
    assert "deja_couvert" not in it


if __name__ == "__main__":
    raise SystemExit(pytest.main([__file__, "-q"]))
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_memory.py
```

Attendu : `7 errors`,
`ModuleNotFoundError: No module named 'app.services.news_memory'`.

- [ ] **Étape 3 : écrire le module**

Créer `backend/app/services/news_memory.py` :

```python
# -*- coding: utf-8 -*-
"""La memoire des sujets deja couverts (plan 2026-09-03, lot 1, P2).

Chaque reel publie laisse une trace : les jetons de ses articles, leur
source, la date. Deux usages :
  - `marquer()` : un article proche d'un sujet deja couvert porte
    `deja_couvert` = le titre du sujet en question (l'ecran l'affiche en
    ambre, il ne le cache pas — c'est a l'utilisateur de trancher) ;
  - `penalites_de_source()` : une source sur-representee sur la fenetre
    recente rend un malus, que `news_rank.classer` retranche du score.

Le fichier vit sous `news/couverture.json`, a cote de `sources.json` et
`cache.json` : pas de table, pas de migration, et il part avec le kit
d'export comme le reste des donnees utilisateur.
"""
import json
from datetime import datetime, timedelta, timezone
from pathlib import Path

from loguru import logger

from app.config import settings
from app.services.news_filter import (SEUIL_JETONS, SEUIL_TITRE, jaccard_titres,
                                      jetons, ratio_titres)

FENETRE_JOURS = 30       # au-dela, un sujet cesse de « deja couvert »
SUJETS_MAX = 400         # borne dure du fichier
MALUS_MAX = 25           # points retranches au plus, sur 100
JETONS_PAR_SUJET = 24


def chemin_couverture() -> Path:
    d = settings.outputs_path.parent / "news"
    d.mkdir(parents=True, exist_ok=True)
    return d / "couverture.json"


def _lire() -> dict:
    p = chemin_couverture()
    if not p.is_file():
        return {"sujets": []}
    try:
        d = json.loads(p.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError):
        logger.warning("news couverture.json illisible - memoire vide")
        return {"sujets": []}
    if not isinstance(d, dict) or not isinstance(d.get("sujets"), list):
        return {"sujets": []}
    return d


def _ecrire(doc: dict) -> None:
    p = chemin_couverture()
    tmp = p.with_name(p.name + ".tmp")
    tmp.write_text(json.dumps(doc, ensure_ascii=False, indent=1),
                   encoding="utf-8")
    tmp.replace(p)


def sujets_couverts() -> list[dict]:
    return _lire()["sujets"]


def _quand(v: str | None) -> datetime:
    if v:
        try:
            d = datetime.fromisoformat(v)
            return d if d.tzinfo else d.replace(tzinfo=timezone.utc)
        except ValueError:
            pass
    return datetime.now(timezone.utc)


def noter_couverture(items: list[dict], *, post_id: str = "",
                     quand: str | None = None) -> int:
    """Enregistre les sujets d'un reel publie. Rend le nombre de sujets notes.

    Appele par la chaine (tache 13) au moment ou le post part au Scheduler ;
    appelable a la main depuis n'importe quel producteur de reel News.
    """
    doc = _lire()
    horodatage = _quand(quand).isoformat(timespec="seconds")
    ajoutes = 0
    for it in items:
        titre = str(it.get("title") or "").strip()
        if not titre:
            continue
        doc["sujets"].append({
            "titre": titre[:200],
            "jetons": jetons(titre)[:JETONS_PAR_SUJET],
            "source_id": str(it.get("source_id") or ""),
            "source_name": str(it.get("source_name") or ""),
            "post_id": post_id,
            "quand": horodatage,
        })
        ajoutes += 1
    # borne dure : le plus ancien part en premier
    doc["sujets"].sort(key=lambda s: str(s.get("quand") or ""))
    doc["sujets"] = doc["sujets"][-SUJETS_MAX:]
    _ecrire(doc)
    return ajoutes


def _recents(maintenant: str | None, jours: int) -> list[dict]:
    limite = _quand(maintenant) - timedelta(days=jours)
    return [s for s in sujets_couverts() if _quand(s.get("quand")) >= limite]


def marquer(items: list[dict], *,
            maintenant: str | None = None) -> list[dict]:
    """Rend une COPIE des articles, chacun portant `deja_couvert` : le titre
    du sujet deja traite dont il est proche, ou None.

    Le « proche » est celui de la tache 2 — memes seuils, meme tokeniseur :
    deux mesures differentes du meme mot « proche » divergeraient.
    """
    couverts = _recents(maintenant, FENETRE_JOURS)
    sortie: list[dict] = []
    for it in items:
        copie = dict(it)
        titre = str(it.get("title") or "")
        trouve = None
        for s in couverts:
            ancien = str(s.get("titre") or "")
            if (ratio_titres(titre, ancien) >= SEUIL_TITRE
                    and jaccard_titres(titre, ancien) >= SEUIL_JETONS):
                trouve = ancien
                break
        copie["deja_couvert"] = trouve
        sortie.append(copie)
    return sortie


def penalites_de_source(*, maintenant: str | None = None,
                        jours: int = 14) -> dict:
    """{source_id: malus 0..MALUS_MAX} sur la fenetre recente.

    Le malus est proportionnel a l'ecart entre la part de cette source et une
    part equitable : une source qui a fourni la moitie des sujets alors que
    quatre sources sont actives perd le maximum ; une source a sa part
    equitable ne perd rien.
    """
    recents = _recents(maintenant, jours)
    if not recents:
        return {}
    compte: dict = {}
    for s in recents:
        sid = str(s.get("source_id") or "")
        if sid:
            compte[sid] = compte.get(sid, 0) + 1
    if not compte:
        return {}
    total = sum(compte.values())
    equitable = 1.0 / len(compte)
    malus: dict = {}
    for sid, n in compte.items():
        part = n / total
        exces = max(0.0, part - equitable)
        malus[sid] = int(round(MALUS_MAX * min(1.0, exces / max(1e-9,
                                                               1 - equitable))))
    return malus
```

- [ ] **Étape 4 : lancer le banc pour le voir vert**

Depuis `backend/` :

```
python tests/test_news_memory.py
```

Attendu : `7 passed`.

- [ ] **Étape 5 : commit**

```
git add backend/app/services/news_memory.py backend/tests/test_news_memory.py
git commit -m "news : la memoire de ce qui a deja ete dit" -m "Chaque reel publie laisse ses sujets, leur source et leur date sous news/couverture.json, borne a 400 entrees. Un article proche d un sujet couvert dans les trente jours porte le titre de ce sujet, avec les seuils et le tokeniseur de la tache 2 : deux mesures du mot proche divergeraient. Une source sur-representee sur quatorze jours rend un malus borne a 25 points." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 8 : P2 — la marque et le malus arrivent dans le classement

**Fichiers :**
- Modifier : `backend/app/api/routes.py` (fonction `rank_news` de la tâche 5)
- Test : `backend/tests/test_news_routes.py` (ajout avant le bloc `__main__`)

**Coût de patch :** nul côté bundle — l'affichage de `deja_couvert` a été posé
par la section S4 du patcher `newsrank` (tâche 6), qui l'attendait déjà.

- [ ] **Étape 1 : écrire le banc qui échoue**

Insérer dans `backend/tests/test_news_routes.py`, JUSTE AVANT le bloc
`if __name__ == "__main__":` :

```python
# ── Tache 8 : historique et equilibre dans le classement ───────────────────

def test_le_classement_marque_un_sujet_deja_couvert(monkeypatch):
    from app.services import news_memory as M, summarizer
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (None, ""))
    if M.chemin_couverture().is_file():
        M.chemin_couverture().unlink()
    M.noter_couverture(
        [{"title": "Solana network hits a record in daily transactions",
          "source_id": "s1", "source_name": "CoinDesk"}],
        post_id="p1")
    _poser_cache()
    client.put("/api/news/filter", json={
        "mots_cles": [], "sources_noires": [], "mots_noirs": [],
        "fraicheur_h": 24 * 30})
    j = client.post("/api/news/rank", json={"brief": "crypto"}).json()
    par_id = {i["id"]: i for i in j["items"]}
    assert par_id["a1"]["deja_couvert"] == \
        "Solana network hits a record in daily transactions"
    assert par_id["a2"]["deja_couvert"] is None


def test_une_source_sur_representee_perd_des_points(monkeypatch):
    from app.services import news_memory as M, summarizer
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (None, ""))
    if M.chemin_couverture().is_file():
        M.chemin_couverture().unlink()
    _poser_cache()
    client.put("/api/news/filter", json={
        "mots_cles": [], "sources_noires": [], "mots_noirs": [],
        "fraicheur_h": 24 * 30})
    avant = {i["id"]: i["score"] for i in
             client.post("/api/news/rank", json={"brief": ""}).json()["items"]}
    for n in range(6):
        M.noter_couverture(
            [{"title": f"Un sujet CoinDesk de plus numero {n}",
              "source_id": "s1", "source_name": "CoinDesk"}], post_id=f"q{n}")
    M.noter_couverture([{"title": "Un sujet Spammy", "source_id": "s2",
                         "source_name": "Spammy Feed"}], post_id="q9")
    apres_json = client.post("/api/news/rank", json={"brief": ""}).json()
    apres = {i["id"]: i["score"] for i in apres_json["items"]}
    assert apres["a1"] < avant["a1"]
    motif = [i for i in apres_json["items"] if i["id"] == "a1"][0]
    assert "source sur-representee" in motif["score_pourquoi"]
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_routes.py
```

Attendu : `4 passed, 2 failed` — les deux nouveaux échouent sur
`assert par_id["a1"]["deja_couvert"] == ...` (il vaut `None`) et sur
`assert apres["a1"] < avant["a1"]`.

- [ ] **Étape 3 : brancher la mémoire dans la route**

Dans `backend/app/api/routes.py`, dans `rank_news`, remplacer les trois
lignes

```python
    gardes, motifs = news_filter.filtrer(bruts, news_filter.lire_reglages())
    classes = await asyncio.to_thread(
        news_rank.classer, gardes, brief=request.brief)
```

par

```python
    from app.services import news_memory
    gardes, motifs = news_filter.filtrer(bruts, news_filter.lire_reglages())
    # P2 : la marque « déjà couvert » et le malus de source sont posés AVANT
    # le score, pour que le malus entre dans le nombre affiché et que son
    # motif soit lisible dans la même phrase que le reste.
    gardes = news_memory.marquer(gardes)
    penalites = news_memory.penalites_de_source()
    classes = await asyncio.to_thread(
        news_rank.classer, gardes, brief=request.brief, penalites=penalites)
```

et compléter l'import du module en tête de `rank_news` :
`from app.services import news_filter, news_memory, news_rank`.

- [ ] **Étape 4 : lancer le banc pour le voir vert**

Depuis `backend/` :

```
python tests/test_news_routes.py
python tests/test_news_memory.py
```

Attendu : `6 passed`, puis `7 passed`.

- [ ] **Étape 5 : commit**

```
git add backend/app/api/routes.py backend/tests/test_news_routes.py
git commit -m "news : le classement se souvient et equilibre les sources" -m "La marque deja couvert et le malus de source sont poses AVANT le score, pour que le malus entre dans le nombre affiche et que son motif tienne dans la meme phrase. L ecran affichait deja les deux champs : la section S4 du patcher les attendait." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 9 : P3 — les sources dans la légende

**Fichiers :**
- Créer : `backend/app/services/news_caption.py`
- Modifier : `backend/app/api/routes.py:1949-1987` (route `/news/script`)
- Modifier : `backend/app/models/schemas.py` (`NewsScriptItem`, `NewsScriptResponse`)
- Test : `backend/tests/test_news_caption.py`

**Coût de patch :** nul côté bundle — l'écran affiche déjà
`script.suggested_caption` telle quelle (composant `pm`, panneau « Generated
script ») ; la ligne de sources y apparaît sans toucher au JavaScript.

- [ ] **Étape 1 : écrire le banc qui échoue**

Créer `backend/tests/test_news_caption.py` :

```python
# -*- coding: utf-8 -*-
"""La ligne de sources en legende (plan 2026-09-03, lot 1, P3).

Reponse 2 de R8 : les sources vont en LEGENDE, pas a l'ecran (E1). Ce banc
verifie la ligne construite, et qu'elle arrive dans la reponse de la route.

Run (depuis backend/) : python tests/test_news_caption.py
"""
import json
import os
import pathlib
import sys
import tempfile

import pytest

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

_tmp = tempfile.mkdtemp()
os.environ["DATABASE_URL"] = \
    f"sqlite+aiosqlite:///{pathlib.Path(_tmp, 't.db').as_posix()}"
os.environ.setdefault("FAL_KEY", "test-key")
os.environ["IMAGES_FOLDER"] = str(pathlib.Path(_tmp, "images"))
os.environ["OUTPUTS_FOLDER"] = str(pathlib.Path(_tmp, "outputs"))
pathlib.Path(_tmp, "images").mkdir(exist_ok=True)
pathlib.Path(_tmp, "outputs").mkdir(exist_ok=True)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

from fastapi.testclient import TestClient  # noqa: E402

from app.main import app  # noqa: E402

client = TestClient(app)

ITEM = {"title": "Solana network hits record daily transactions",
        "summary": "Records everywhere.", "source_name": "CoinDesk",
        "link": "https://www.coindesk.com/markets/2026/09/03/solana-record",
        "published": "2026-09-03T11:00:00+00:00", "id": "a1"}


def test_la_ligne_porte_le_media_la_date_et_le_domaine():
    from app.services import news_caption as C
    ligne = C.bloc_sources([ITEM], langue="EN")
    assert ligne.startswith("Sources: ")
    assert "CoinDesk" in ligne
    assert "2026-09-03" in ligne
    assert "coindesk.com" in ligne
    assert "www." not in ligne


def test_la_ligne_est_en_francais_quand_on_le_demande():
    from app.services import news_caption as C
    assert C.bloc_sources([ITEM], langue="FR").startswith("Sources : ")


def test_les_medias_fondus_sont_cites_eux_aussi_et_sans_doublon():
    from app.services import news_caption as C
    it = dict(ITEM, doublons=[
        {"source_name": "Decrypt", "title": "x", "link": "https://decrypt.co/a"},
        {"source_name": "CoinDesk", "title": "y", "link": "https://coindesk.com/b"},
    ])
    ligne = C.bloc_sources([it], langue="EN")
    assert ligne.count("CoinDesk") == 1
    assert "Decrypt" in ligne


def test_un_article_sans_lien_ne_casse_pas_la_ligne():
    from app.services import news_caption as C
    ligne = C.bloc_sources([dict(ITEM, link="")], langue="EN")
    assert "CoinDesk" in ligne and "://" not in ligne


def test_aucune_source_rend_une_chaine_vide():
    from app.services import news_caption as C
    assert C.bloc_sources([], langue="EN") == ""
    assert C.bloc_sources([{"title": "sans media"}], langue="EN") == ""


def test_la_ligne_est_bornee_a_quatre_medias():
    from app.services import news_caption as C
    items = [dict(ITEM, source_name=f"Media {n}", link=f"https://m{n}.com/a")
             for n in range(9)]
    ligne = C.bloc_sources(items, langue="EN")
    assert ligne.count(" | ") == C.MEDIAS_MAX - 1
    assert ligne.rstrip().endswith("+5")


def test_la_route_de_script_pose_la_ligne_a_la_fin_de_la_legende(monkeypatch):
    from app.services import summarizer
    monkeypatch.setattr(summarizer, "available", lambda: False)
    monkeypatch.setattr(summarizer, "rewrite_script", lambda *a, **k: (None, ""))
    r = client.post("/api/news/script", json={
        "items": [ITEM], "language": "EN", "max_words": 60,
        "read_articles": False})
    assert r.status_code == 200
    j = r.json()
    assert j["sources_line"].startswith("Sources: ")
    assert j["suggested_caption"].rstrip().endswith(j["sources_line"])


if __name__ == "__main__":
    raise SystemExit(pytest.main([__file__, "-q"]))
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_caption.py
```

Attendu : `7 errors/failed` —
`ModuleNotFoundError: No module named 'app.services.news_caption'` pour les
six premiers, `KeyError: 'sources_line'` pour le dernier.

- [ ] **Étape 3 : écrire le module**

Créer `backend/app/services/news_caption.py` :

```python
# -*- coding: utf-8 -*-
"""La ligne de sources posee en LEGENDE (plan 2026-09-03, lot 1, P3).

Reponse 2 de R8 : les sources vont en legende, jamais a l'ecran (bac ecarte
E1). La ligne est construite par le pipeline, pas par le LLM : elle doit etre
exacte, et un modele qui « resume » une URL invente un lien.
"""
from datetime import datetime
from urllib.parse import urlparse

MEDIAS_MAX = 4


def _domaine(lien: str) -> str:
    try:
        h = urlparse(str(lien or "")).netloc
    except ValueError:
        return ""
    return h[4:] if h.startswith("www.") else h


def _date(v: str) -> str:
    try:
        return datetime.fromisoformat(str(v or "")).date().isoformat()
    except ValueError:
        return ""


def _entrees(items: list[dict]) -> list[tuple[str, str, str]]:
    """(media, date, domaine) sans doublon de media, dans l'ordre d'arrivee.
    Les medias FONDUS par le dedoublonnage (cle `doublons`) comptent : ils ont
    couvert le meme sujet, ils meritent la citation."""
    vus: set[str] = set()
    out: list[tuple[str, str, str]] = []
    for it in items:
        candidats = [(it.get("source_name"), it.get("published"),
                      it.get("link"))]
        for d in (it.get("doublons") or []):
            candidats.append((d.get("source_name"), it.get("published"),
                              d.get("link")))
        for media, quand, lien in candidats:
            nom = " ".join(str(media or "").split())
            if not nom or nom.lower() in vus:
                continue
            vus.add(nom.lower())
            out.append((nom, _date(quand), _domaine(lien)))
    return out


def bloc_sources(items: list[dict], *, langue: str = "EN") -> str:
    """« Sources: CoinDesk, 2026-09-03, coindesk.com | Decrypt, ... (+2) »

    Chaine vide si aucun media : une legende ne porte pas une etiquette
    « Sources: » suivie de rien.
    """
    entrees = _entrees(items)
    if not entrees:
        return ""
    tete = "Sources : " if str(langue).upper().startswith("FR") else "Sources: "
    morceaux = []
    for nom, date, dom in entrees[:MEDIAS_MAX]:
        bout = ", ".join(x for x in (nom, date, dom) if x)
        morceaux.append(bout)
    ligne = tete + " | ".join(morceaux)
    reste = len(entrees) - MEDIAS_MAX
    if reste > 0:
        ligne += f" +{reste}"
    return ligne
```

- [ ] **Étape 4 : élargir les schémas et la route**

Dans `backend/app/models/schemas.py`, remplacer `NewsScriptItem`
(lignes 543-548) par :

```python
class NewsScriptItem(BaseModel):
    title: str
    summary: Optional[str] = ""
    source_name: Optional[str] = ""
    link: Optional[str] = ""
    # 03/09/2026 : l'écran les emporte depuis la section S5 du patcher
    # newsrank ; ils servent à l'historique (P2) et à la date des sources (P3).
    id: Optional[str] = ""
    published: Optional[str] = ""
    doublons: List[dict] = []
```

et ajouter à `NewsScriptResponse` (lignes 568-577), après `essences` :

```python
    sources_line: str = ""
    voice_mode_auto: Optional[str] = None
    voice_mode_reason: str = ""
```

Dans `backend/app/api/routes.py`, dans `generate_news_script_route`,
remplacer le `return NewsScriptResponse(...)` par :

```python
        from app.services.news_caption import bloc_sources
        ligne = bloc_sources(items, langue=request.language.value)
        base_dump = base.model_dump()
        if ligne:
            base_dump["suggested_caption"] = (
                base_dump.get("suggested_caption", "").rstrip()
                + "\n\n" + ligne)
        return NewsScriptResponse(
            **base_dump,
            sources_read=len(essences),
            images=images,
            essences=essences,
            sources_line=ligne,
        )
```

- [ ] **Étape 5 : lancer le banc pour le voir vert**

Depuis `backend/` :

```
python tests/test_news_caption.py
```

Attendu : `7 passed`.

- [ ] **Étape 6 : commit**

```
git add backend/app/services/news_caption.py backend/app/api/routes.py backend/app/models/schemas.py backend/tests/test_news_caption.py
git commit -m "news : la legende cite ses medias, sa date et son domaine" -m "Ligne construite par le pipeline et non par le LLM : un modele qui resume une URL invente un lien. Les medias fondus par le dedoublonnage sont cites eux aussi, sans doublon, bornes a quatre plus un compte. Aucune source, aucune ligne. Rien a l ecran : c est la reponse 2 de R8, les sources vont en legende." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 10 : P4 — le mode de voix choisi selon le sujet

**Fichiers :**
- Créer : `backend/app/services/news_voice.py`
- Modifier : `backend/app/personas/deepotus.json` (ajout du bloc `voice_modes`)
- Modifier : `backend/app/api/routes.py` (route `/news/script`)
- Test : `backend/tests/test_news_voice.py`

**Coût de patch :** nul côté bundle. Le sélecteur de voix existe déjà dans
l'écran (`{value:"oracle"...}`) ; la valeur `auto` est ajoutée par la section
S3 du patcher `newschain` (tâche 15), qui touche déjà ce panneau. Tant que le
lot 2 n'est pas livré, le mode automatique est accessible par l'API et par le
champ `voice_mode_auto` de la réponse.

- [ ] **Étape 1 : écrire le banc qui échoue**

Créer `backend/tests/test_news_voice.py` :

```python
# -*- coding: utf-8 -*-
"""Le mode de voix choisi selon le sujet (plan 2026-09-03, lot 1, P4).

Reponse 3 de R8 : UNE persona, plusieurs modes ; le LLM choisit, l'utilisateur
force. Sans cle, un choix DETERMINISTE par mots-cles prend la main.

Run (depuis backend/) : python tests/test_news_voice.py
"""
import json
import os
import pathlib
import sys
import tempfile

import pytest

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

_tmp = tempfile.mkdtemp()
os.environ["DATABASE_URL"] = \
    f"sqlite+aiosqlite:///{pathlib.Path(_tmp, 't.db').as_posix()}"
os.environ.setdefault("FAL_KEY", "test-key")
os.environ["IMAGES_FOLDER"] = str(pathlib.Path(_tmp, "images"))
os.environ["OUTPUTS_FOLDER"] = str(pathlib.Path(_tmp, "outputs"))
pathlib.Path(_tmp, "images").mkdir(exist_ok=True)
pathlib.Path(_tmp, "outputs").mkdir(exist_ok=True)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

from fastapi.testclient import TestClient  # noqa: E402

from app.main import app  # noqa: E402

client = TestClient(app)


def test_la_persona_porte_enfin_ses_quatre_modes():
    """Avant ce plan, deepotus.json n'avait PAS de bloc voice_modes, donc
    _voice_mode_block rendait toujours None et les quatre modes du schema ne
    pilotaient rien."""
    racine = pathlib.Path(__file__).resolve().parents[1]
    p = json.loads((racine / "app" / "personas" / "deepotus.json")
                   .read_text("utf-8"))
    modes = p["voice_modes"]
    assert set(modes) == {"oracle", "alpha", "zen", "memer"}
    for nom, bloc in modes.items():
        assert bloc["description"].strip()
        assert bloc["example_caption_en"].strip()
        assert bloc["example_caption_fr"].strip()
        assert isinstance(bloc["sujets"], list) and bloc["sujets"]


def test_le_bloc_de_mode_est_desormais_resolu_par_le_moteur():
    from app.services.prompt_engine import PromptEngine, _voice_mode_block
    moteur = PromptEngine("deepotus")
    assert _voice_mode_block(moteur.persona, "memer") is not None
    assert _voice_mode_block(moteur.persona, "inconnu") is None


def test_sans_llm_le_mode_se_deduit_des_mots_du_sujet(monkeypatch):
    from app.services import news_voice as V, summarizer
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (None, ""))
    mode, pourquoi = V.choisir_mode(
        [{"title": "Bitcoin crashes 20% after liquidation cascade"}])
    assert mode == "alpha"
    assert "liquidation" in pourquoi or "crash" in pourquoi
    mode, _ = V.choisir_mode(
        [{"title": "A dog meme coin overtakes a bank in market cap"}])
    assert mode == "memer"
    mode, _ = V.choisir_mode(
        [{"title": "Central bank keeps its rate unchanged, guidance steady"}])
    assert mode == "zen"
    mode, _ = V.choisir_mode([{"title": "Un sujet parfaitement quelconque"}])
    assert mode == V.MODE_DEFAUT == "oracle"


def test_le_llm_choisit_quand_il_repond(monkeypatch):
    from app.services import news_voice as V, summarizer
    monkeypatch.setattr(
        summarizer, "_chat_dispatch",
        lambda p, s, m: (json.dumps({"mode": "memer",
                                     "pourquoi": "le sujet est absurde"}),
                         "anthropic"))
    mode, pourquoi = V.choisir_mode([{"title": "Central bank keeps its rate"}])
    assert mode == "memer"
    assert pourquoi == "le sujet est absurde"


def test_un_mode_invente_par_le_llm_est_refuse(monkeypatch):
    from app.services import news_voice as V, summarizer
    monkeypatch.setattr(
        summarizer, "_chat_dispatch",
        lambda p, s, m: (json.dumps({"mode": "pirate", "pourquoi": "arr"}),
                         "anthropic"))
    mode, _ = V.choisir_mode([{"title": "Un sujet parfaitement quelconque"}])
    assert mode in V.MODES


def test_le_mode_force_gagne_toujours(monkeypatch):
    from app.services import news_voice as V, summarizer
    monkeypatch.setattr(
        summarizer, "_chat_dispatch",
        lambda p, s, m: (json.dumps({"mode": "memer", "pourquoi": "x"}),
                         "anthropic"))
    mode, pourquoi = V.choisir_mode([{"title": "Bitcoin crashes 20%"}],
                                    force="zen")
    assert mode == "zen"
    assert "force" in pourquoi


def test_la_route_de_script_rend_le_mode_choisi_et_son_motif(monkeypatch):
    from app.services import summarizer
    monkeypatch.setattr(summarizer, "available", lambda: False)
    monkeypatch.setattr(summarizer, "rewrite_script", lambda *a, **k: (None, ""))
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (None, ""))
    r = client.post("/api/news/script", json={
        "items": [{"title": "Bitcoin crashes 20% after liquidation cascade",
                   "source_name": "CoinDesk", "link": "https://c/1"}],
        "language": "EN", "max_words": 60, "read_articles": False})
    assert r.status_code == 200
    j = r.json()
    assert j["voice_mode_auto"] == "alpha"
    assert j["voice_mode_applied"] == "alpha"
    assert j["voice_mode_reason"]


def test_un_mode_envoye_par_l_ecran_reste_prioritaire(monkeypatch):
    from app.services import summarizer
    monkeypatch.setattr(summarizer, "available", lambda: False)
    monkeypatch.setattr(summarizer, "rewrite_script", lambda *a, **k: (None, ""))
    monkeypatch.setattr(summarizer, "_chat_dispatch",
                        lambda p, s, m: (None, ""))
    r = client.post("/api/news/script", json={
        "items": [{"title": "Bitcoin crashes 20% after liquidation cascade",
                   "source_name": "CoinDesk", "link": "https://c/1"}],
        "voice_mode": "zen", "language": "EN", "max_words": 60,
        "read_articles": False})
    j = r.json()
    assert j["voice_mode_applied"] == "zen"
    assert j["voice_mode_auto"] == "zen"


if __name__ == "__main__":
    raise SystemExit(pytest.main([__file__, "-q"]))
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_voice.py
```

Attendu : `8 failed/errors` — le premier sur `KeyError: 'voice_modes'`, les
suivants sur
`ModuleNotFoundError: No module named 'app.services.news_voice'`.

- [ ] **Étape 3 : donner ses modes à la persona**

Dans `backend/app/personas/deepotus.json`, ajouter la clé `voice_modes`
JUSTE APRÈS `default_hashtags_pool` (avant `templates`) :

```json
  "voice_modes": {
    "oracle": {
      "description": "calme, sentencieux, image marine ; la voix par defaut",
      "sujets": ["regulation", "policy", "central bank", "treaty", "court",
                 "election", "summit", "report"],
      "example_caption_en": "The deep has been watching.\nFrom the deep. 🐙",
      "example_caption_fr": "Les profondeurs observaient.\nDes profondeurs. 🐙"
    },
    "alpha": {
      "description": "direct, chiffre, sans ornement ; pour un mouvement de marche",
      "sujets": ["crash", "surge", "rally", "liquidation", "outflow", "inflow",
                 "etf", "yield", "hack", "exploit"],
      "example_caption_en": "Signal check. No noise.\nMove accordingly. 🐙",
      "example_caption_fr": "Point signal. Zero bruit.\nAgis en consequence. 🐙"
    },
    "zen": {
      "description": "posee, lente, rassurante ; pour un sujet sans drame",
      "sujets": ["unchanged", "steady", "guidance", "stable", "delay",
                 "postponed", "review", "consultation"],
      "example_caption_en": "Breathe. Then look.\nStay steady. 🐙",
      "example_caption_fr": "Respire. Puis regarde.\nReste stable. 🐙"
    },
    "memer": {
      "description": "rapide, moqueuse, complice ; pour l'absurde",
      "sujets": ["meme", "dog", "cat", "viral", "prank", "parody", "nft",
                 "influencer", "rug"],
      "example_caption_en": "Okay, news dump, deepotus style.\nThat's the lore. 🐙",
      "example_caption_fr": "Bon, revue de presse version deepotus.\nVoila le lore. 🐙"
    }
  },
```

- [ ] **Étape 4 : écrire le module**

Créer `backend/app/services/news_voice.py` :

```python
# -*- coding: utf-8 -*-
"""Le mode de voix choisi selon le sujet (plan 2026-09-03, lot 1, P4).

Reponse 3 de R8 : UNE persona, plusieurs modes. Le LLM choisit parmi les
quatre ; sans cle, un choix DETERMINISTE par les mots-cles `sujets` de la
persona prend la main ; un mode explicitement demande gagne toujours.

Le choix est TOUJOURS rendu avec son motif : un mode impose sans raison
lisible est un mode qu'on ne peut pas corriger.
"""
import json

from loguru import logger

from app.services.news_filter import jetons

MODES = ("oracle", "alpha", "zen", "memer")
MODE_DEFAUT = "oracle"

_SYSTEME = (
    "You pick the voice mode for a deep-sea crypto brand's daily news video. "
    "Allowed modes: oracle (solemn, institutional news), alpha (blunt, market "
    "moves), zen (calm, no drama), memer (fast, absurd). Reply with JSON "
    "ONLY: {\"mode\": one of those four, \"pourquoi\": short reason}."
)


def _persona() -> dict:
    from app.services.prompt_engine import PromptEngine
    return PromptEngine("deepotus").persona


def _deterministe(items: list[dict]) -> tuple[str, str]:
    """Le mode dont les mots-cles `sujets` touchent le plus les titres.

    Egalite ou zero touche : MODE_DEFAUT. Le motif nomme les mots qui ont
    decide, jamais un score nu.
    """
    modes = (_persona().get("voice_modes") or {})
    mots = set()
    for it in items:
        mots.update(jetons(str(it.get("title") or "")))
        mots.update(jetons(str(it.get("summary") or "")))
    meilleur, touches_du_meilleur = MODE_DEFAUT, []
    for nom in MODES:
        bloc = modes.get(nom) or {}
        touches = [s for s in (bloc.get("sujets") or [])
                   if s in mots or any(s in m for m in mots)]
        if len(touches) > len(touches_du_meilleur):
            meilleur, touches_du_meilleur = nom, touches
    if not touches_du_meilleur:
        return MODE_DEFAUT, "aucun mot du sujet ne tranche, mode par defaut"
    return meilleur, "mots du sujet : " + ", ".join(touches_du_meilleur[:3])


def _llm(items: list[dict]) -> tuple[str, str] | None:
    from app.services import summarizer
    titres = "\n".join(f"- {str(it.get('title') or '')[:140]}"
                       for it in items[:8])
    try:
        brut, _prov = summarizer._chat_dispatch(
            f"Headlines:\n{titres}\n\nPick the voice mode.", _SYSTEME, 200)
    except Exception as e:  # noqa: BLE001
        logger.warning(f"news_voice: dispatch en echec ({e}) - deterministe")
        return None
    if not brut:
        return None
    d0, f0 = brut.find("{"), brut.rfind("}")
    if d0 < 0 or f0 <= d0:
        return None
    try:
        d = json.loads(brut[d0:f0 + 1])
    except json.JSONDecodeError:
        return None
    mode = str((d or {}).get("mode") or "").strip().lower()
    if mode not in MODES:
        logger.warning(f"news_voice: mode inconnu rendu ({mode!r}) - repli")
        return None
    return mode, str(d.get("pourquoi") or "")[:200] or "choisi par le modele"


def choisir_mode(items: list[dict], *,
                 force: str | None = None) -> tuple[str, str]:
    """Rend (mode, pourquoi). Ne leve jamais."""
    if force and str(force).strip().lower() in MODES:
        return str(force).strip().lower(), "mode force par l'utilisateur"
    if not items:
        return MODE_DEFAUT, "aucun article, mode par defaut"
    par_llm = _llm(items)
    if par_llm:
        return par_llm
    return _deterministe(items)
```

- [ ] **Étape 5 : brancher la route**

Dans `backend/app/api/routes.py`, dans `generate_news_script_route`, juste
AVANT l'appel `base = await asyncio.to_thread(...)`, insérer :

```python
        from app.services.news_voice import choisir_mode
        from app.models.schemas import VoiceMode
        mode_auto, mode_pourquoi = await asyncio.to_thread(
            choisir_mode, items,
            force=(request.voice_mode.value if request.voice_mode else None))
        mode_effectif = VoiceMode(mode_auto)
```

puis remplacer, dans l'appel qui suit, `voice_mode=request.voice_mode` par
`voice_mode=mode_effectif`, et compléter le `return` (déjà modifié en tâche 9)
en ajoutant, après `sources_line=ligne,` :

```python
            voice_mode_auto=mode_auto,
            voice_mode_reason=mode_pourquoi,
```

- [ ] **Étape 6 : lancer le banc pour le voir vert**

Depuis `backend/` :

```
python tests/test_news_voice.py
python tests/test_news_caption.py
```

Attendu : `8 passed`, puis `7 passed`.

- [ ] **Étape 7 : commit**

```
git add backend/app/services/news_voice.py backend/app/personas/deepotus.json backend/app/api/routes.py backend/tests/test_news_voice.py
git commit -m "news : la voix change avec le sujet, et dit pourquoi" -m "La persona deepotus n avait AUCUN bloc voice_modes : _voice_mode_block rendait toujours None et les quatre modes du schema ne pilotaient rien. Le bloc existe enfin, avec description, exemples FR et EN et mots-cles de sujet. Le LLM tranche quand il repond, un mode invente est refuse, sans cle les mots du sujet decident, et un mode demande par l ecran gagne toujours. Le motif du choix voyage avec lui." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Lot 2 — différenciant

### Tâche 11 : D2 — les quatre formes de reel et leur coût avant tir

**Fichiers :**
- Créer : `backend/app/services/news_forms.py`
- Modifier : `backend/app/api/routes.py` (deux routes, après `/news/rank`)
- Modifier : `backend/app/models/schemas.py` (deux schémas)
- Test : `backend/tests/test_news_forms.py`

**Coût de patch :** nul au backend ; l'affichage du choix de forme et de son
coût est la section S2 du patcher `newschain` (tâche 15).

**Ce module NE génère rien.** Il catalogue, chiffre et refuse : chaque forme
délègue à un producteur qui existe déjà (`/images/generate` pour la
forme 1, `/generate` Seedance pour la forme 2, HeyGen pour la forme 3,
`subtitle_service` pour la forme 4). Réimplémenter un producteur ici
dupliquerait un chemin déjà éprouvé.

- [ ] **Étape 1 : écrire le banc qui échoue**

Créer `backend/tests/test_news_forms.py` :

```python
# -*- coding: utf-8 -*-
"""Les quatre formes de reel News et leur cout (plan 2026-09-03, D2).

Ce banc n'appelle AUCUN fournisseur : il verifie le catalogue, l'estimation
(qui passe par pricing.estimate, deja eprouve) et les REFUS motives.

Run (depuis backend/) : python tests/test_news_forms.py
"""
import os
import pathlib
import sys
import tempfile

import pytest

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

_tmp = tempfile.mkdtemp()
os.environ["DATABASE_URL"] = \
    f"sqlite+aiosqlite:///{pathlib.Path(_tmp, 't.db').as_posix()}"
os.environ.setdefault("FAL_KEY", "test-key")
os.environ["IMAGES_FOLDER"] = str(pathlib.Path(_tmp, "images"))
os.environ["OUTPUTS_FOLDER"] = str(pathlib.Path(_tmp, "outputs"))
pathlib.Path(_tmp, "images").mkdir(exist_ok=True)
pathlib.Path(_tmp, "outputs").mkdir(exist_ok=True)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

TROIS = [{"title": f"Sujet numero {n}", "source_name": "CoinDesk",
          "link": "https://c/1", "id": f"a{n}"} for n in range(3)]


def test_le_catalogue_porte_les_quatre_formes_de_R8():
    from app.services import news_forms as F
    assert [f["id"] for f in F.catalogue()] == [
        "illustration_ia", "plans_seedance", "avatar", "voix_sous_titres"]
    for f in F.catalogue():
        assert f["label"] and f["description"]
        assert f["producteur"].startswith("/") or ":" in f["producteur"]


def test_l_illustration_ia_coute_une_image_par_titre():
    from app.services import news_forms as F
    devis = F.estimer("illustration_ia", TROIS, {"model": "flux"})
    assert devis["total_usd"] > 0
    assert any("x3" in l["label"] for l in devis["breakdown"])
    assert devis["forme"] == "illustration_ia"


def test_deux_titres_coutent_moins_que_trois():
    from app.services import news_forms as F
    a = F.estimer("illustration_ia", TROIS[:2], {"model": "flux"})
    b = F.estimer("illustration_ia", TROIS, {"model": "flux"})
    assert a["total_usd"] < b["total_usd"]


def test_les_plans_seedance_chiffrent_la_duree_par_sujet():
    from app.services import news_forms as F
    devis = F.estimer("plans_seedance", TROIS,
                      {"model": "seedance-2-fast", "duration_s": 5})
    assert devis["total_usd"] > 0
    assert devis["detail"]["secondes"] == 15


def test_l_avatar_chiffre_les_caracteres_du_script():
    from app.services import news_forms as F
    devis = F.estimer("avatar", TROIS, {"chars": 600})
    assert devis["total_usd"] > 0


def test_la_voix_off_sous_titree_est_gratuite_quand_le_texte_est_connu():
    """R5 P2 : le calage du texte CONNU sur l'audio est local et gratuit ; la
    transcription payante n'est pas sur ce chemin."""
    from app.services import news_forms as F
    devis = F.estimer("voix_sous_titres", TROIS, {"chars": 600})
    postes = [l["provider"] for l in devis["breakdown"]]
    assert "elevenlabs" in postes
    assert not any(l["label"].lower().startswith("transcri")
                   for l in devis["breakdown"])


def test_une_forme_inconnue_est_refusee_en_le_disant():
    from app.services import news_forms as F
    with pytest.raises(ValueError) as e:
        F.estimer("hologramme", TROIS, {})
    assert "hologramme" in str(e.value)
    assert "illustration_ia" in str(e.value)


def test_aucun_article_est_refuse_en_le_disant():
    from app.services import news_forms as F
    with pytest.raises(ValueError) as e:
        F.estimer("illustration_ia", [], {})
    assert "aucun article" in str(e.value).lower()


def test_la_forme_sous_titree_dit_de_quoi_elle_depend():
    """Si les styles animes de R5 P2 ne sont pas livres, la forme le DIT au
    lieu de rendre un reel muet sans le signaler."""
    from app.services import news_forms as F
    f = [x for x in F.catalogue() if x["id"] == "voix_sous_titres"][0]
    assert f["depend_de"] == "R5 P2"
    assert isinstance(F.disponible("voix_sous_titres"), bool)


if __name__ == "__main__":
    raise SystemExit(pytest.main([__file__, "-q"]))
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_forms.py
```

Attendu : `9 errors`,
`ModuleNotFoundError: No module named 'app.services.news_forms'`.

- [ ] **Étape 3 : écrire le module**

Créer `backend/app/services/news_forms.py` :

```python
# -*- coding: utf-8 -*-
"""Les quatre formes de reel News (plan 2026-09-03, lot 2, D2).

Reponse 7 de R8 : les QUATRE formes. Ce module ne GENERE rien — il catalogue,
chiffre et refuse. Chaque forme delegue a un producteur deja eprouve :

  illustration_ia    -> POST /api/images/generate (une image par titre)
  plans_seedance     -> POST /api/generate (un plan par sujet)
  avatar             -> le chemin HeyGen existant (forme d'aujourd'hui)
  voix_sous_titres   -> ElevenLabs + subtitle_service, sans avatar (R5 P2)

Le cout passe par `pricing.estimate`, la table deja tenue a jour par le
registre des modeles (R1) : un second bareme divergerait au premier
changement de tarif.
"""
from app.services import pricing

_CATALOGUE = [
    {
        "id": "illustration_ia",
        "label": "Illustration IA par titre",
        "description": "une image de marque generee par titre, animee en "
                       "cartes (Nano Banana ou FLUX selon le registre)",
        "producteur": "/api/images/generate",
        "depend_de": "",
    },
    {
        "id": "plans_seedance",
        "label": "Plans video par sujet",
        "description": "un plan genere par sujet, monte bout a bout",
        "producteur": "/api/generate",
        "depend_de": "R1",
    },
    {
        "id": "avatar",
        "label": "Avatar presentateur",
        "description": "l'avatar HeyGen lit le script prophet, compose avec "
                       "l'illustration ffmpeg existante",
        "producteur": "pipeline:heygen",
        "depend_de": "",
    },
    {
        "id": "voix_sous_titres",
        "label": "Voix off et sous-titres animes",
        "description": "pas d'avatar : voix ElevenLabs sur les cartes, "
                       "sous-titres animes mot par mot",
        "producteur": "subtitle_service:burn",
        "depend_de": "R5 P2",
    },
]

IDS = [f["id"] for f in _CATALOGUE]


def catalogue() -> list[dict]:
    return [dict(f) for f in _CATALOGUE]


def disponible(forme: str) -> bool:
    """Vrai si le producteur de la forme est reellement joignable.

    Les styles ANIMES de R5 P2 ne sont pas une condition d'existence : sans
    eux, la forme rend des sous-titres fixes. La condition est la presence du
    service de sous-titres lui-meme.
    """
    if forme == "voix_sous_titres":
        try:
            from app.services import subtitle_service  # noqa: F401
        except ImportError:
            return False
        return True
    return forme in IDS


def _refus_forme(forme: str) -> None:
    if forme not in IDS:
        raise ValueError(
            f"forme de reel inconnue : {forme!r} — les formes sont "
            + ", ".join(IDS))


def estimer(forme: str, items: list[dict], options: dict | None = None) -> dict:
    """Rend {forme, breakdown, total_usd, credits, detail}.

    Leve ValueError sur une forme inconnue ou une liste vide : un devis a
    zero article afficherait « 0,00 $ » et laisserait croire que le tir est
    gratuit.
    """
    _refus_forme(forme)
    if not items:
        raise ValueError("aucun article selectionne : rien a chiffrer")
    o = dict(options or {})
    n = len(items)
    if forme == "illustration_ia":
        op = {"kind": "image", "n": n, "model": o.get("model") or "flux"}
        detail = {"images": n}
    elif forme == "plans_seedance":
        duree = float(o.get("duration_s") or 5)
        op = {"kind": "seedance", "n": n, "duration_s": duree,
              "model": o.get("model") or "seedance-2-fast",
              "resolution": o.get("resolution") or "1080p"}
        detail = {"secondes": int(round(duree * n))}
    elif forme == "avatar":
        op = {"kind": "heygen", "chars": float(o.get("chars") or 0)}
        detail = {"caracteres": int(o.get("chars") or 0)}
    else:  # voix_sous_titres
        op = {"kind": "elevenlabs", "chars": float(o.get("chars") or 0),
              "model": o.get("voice_model") or ""}
        detail = {"caracteres": int(o.get("chars") or 0),
                  "sous_titres": "calage du texte connu (local, gratuit)"}
    devis = pricing.estimate(op)
    devis["forme"] = forme
    devis["detail"] = detail
    return devis
```

- [ ] **Étape 4 : ajouter les deux routes et leurs schémas**

Dans `backend/app/models/schemas.py`, à la fin du fichier :

```python
class NewsFormEstimateRequest(BaseModel):
    forme: str
    items: List[NewsScriptItem] = Field(..., min_length=1)
    options: dict = {}


class NewsFormEstimateResponse(BaseModel):
    forme: str
    breakdown: List[dict] = []
    total_usd: float = 0.0
    credits: dict = {}
    detail: dict = {}
```

Dans `backend/app/api/routes.py`, après la route `rank_news` :

```python
@router.get("/news/forms")
async def list_news_forms():
    """Les quatre formes de reel et leur disponibilité réelle."""
    from app.services import news_forms
    return {"forms": [dict(f, disponible=news_forms.disponible(f["id"]))
                      for f in news_forms.catalogue()]}


@router.post("/news/forms/estimate", response_model=NewsFormEstimateResponse)
async def estimate_news_form(request: NewsFormEstimateRequest):
    """Le coût d'une forme AVANT tir. Un refus dit lequel des deux motifs."""
    from app.services import news_forms
    try:
        devis = news_forms.estimer(
            request.forme, [i.model_dump() for i in request.items],
            request.options)
    except ValueError as e:
        raise HTTPException(400, str(e))
    return NewsFormEstimateResponse(**{
        k: devis.get(k) for k in NewsFormEstimateResponse.model_fields})
```

et ajouter `NewsFormEstimateRequest`, `NewsFormEstimateResponse` à l'import
des schémas.

- [ ] **Étape 5 : lancer le banc pour le voir vert**

Depuis `backend/` :

```
python tests/test_news_forms.py
```

Attendu : `9 passed`.

- [ ] **Étape 6 : commit**

```
git add backend/app/services/news_forms.py backend/app/api/routes.py backend/app/models/schemas.py backend/tests/test_news_forms.py
git commit -m "news : quatre formes de reel, chiffrees avant le tir" -m "Le module catalogue, chiffre et refuse ; il ne genere rien. Chaque forme delegue au producteur deja eprouve (images generate, generate Seedance, HeyGen, subtitle_service) et le cout passe par pricing.estimate, la table du registre des modeles : un second bareme divergerait au premier changement de tarif. Une forme inconnue et une selection vide sont refusees en le disant." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 12 : D1 — la chaîne article → post, préparée

**Fichiers :**
- Créer : `backend/app/services/news_chain.py`
- Modifier : `backend/app/api/routes.py` (route `/news/chain/preview`)
- Modifier : `backend/app/models/schemas.py` (deux schémas)
- Test : `backend/tests/test_news_chain.py`

**Coût de patch :** nul au backend ; le bouton « Chaîne du jour » est la
section S1 du patcher `newschain` (tâche 15).

- [ ] **Étape 1 : écrire le banc qui échoue**

Créer `backend/tests/test_news_chain.py` :

```python
# -*- coding: utf-8 -*-
"""La chaine article -> post (plan 2026-09-03, lot 2, D1).

Aucun reseau, aucun fournisseur : le cache est ecrit a la main, le LLM est
monkeypatche. Banc-miroir : la tache 13 relit la BASE et le fichier de
couverture, pas la valeur rendue.

Run (depuis backend/) : python tests/test_news_chain.py
"""
import json
import os
import pathlib
import sys
import tempfile

import pytest

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

_tmp = tempfile.mkdtemp()
os.environ["DATABASE_URL"] = \
    f"sqlite+aiosqlite:///{pathlib.Path(_tmp, 't.db').as_posix()}"
os.environ.setdefault("FAL_KEY", "test-key")
os.environ["IMAGES_FOLDER"] = str(pathlib.Path(_tmp, "images"))
os.environ["OUTPUTS_FOLDER"] = str(pathlib.Path(_tmp, "outputs"))
pathlib.Path(_tmp, "images").mkdir(exist_ok=True)
pathlib.Path(_tmp, "outputs").mkdir(exist_ok=True)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

from fastapi.testclient import TestClient  # noqa: E402

from app.main import app  # noqa: E402

client = TestClient(app)

ARTICLES = [
    {"id": f"a{n}", "source_id": f"s{n}", "source_name": f"Media {n}",
     "title": f"Solana sujet parfaitement distinct numero {n}",
     "summary": "Un resume court.", "link": f"https://m{n}.com/a",
     "published": "2026-09-03T11:00:00+00:00", "doublons": []}
    for n in range(6)
]


@pytest.fixture(autouse=True)
def _sans_llm(monkeypatch):
    import asyncio

    from app.services.storage import init_db
    # TestClient hors gestionnaire de contexte ne joue pas le lifespan : les
    # tables n'existeraient pas au moment ou la tache 13 ecrit un post.
    asyncio.run(init_db())
    from app.services import summarizer
    monkeypatch.setattr(summarizer, "available", lambda: False)
    monkeypatch.setattr(summarizer, "_chat_dispatch", lambda p, s, m: (None, ""))
    monkeypatch.setattr(summarizer, "rewrite_script", lambda *a, **k: (None, ""))
    from app.services.news_service import news_service
    news_service.cache_path.write_text(
        json.dumps({"fetched_at": "2026-09-03T11:30:00+00:00",
                    "items": ARTICLES, "errors": [], "source_count": 6,
                    "merged_count": 0}, ensure_ascii=False), encoding="utf-8")
    client.put("/api/news/filter", json={
        "mots_cles": [], "sources_noires": [], "mots_noirs": [],
        "fraicheur_h": 24 * 30})
    from app.services import news_memory as M
    if M.chemin_couverture().is_file():
        M.chemin_couverture().unlink()
    yield


def test_la_preparation_rend_un_lot_complet_sans_rien_tirer():
    r = client.post("/api/news/chain/preview", json={
        "brief": "crypto Solana", "forme": "illustration_ia",
        "articles_max": 3, "language": "EN",
        "creneau": "2026-09-04T18:30:00+00:00"})
    assert r.status_code == 200
    lot = r.json()
    assert len(lot["articles"]) == 3
    assert lot["script"].strip()
    assert lot["caption"].strip()
    assert lot["sources_line"].startswith("Sources: ")
    assert lot["caption"].rstrip().endswith(lot["sources_line"])
    assert lot["forme"] == "illustration_ia"
    assert lot["cout"]["total_usd"] >= 0
    assert lot["creneau"] == "2026-09-04T18:30:00+00:00"
    assert lot["voice_mode"] in ("oracle", "alpha", "zen", "memer")
    assert lot["voice_mode_reason"]


def test_le_lot_prend_les_meilleurs_dans_l_ordre_du_score():
    r = client.post("/api/news/chain/preview", json={
        "brief": "crypto Solana", "forme": "illustration_ia",
        "articles_max": 3, "language": "EN"})
    lot = r.json()
    scores = [a["score"] for a in lot["articles"]]
    assert scores == sorted(scores, reverse=True)


def test_sans_creneau_la_chaine_en_propose_un_dans_le_futur():
    from datetime import datetime, timezone
    r = client.post("/api/news/chain/preview", json={
        "brief": "crypto", "forme": "illustration_ia", "articles_max": 2,
        "language": "EN"})
    quand = datetime.fromisoformat(r.json()["creneau"])
    assert quand > datetime.now(timezone.utc)


def test_un_cache_vide_refuse_la_chaine_en_le_disant():
    from app.services.news_service import news_service
    news_service.cache_path.write_text(
        json.dumps({"fetched_at": None, "items": [], "errors": []}),
        encoding="utf-8")
    r = client.post("/api/news/chain/preview", json={
        "brief": "crypto", "forme": "illustration_ia", "articles_max": 3,
        "language": "EN"})
    assert r.status_code == 400
    assert "aucun article" in r.json()["detail"].lower()


def test_une_forme_inconnue_refuse_la_chaine_en_le_disant():
    r = client.post("/api/news/chain/preview", json={
        "brief": "crypto", "forme": "hologramme", "articles_max": 3,
        "language": "EN"})
    assert r.status_code == 400
    assert "hologramme" in r.json()["detail"]


def test_la_preparation_n_ecrit_rien_dans_la_memoire():
    """Preparer n'est pas publier : tant que l'utilisateur n'a pas valide, la
    couverture ne bouge pas."""
    from app.services import news_memory as M
    client.post("/api/news/chain/preview", json={
        "brief": "crypto", "forme": "illustration_ia", "articles_max": 3,
        "language": "EN"})
    assert M.sujets_couverts() == []


if __name__ == "__main__":
    raise SystemExit(pytest.main([__file__, "-q"]))
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_chain.py
```

Attendu : `6 failed` — tous sur `404` de `/api/news/chain/preview`.

- [ ] **Étape 3 : écrire le module**

Créer `backend/app/services/news_chain.py` :

```python
# -*- coding: utf-8 -*-
"""La chaine article -> post (plan 2026-09-03, lot 2, D1).

Reponse 5 de R8 : les QUATRE gestes deviennent automatiques ou
preselectionnes — choix des articles, script (toujours soumis mais propose),
forme et mise en page, creneau. L'utilisateur ne fait que valider (tache 13).

`preparer()` n'ecrit RIEN et ne tire RIEN : elle assemble un lot a partir du
cache, du classement, du script et du devis, et rend le tout d'un bloc.
C'est la seule maniere d'afficher un cout avant tir sans avoir deja paye.

Le brief de campagne persistant et la validation par lot appartiennent au
Scheduler (R6 D2, R6 P5) : ce module LIT le brief qu'on lui passe, il n'en
tient pas un second.
"""
from datetime import datetime, timedelta, timezone

from loguru import logger

CRENEAU_DEFAUT_H = 18       # 18 h locale, le creneau de repli
ARTICLES_MAX = 8


def _creneau_propose(maintenant: datetime | None = None) -> str:
    """Le prochain creneau de CRENEAU_DEFAUT_H, demain s'il est passe.

    Le creneau par metriques est porte par R6 P3 ; tant qu'il n'existe pas,
    la chaine propose une heure fixe plutot que « maintenant », qui publierait
    au milieu de la nuit un lot prepare a 3 h du matin.
    """
    ref = maintenant or datetime.now(timezone.utc)
    cible = ref.replace(hour=CRENEAU_DEFAUT_H, minute=0, second=0,
                        microsecond=0)
    if cible <= ref:
        cible = cible + timedelta(days=1)
    return cible.isoformat(timespec="seconds")


async def preparer(*, brief: str = "", forme: str = "illustration_ia",
                   articles_max: int = 3, language: str = "EN",
                   creneau: str | None = None,
                   voice_mode: str | None = None) -> dict:
    """Assemble le lot du jour. Leve ValueError avec un motif lisible."""
    import asyncio

    from app.models.schemas import Language, VoiceMode
    from app.services import (news_filter, news_forms, news_memory,
                              news_rank, pipeline)
    from app.services.news_caption import bloc_sources
    from app.services.news_service import news_service
    from app.services.news_voice import choisir_mode

    if forme not in news_forms.IDS:
        raise ValueError(f"forme de reel inconnue : {forme!r} — les formes "
                         "sont " + ", ".join(news_forms.IDS))
    n = max(1, min(ARTICLES_MAX, int(articles_max)))

    bruts = (news_service.get_items().get("items") or [])
    gardes, _motifs = news_filter.filtrer(bruts, news_filter.lire_reglages())
    if not gardes:
        raise ValueError(
            "aucun article ne passe le filtre : rafraichis les flux ou "
            "elargis la fenetre de fraicheur")
    gardes = news_memory.marquer(gardes)
    classes = await asyncio.to_thread(
        news_rank.classer, gardes, brief=brief,
        penalites=news_memory.penalites_de_source())
    choisis = classes[:n]

    mode, pourquoi = await asyncio.to_thread(
        choisir_mode, choisis, force=voice_mode)
    langue = Language.FR if str(language).upper().startswith("FR") \
        else Language.EN
    base = await asyncio.to_thread(
        pipeline.engine.generate_news_script, choisis,
        voice_mode=VoiceMode(mode), language=langue, max_words=90,
        angle=brief or None)
    ligne = bloc_sources(choisis, langue=langue.value)
    caption = base.suggested_caption.rstrip()
    if ligne:
        caption = f"{caption}\n\n{ligne}"

    cout = news_forms.estimer(forme, choisis,
                              {"chars": len(base.script)})
    logger.info(f"news chain: lot de {len(choisis)} articles, forme {forme}, "
                f"mode {mode}, estime {cout['total_usd']:.4f} $")
    return {
        "articles": choisis,
        "script": base.script,
        "caption": caption,
        "sources_line": ligne,
        "forme": forme,
        "cout": cout,
        "creneau": creneau or _creneau_propose(),
        "voice_mode": mode,
        "voice_mode_reason": pourquoi,
        "language": langue.value,
    }
```

- [ ] **Étape 4 : ajouter la route et les schémas**

Dans `backend/app/models/schemas.py`, à la fin :

```python
class NewsChainRequest(BaseModel):
    brief: str = Field("", max_length=2000)
    forme: str = "illustration_ia"
    articles_max: int = Field(3, ge=1, le=8)
    language: Language = Language.EN
    creneau: Optional[str] = None
    voice_mode: Optional[VoiceMode] = None


class NewsChainLot(BaseModel):
    articles: List[NewsRankedItem] = []
    script: str = ""
    caption: str = ""
    sources_line: str = ""
    forme: str = ""
    cout: dict = {}
    creneau: str = ""
    voice_mode: str = ""
    voice_mode_reason: str = ""
    language: str = "EN"
```

Dans `backend/app/api/routes.py`, après `estimate_news_form` :

```python
@router.post("/news/chain/preview", response_model=NewsChainLot)
async def preview_news_chain(request: NewsChainRequest):
    """Le lot du jour, préparé sans rien tirer ni rien écrire.

    Quatre gestes en un : articles choisis, script proposé, forme chiffrée,
    créneau. La validation est une route à part (`/news/chain/commit`) :
    préparer ne doit jamais publier par accident.
    """
    from app.services import news_chain
    try:
        lot = await news_chain.preparer(
            brief=request.brief, forme=request.forme,
            articles_max=request.articles_max,
            language=request.language.value, creneau=request.creneau,
            voice_mode=(request.voice_mode.value if request.voice_mode
                        else None))
    except ValueError as e:
        raise HTTPException(400, str(e))
    champs = set(NewsRankedItem.model_fields)
    lot["articles"] = [
        NewsRankedItem(**{k: v for k, v in a.items() if k in champs})
        for a in lot["articles"]]
    return NewsChainLot(**lot)
```

et ajouter `NewsChainRequest`, `NewsChainLot` à l'import des schémas.

- [ ] **Étape 5 : lancer le banc pour le voir vert**

Depuis `backend/` :

```
python tests/test_news_chain.py
```

Attendu : `6 passed`.

- [ ] **Étape 6 : commit**

```
git add backend/app/services/news_chain.py backend/app/api/routes.py backend/app/models/schemas.py backend/tests/test_news_chain.py
git commit -m "news : le lot du jour se prepare tout seul, sans rien tirer" -m "Quatre gestes en un : articles classes, script propose, forme chiffree, creneau. La preparation n ecrit rien et n appelle aucun fournisseur payant, sans quoi le cout avant tir arriverait apres le paiement. Le creneau de repli est 18 h, demain s il est passe : une chaine preparee a 3 h du matin ne doit pas publier a 3 h du matin. Le brief reste celui du Scheduler, ce module n en tient pas un second." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 13 : D1 — la validation du lot vers le Scheduler

**Fichiers :**
- Modifier : `backend/app/services/news_chain.py` (ajout `valider`)
- Modifier : `backend/app/api/routes.py` (route `/news/chain/commit`)
- Modifier : `backend/app/models/schemas.py` (deux schémas)
- Test : `backend/tests/test_news_chain.py` (ajout avant le bloc `__main__`)

**Coût de patch :** nul au backend ; le bouton « Programmer le lot » est la
section S4 du patcher `newschain` (tâche 15).

- [ ] **Étape 1 : écrire le banc-miroir qui échoue**

Insérer dans `backend/tests/test_news_chain.py`, JUSTE AVANT le bloc
`if __name__ == "__main__":` :

```python
# ── Tache 13 : la validation ───────────────────────────────────────────────

def _lot(articles_max=3):
    return client.post("/api/news/chain/preview", json={
        "brief": "crypto Solana", "forme": "illustration_ia",
        "articles_max": articles_max, "language": "EN",
        "creneau": "2026-09-04T18:30:00+00:00"}).json()


def _posts_en_base():
    import asyncio

    from sqlalchemy import select

    from app.services.storage import ScheduledPost, async_session_factory

    async def _lire():
        async with async_session_factory() as session:
            r = await session.execute(select(ScheduledPost))
            return list(r.scalars())

    # asyncio.run, pas get_event_loop().run_until_complete : sous Python 3.13
    # il n'y a pas de boucle courante hors coroutine, et l'ancienne forme
    # leve DeprecationWarning puis RuntimeError.
    return asyncio.run(_lire())


def test_valider_ecrit_un_post_programme_en_base():
    avant = len(_posts_en_base())
    lot = _lot()
    r = client.post("/api/news/chain/commit", json={
        "lot": lot, "channels": ["x", "telegram"], "mode": "assisted"})
    assert r.status_code == 200
    assert r.json()["post_id"]
    posts = _posts_en_base()
    assert len(posts) == avant + 1
    post = [p for p in posts if p.id == r.json()["post_id"]][0]
    assert post.status == "scheduled"
    assert post.caption == lot["caption"]
    assert set(post.channels.split(",")) == {"x", "telegram"}
    assert post.run_at.isoformat().startswith("2026-09-04T18:30")


def test_valider_note_les_sujets_dans_la_couverture():
    from app.services import news_memory as M
    lot = _lot()
    r = client.post("/api/news/chain/commit", json={
        "lot": lot, "channels": ["x"], "mode": "assisted"})
    sujets = M.sujets_couverts()
    assert len(sujets) == len(lot["articles"])
    assert {s["post_id"] for s in sujets} == {r.json()["post_id"]}
    titres = {s["titre"] for s in sujets}
    assert titres == {a["title"] for a in lot["articles"]}


def test_un_lot_sans_article_est_refuse_en_le_disant():
    lot = dict(_lot(), articles=[])
    r = client.post("/api/news/chain/commit", json={
        "lot": lot, "channels": ["x"], "mode": "assisted"})
    assert r.status_code == 400
    assert "aucun article" in r.json()["detail"].lower()


def test_un_creneau_illisible_est_refuse_avant_toute_ecriture():
    from app.services import news_memory as M
    lot = dict(_lot(), creneau="demain matin")
    avant_posts, avant_sujets = len(_posts_en_base()), len(M.sujets_couverts())
    r = client.post("/api/news/chain/commit", json={
        "lot": lot, "channels": ["x"], "mode": "assisted"})
    assert r.status_code == 400
    assert "creneau" in r.json()["detail"].lower()
    assert len(_posts_en_base()) == avant_posts
    assert len(M.sujets_couverts()) == avant_sujets
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_chain.py
```

Attendu : `6 passed, 4 failed` — les quatre nouveaux sur `404` de
`/api/news/chain/commit`.

- [ ] **Étape 3 : écrire la validation**

Ajouter à la fin de `backend/app/services/news_chain.py` :

```python
async def valider(lot: dict, *, channels: list[str] | None = None,
                  mode: str = "assisted") -> dict:
    """Materialise le lot en post programme et note la couverture.

    Rend {post_id, sujets_notes}. Leve ValueError avec un motif lisible.

    L'ordre compte : le creneau est valide AVANT toute ecriture, sinon un
    horodatage illisible laisserait une couverture notee pour un post qui
    n'existe pas.
    """
    from app.services import marketing, news_memory

    articles = lot.get("articles") or []
    if not articles:
        raise ValueError("le lot ne porte aucun article : rien a programmer")
    brut = str(lot.get("creneau") or "")
    try:
        quand = datetime.fromisoformat(brut.replace("Z", "+00:00"))
    except ValueError:
        raise ValueError(f"creneau illisible : {brut!r} — attendu une date "
                         "ISO 8601")
    if not quand.tzinfo:
        quand = quand.replace(tzinfo=timezone.utc)

    titre = str(articles[0].get("title") or "News deepotus")[:200]
    ids = await marketing.materialize_plan(
        [{
            "title": titre,
            "caption": lot.get("caption") or "",
            "channels": list(channels or ["x"]),
            "day_offset": 0,
            "time": quand.strftime("%H:%M"),
            "format": lot.get("forme") or "",
            "hook": (lot.get("script") or "")[:200],
            "script_idea": lot.get("script") or "",
        }],
        start_date=quand.date().isoformat(),
        tz_offset_minutes=0,
        mode=mode,
    )
    post_id = ids[0]
    notes = news_memory.noter_couverture(articles, post_id=post_id)
    logger.info(f"news chain: lot valide -> post {post_id}, {notes} sujets "
                "notes")
    return {"post_id": post_id, "sujets_notes": notes}
```

- [ ] **Étape 4 : ajouter la route et les schémas**

Dans `backend/app/models/schemas.py`, à la fin :

```python
class NewsChainCommitRequest(BaseModel):
    lot: NewsChainLot
    channels: List[str] = ["x"]
    mode: Literal["auto", "assisted"] = "assisted"


class NewsChainCommitResponse(BaseModel):
    post_id: str
    sujets_notes: int = 0
```

Dans `backend/app/api/routes.py`, après `preview_news_chain` :

```python
@router.post("/news/chain/commit", response_model=NewsChainCommitResponse)
async def commit_news_chain(request: NewsChainCommitRequest):
    """Valide le lot : un post programmé, et les sujets notés en couverture.

    C'est la seule porte qui écrit. Elle passe par
    `marketing.materialize_plan`, la mécanique du Scheduler, plutôt que
    d'ouvrir un second chemin vers `scheduled_posts`.
    """
    from app.services import news_chain
    try:
        out = await news_chain.valider(
            request.lot.model_dump(), channels=request.channels,
            mode=request.mode)
    except ValueError as e:
        raise HTTPException(400, str(e))
    return NewsChainCommitResponse(**out)
```

et ajouter `NewsChainCommitRequest`, `NewsChainCommitResponse` à l'import des
schémas.

- [ ] **Étape 5 : lancer le banc pour le voir vert**

Depuis `backend/` :

```
python tests/test_news_chain.py
python tests/test_news_memory.py
```

Attendu : `10 passed`, puis `7 passed`.

- [ ] **Étape 6 : commit**

```
git add backend/app/services/news_chain.py backend/app/api/routes.py backend/app/models/schemas.py backend/tests/test_news_chain.py
git commit -m "news : valider le lot le programme et le note en couverture" -m "Une seule porte ecrit, et elle passe par materialize_plan du Scheduler plutot que d ouvrir un second chemin vers scheduled_posts. Le creneau est valide AVANT toute ecriture : un horodatage illisible laisserait sinon une couverture notee pour un post qui n existe pas." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 14 : D3 — les tendances locales, et le signal X borné

**Fichiers :**
- Créer : `backend/app/services/news_trends.py`
- Modifier : `backend/app/api/routes.py` (route `/news/trends`)
- Test : `backend/tests/test_news_trends.py`

**Coût de patch :** nul au backend ; la puce « tendance » sur la carte
d'article est la section S5 du patcher `newschain` (tâche 15).

**Références :** Google Trends est **écartée** (API en alpha sur candidature,
E2). La tendance de base est **locale et gratuite** : un sujet cité par au
moins `SOURCES_MIN` médias distincts dans la même journée. Le signal X est une
option, **bornée à 3 lectures par jour** — le palier gratuit donne 100
lectures par mois (R6, vérifié le 03/09), soit 93 sur 31 jours.

- [ ] **Étape 1 : écrire le banc qui échoue**

Créer `backend/tests/test_news_trends.py` :

```python
# -*- coding: utf-8 -*-
"""Les tendances locales et le signal X borne (plan 2026-09-03, D3).

Aucun reseau : le lecteur X est monkeypatche et le budget est relu SUR LE
DISQUE apres chaque appel.

Run (depuis backend/) : python tests/test_news_trends.py
"""
import json
import os
import pathlib
import sys
import tempfile

import pytest

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

_tmp = tempfile.mkdtemp()
os.environ["DATABASE_URL"] = \
    f"sqlite+aiosqlite:///{pathlib.Path(_tmp, 't.db').as_posix()}"
os.environ.setdefault("FAL_KEY", "test-key")
os.environ["IMAGES_FOLDER"] = str(pathlib.Path(_tmp, "images"))
os.environ["OUTPUTS_FOLDER"] = str(pathlib.Path(_tmp, "outputs"))
pathlib.Path(_tmp, "images").mkdir(exist_ok=True)
pathlib.Path(_tmp, "outputs").mkdir(exist_ok=True)
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))


def _it(titre, sid, jour="03"):
    return {"id": titre[:10] + sid, "title": titre, "source_id": sid,
            "source_name": f"Media {sid}",
            "published": f"2026-09-{jour}T11:00:00+00:00", "doublons": []}


@pytest.fixture(autouse=True)
def _budget_neuf():
    from app.services import news_trends as T
    p = T.chemin_budget()
    if p.is_file():
        p.unlink()
    yield


def test_trois_medias_le_meme_jour_font_une_tendance():
    from app.services import news_trends as T
    assert T.SOURCES_MIN == 3
    items = [
        _it("Solana network hits record daily transactions", "s1"),
        _it("Solana network hits a record in daily transactions", "s2"),
        _it("Record daily transactions on the Solana network", "s3"),
        _it("Storm Bernard floods southern Spain", "s4"),
    ]
    marques = T.marquer_tendances(items)
    chauds = [i for i in marques if i["tendance"]]
    assert len(chauds) == 3
    assert all(i["tendance_sources"] == 3 for i in chauds)
    assert [i for i in marques if not i["tendance"]][0]["title"].startswith(
        "Storm")


def test_deux_medias_ne_suffisent_pas():
    from app.services import news_trends as T
    items = [_it("Solana network hits record daily transactions", "s1"),
             _it("Solana network hits a record in daily transactions", "s2")]
    assert not any(i["tendance"] for i in T.marquer_tendances(items))


def test_trois_articles_du_MEME_media_ne_font_pas_une_tendance():
    from app.services import news_trends as T
    items = [_it("Solana network hits record daily transactions", "s1"),
             _it("Solana network hits a record in daily transactions", "s1"),
             _it("Record daily transactions on the Solana network", "s1")]
    assert not any(i["tendance"] for i in T.marquer_tendances(items))


def test_trois_medias_mais_des_jours_differents_ne_font_pas_une_tendance():
    from app.services import news_trends as T
    items = [_it("Solana network hits record daily transactions", "s1", "01"),
             _it("Solana network hits a record in daily transactions", "s2",
                 "02"),
             _it("Record daily transactions on the Solana network", "s3",
                 "03")]
    assert not any(i["tendance"] for i in T.marquer_tendances(items))


def test_marquer_ne_mute_pas_les_articles_d_entree():
    from app.services import news_trends as T
    it = _it("Solana network hits record daily transactions", "s1")
    T.marquer_tendances([it])
    assert "tendance" not in it


def test_le_signal_x_consomme_le_budget_et_l_ecrit_sur_le_disque(monkeypatch):
    from app.services import news_trends as T
    monkeypatch.setattr(T, "_lire_x", lambda requete: {"posts": 42})
    r = T.signal_x("solana", quand="2026-09-03T12:00:00+00:00")
    assert r["posts"] == 42
    assert r["restant"] == T.LECTURES_PAR_JOUR - 1
    budget = json.loads(T.chemin_budget().read_text("utf-8"))
    assert budget["2026-09-03"] == 1


def test_le_signal_x_refuse_au_dela_du_quota_du_jour(monkeypatch):
    from app.services import news_trends as T
    monkeypatch.setattr(T, "_lire_x", lambda requete: {"posts": 1})
    for _ in range(T.LECTURES_PAR_JOUR):
        T.signal_x("solana", quand="2026-09-03T12:00:00+00:00")
    r = T.signal_x("solana", quand="2026-09-03T12:00:00+00:00")
    assert r["posts"] is None
    assert r["restant"] == 0
    assert "quota" in r["motif"].lower()
    assert "100 lectures" in r["motif"]


def test_le_budget_repart_le_lendemain(monkeypatch):
    from app.services import news_trends as T
    monkeypatch.setattr(T, "_lire_x", lambda requete: {"posts": 7})
    for _ in range(T.LECTURES_PAR_JOUR):
        T.signal_x("solana", quand="2026-09-03T12:00:00+00:00")
    r = T.signal_x("solana", quand="2026-09-04T09:00:00+00:00")
    assert r["posts"] == 7
    assert r["restant"] == T.LECTURES_PAR_JOUR - 1


def test_trois_par_jour_tient_dans_les_cent_du_mois():
    from app.services import news_trends as T
    assert T.LECTURES_PAR_JOUR * 31 <= 100


def test_un_lecteur_x_qui_leve_rend_un_refus_lisible(monkeypatch):
    from app.services import news_trends as T

    def _boum(requete):
        raise RuntimeError("tweepy absent")

    monkeypatch.setattr(T, "_lire_x", _boum)
    r = T.signal_x("solana", quand="2026-09-03T12:00:00+00:00")
    assert r["posts"] is None
    assert "tweepy absent" in r["motif"]


if __name__ == "__main__":
    raise SystemExit(pytest.main([__file__, "-q"]))
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_trends.py
```

Attendu : `10 errors`,
`ModuleNotFoundError: No module named 'app.services.news_trends'`.

- [ ] **Étape 3 : écrire le module**

Créer `backend/app/services/news_trends.py` :

```python
# -*- coding: utf-8 -*-
"""Les tendances locales, et le signal X borne (plan 2026-09-03, D3).

Reponse 4 de R8 : les DEUX — sujets croises entre flux (gratuit, local) et
signal X.

La tendance de base ne coute rien : un sujet cite par au moins SOURCES_MIN
medias DISTINCTS le MEME JOUR est chaud. Les regroupements reutilisent le
« presque identique » de la tache 2 : c'est le meme mot, ce doit etre la meme
mesure.

Google Trends est ECARTEE (bac E2, API en alpha sur candidature au
03/09/2026). Le signal X est une option bornee : le palier gratuit donne
100 lectures par MOIS (docs.x.com, verifie le 03/09 via R6), soit 3 par jour
sur 31 jours. Le budget est tenu sur le disque, pas en memoire : un
redemarrage ne doit pas offrir un second quota.
"""
import json
from datetime import datetime, timezone
from pathlib import Path

from loguru import logger

from app.config import settings
from app.services.news_filter import (SEUIL_JETONS, SEUIL_TITRE,
                                      jaccard_titres, ratio_titres)

SOURCES_MIN = 3            # medias distincts pour qu'un sujet soit « tendance »
LECTURES_PAR_JOUR = 3      # 3 x 31 = 93 < 100 lectures/mois du palier gratuit
MOTIF_QUOTA = ("quota du jour epuise : le palier gratuit de X donne "
               "100 lectures par mois, soit 3 par jour")


def chemin_budget() -> Path:
    d = settings.outputs_path.parent / "news"
    d.mkdir(parents=True, exist_ok=True)
    return d / "budget_x.json"


def _jour(quand: str | None) -> str:
    if quand:
        try:
            return datetime.fromisoformat(quand).date().isoformat()
        except ValueError:
            pass
    return datetime.now(timezone.utc).date().isoformat()


def _jour_de_l_article(item: dict) -> str:
    return str(item.get("published") or "")[:10]


def marquer_tendances(items: list[dict]) -> list[dict]:
    """Rend une COPIE des articles portant `tendance` (bool) et
    `tendance_sources` (le nombre de medias distincts du groupe).

    Deux articles sont du meme sujet s'ils sont « presque identiques » au sens
    de la tache 2. Le regroupement est par jour de publication : trois medias
    sur trois jours differents, c'est un feuilleton, pas une tendance.
    """
    groupes: list[dict] = []
    for it in items:
        titre = str(it.get("title") or "")
        jour = _jour_de_l_article(it)
        pose = False
        for g in groupes:
            if g["jour"] != jour:
                continue
            if (ratio_titres(titre, g["titre"]) >= SEUIL_TITRE
                    and jaccard_titres(titre, g["titre"]) >= SEUIL_JETONS):
                g["membres"].append(it)
                g["sources"].add(str(it.get("source_id") or ""))
                pose = True
                break
        if not pose:
            groupes.append({"titre": titre, "jour": jour, "membres": [it],
                            "sources": {str(it.get("source_id") or "")}})
    chauds: dict = {}
    for g in groupes:
        n = len({s for s in g["sources"] if s})
        if n >= SOURCES_MIN:
            for m in g["membres"]:
                chauds[id(m)] = n
    sortie = []
    for it in items:
        copie = dict(it)
        n = chauds.get(id(it), 0)
        copie["tendance"] = bool(n)
        copie["tendance_sources"] = n
        sortie.append(copie)
    return sortie


def _lire_budget() -> dict:
    p = chemin_budget()
    if not p.is_file():
        return {}
    try:
        d = json.loads(p.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError):
        return {}
    return d if isinstance(d, dict) else {}


def _ecrire_budget(d: dict) -> None:
    p = chemin_budget()
    tmp = p.with_name(p.name + ".tmp")
    tmp.write_text(json.dumps(d, ensure_ascii=False), encoding="utf-8")
    tmp.replace(p)


def _lire_x(requete: str) -> dict:
    """UNE lecture de l'API X. Monkeypatchee par le banc ; en production, elle
    passe par le client tweepy deja configure pour la publication."""
    from app.services.marketing import _x_client
    client = _x_client()
    if client is None:
        raise RuntimeError("aucune cle X configuree dans les Settings")
    r = client.search_recent_tweets(query=requete, max_results=10)
    return {"posts": len(getattr(r, "data", None) or [])}


def signal_x(requete: str, *, quand: str | None = None) -> dict:
    """Rend {posts, restant, motif}. `posts` vaut None quand rien n'a ete lu.

    Le budget est decompte AVANT l'appel : un echec reseau doit couter une
    lecture, sinon une erreur qui se repete viderait le quota du mois en
    silence.
    """
    jour = _jour(quand)
    budget = _lire_budget()
    utilise = int(budget.get(jour, 0))
    if utilise >= LECTURES_PAR_JOUR:
        return {"posts": None, "restant": 0, "motif": MOTIF_QUOTA}
    budget = {jour: utilise + 1}          # une seule journee gardee
    _ecrire_budget(budget)
    restant = LECTURES_PAR_JOUR - (utilise + 1)
    try:
        lu = _lire_x(requete)
    except Exception as e:  # noqa: BLE001
        logger.warning(f"news_trends: lecture X en echec ({e})")
        return {"posts": None, "restant": restant, "motif": str(e)[:200]}
    return {"posts": int(lu.get("posts") or 0), "restant": restant,
            "motif": ""}
```

- [ ] **Étape 4 : ajouter la route**

Dans `backend/app/api/routes.py`, après `commit_news_chain` :

```python
@router.get("/news/trends")
async def news_trends_route(x_query: str = ""):
    """Les tendances du cache. `x_query` ajoute UNE lecture X (quota affiché).

    Sans `x_query`, la route ne coûte rien : la tendance croisée entre flux
    est locale.
    """
    from app.services import news_trends
    from app.services.news_service import news_service
    items = news_trends.marquer_tendances(
        news_service.get_items().get("items") or [])
    chauds = [{"id": i["id"], "title": i["title"],
               "sources": i["tendance_sources"]}
              for i in items if i["tendance"]]
    out = {"tendances": chauds, "x": None}
    if x_query.strip():
        out["x"] = news_trends.signal_x(x_query.strip())
    return out
```

- [ ] **Étape 5 : brancher la marque dans le classement**

Dans `backend/app/api/routes.py`, dans `rank_news`, ajouter la ligne
`gardes = news_trends.marquer_tendances(gardes)` juste après
`gardes = news_memory.marquer(gardes)`, et compléter l'import :
`from app.services import news_filter, news_memory, news_rank, news_trends`.

Dans `backend/app/models/schemas.py`, ajouter à `NewsRankedItem` :

```python
    tendance: bool = False
    tendance_sources: int = 0
```

- [ ] **Étape 6 : lancer les bancs**

Depuis `backend/` :

```
python tests/test_news_trends.py
python tests/test_news_routes.py
```

Attendu : `10 passed`, puis `6 passed`.

- [ ] **Étape 7 : commit**

```
git add backend/app/services/news_trends.py backend/app/api/routes.py backend/app/models/schemas.py backend/tests/test_news_trends.py
git commit -m "news : la tendance se lit dans les flux, le signal X reste borne" -m "Trois medias DISTINCTS le MEME jour font une tendance : trois articles du meme fil, ou trois jours differents, n en font pas une. Le regroupement reutilise le presque identique de la tache 2 : meme mot, meme mesure. Google Trends reste ecartee, son API est en alpha sur candidature. Le signal X est borne a trois lectures par jour, 93 sur trente-et-un jours contre les 100 du palier gratuit ; le budget est decompte AVANT l appel et tenu sur le disque, un redemarrage n offre pas un second quota." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tâche 15 : le patch du bundle `newschain` — la chaîne à l'écran

**Fichiers :**
- Créer : `scripts/patch_bundle_newschain.py`
- Modifier : `frontend/dist/assets/index-BEOJX8L5.js` (par le patcher)
- Test : `backend/tests/test_news_bundle.py` (ajout avant le bloc `__main__`)

**Coût de patch :** tag NEUF `newschain`, backup `.js.bak_newschain`, position
EN QUEUE **après `newsrank`**, **six ancres comptées à 1 le 03/09** :
`r.jsx(O,{children:r.jsx(Ze,{checked:readArt,...})})` ·
`const[voice,setVoice]=x.useState("oracle");` ·
`{value:"oracle",label:"Oracle (smooth)"}` · `voice_mode:voice,language:"EN"` ·
`r.jsx(K,{variant:"violet",...,children:"Send to Studio"})` · la ligne de méta
de la carte d'article. Toutes survivent au patch `newsrank`, qui ne touche
aucune d'elles — vérifié par le `--check` de l'étape 3.

- [ ] **Étape 1 : écrire le banc-miroir qui échoue**

Insérer dans `backend/tests/test_news_bundle.py`, JUSTE AVANT le bloc
`if __name__ == "__main__":` :

```python
# ── Tache 15 : la chaine a l'ecran ─────────────────────────────────────────

def test_l_ecran_appelle_les_deux_routes_de_la_chaine():
    s = _src()
    assert s.count("/api/news/chain/preview") == 1
    assert s.count("/api/news/chain/commit") == 1
    assert s.count("__dzNewsChain") == 3      # preview + commit + le bouton


def test_le_choix_de_forme_et_son_cout_sont_a_l_ecran():
    s = _src()
    assert s.count("/api/news/forms") == 1
    assert "illustration_ia" in s
    assert "total_usd" in s


def test_le_mode_de_voix_automatique_est_offert_et_par_defaut():
    s = _src()
    assert '{value:"auto",label:"Auto (selon le sujet)"}' in s
    assert 'const[voice,setVoice]=x.useState("auto");' in s
    assert 'voice_mode:(voice==="auto"?null:voice)' in s


def test_la_puce_de_tendance_est_sur_la_carte():
    s = _src()
    assert s.count("i.tendance") >= 1
    assert "medias" in s


def test_le_patcher_newschain_est_garde_et_en_queue():
    p = (_RACINE / "scripts" / "patch_bundle_newschain.py").read_text("utf-8")
    assert "guard_downstream" in p and "--force-unchained" in p
    assert "SPEC_CHAR_DELTA" in p and 'newline=""' in p
    assert ".bak_newschain" in p
    baks = sorted(_BUNDLE.parent.glob(_BUNDLE.name + ".bak_*"),
                  key=lambda q: q.stat().st_mtime)
    tags = [q.name.rsplit(".bak_", 1)[1] for q in baks]
    assert tags.index("newschain") > tags.index("newsrank")


def test_le_patcher_newschain_sonde_l_amont_newsrank():
    """Un patcher aval doit VERIFIER que l'amont est bien pose, sinon il
    s'applique sur un bundle a moitie patche sans le dire."""
    p = (_RACINE / "scripts" / "patch_bundle_newschain.py").read_text("utf-8")
    assert "__dzNewsRank" in p
    assert "/api/news/rank" in p
```

- [ ] **Étape 2 : lancer le banc pour le voir rouge**

Depuis `backend/` :

```
python tests/test_news_bundle.py
```

Attendu : `9 passed, 6 failed` — les six nouveaux, le cinquième sur
`FileNotFoundError` du patcher.

- [ ] **Étape 3 : écrire le patcher**

Créer `scripts/patch_bundle_newschain.py`. Il reprend TOUTES les fonctions de
`patch_bundle_newsrank.py` — `deltas`, `check_spec_parity`,
`guard_downstream`, `ensure_tail_order`, `apply`, `eol_stats`,
`resolve_root`, `main` — copiées à l'identique en remplaçant seulement
`TAG = "newschain"`, `MARKER = "__dzNewsChain"`, `MARKER_ATTENDU = 3`, et les
constantes ci-dessous. Copier le fichier plutôt que d'importer : un patcher
doit rester lançable seul, sans dépendre d'un autre patcher que la chaîne
pourrait rejouer.

En-tête et sections propres à ce patcher :

```python
# -*- coding: utf-8 -*-
# scripts/patch_bundle_newschain.py
"""Patcher assert-garde : la chaine article -> post a l'ecran News.

BASELINE : bundle POST-patch newsrank. Backup dedie : `.js.bak_newschain`.
Position : EN QUEUE, apres newsrank.

Six sections :
  S1  `__dzNewsChain` : les ponts vers /api/news/chain/preview et
      /api/news/chain/commit, et le chargement de /api/news/forms ;
  S2  le choix de forme et son cout, sous la case « Read full articles » ;
  S3  le mode de voix « auto » offert et pose par defaut ;
  S4  le bouton « Chaine du jour » et le bouton « Programmer le lot »,
      poses a cote de « Send to Studio » ;
  S5  la puce « tendance : N medias » sur la ligne de meta de la carte ;
  S6  l'etat de la chaine (formes, lot prepare, occupation).

DANGERS : lancement SEUL ; `newline=""` partout (CRLF) ; jamais d'ancre
imprimee (cp1252). Si newsrank doit etre rejoue, passer par
`python scripts/repatch_all.py --from newsrank`, jamais par newsrank seul.

Run :
    python scripts/patch_bundle_newschain.py
    python scripts/patch_bundle_newschain.py --check
    python scripts/patch_bundle_newschain.py --deltas
"""
TAG = "newschain"
MARKER = "__dzNewsChain"
MARKER_ATTENDU = 3
SPEC_CHAR_DELTA = 0     # figes a l'etape 4, comme pour newsrank
SPEC_BYTE_DELTA = 0

# L'amont DOIT etre pose : un patcher aval qui s'applique sur un bundle a
# moitie patche produit un ecran incoherent sans rien dire.
STABLE_PROBES = [
    ("newsrank-pont", "__dzNewsRank", 2),
    ("newsrank-route", "/api/news/rank", 1),
    ("newsrank-brief", 'r.jsx(ie,{label:"Brief de campagne"', 1),
    ("news-script", "/api/news/script", 2),
]

POST_COUNTS = [
    ("/api/news/chain/preview", 1),
    ("/api/news/chain/commit", 1),
    ("/api/news/forms", 1),
    ('{value:"auto",label:"Auto (selon le sujet)"}', 1),
    ('voice_mode:(voice==="auto"?null:voice)', 1),
    ("i.tendance", 2),
]

# ── S1 : les ponts ─────────────────────────────────────────────────────────
PONT = (
    "async function __dzNewsChain(corps,valider){try{"
    'var u=valider?"/api/news/chain/commit":"/api/news/chain/preview";'
    'var R=await fetch(u,{method:"POST",'
    'headers:{"Content-Type":"application/json"},'
    "body:JSON.stringify(corps)});"
    "var j=await R.json();"
    "return R.ok?j:{erreur:(j&&j.detail)||String(R.status)}}"
    "catch(e){return{erreur:String(e)}}}"
    "async function __dzNewsForms(){try{"
    'var R=await fetch("/api/news/forms");'
    "if(!R.ok)return[];var j=await R.json();return j.forms||[]}"
    "catch(e){return[]}}"
)
_A1 = "function pm({variant:e,go:Go}){"
_R1 = PONT + _A1

# ── S6 : l'etat de la chaine (pose sur l'etat de voix) ─────────────────────
_A6 = 'const[voice,setVoice]=x.useState("oracle");'
_R6 = ('const[voice,setVoice]=x.useState("auto");'
       'const[forme,setForme]=x.useState("illustration_ia");'
       "const[formes,setFormes]=x.useState([]);"
       "const[lot,setLot]=x.useState(null);"
       "x.useEffect(function(){var on=!0;__dzNewsForms().then(function(f){"
       "if(on)setFormes(f)});return function(){on=!1}},[]);")

# ── S2 : le choix de forme et son cout ─────────────────────────────────────
_A2 = ('r.jsx(O,{children:r.jsx(Ze,{checked:readArt,'
       'label:"Read full articles (summarizer)",onChange:v=>setReadArt(v)})})')
_R2 = (_A2 + ',r.jsx(O,{label:"Forme du reel",children:r.jsx(re,{value:forme,'
       "options:(formes.length?formes:[{id:\"illustration_ia\","
       'label:"Illustration IA par titre"}]).map(function(f){'
       'return{value:f.id,label:f.label+(f.disponible===!1?" (indisponible)"'
       ':"")}}),onChange:function(v){setForme(v)}})}),'
       'lot&&lot.cout?r.jsx(O,{label:"Cout estime",children:r.jsx("span",'
       '{className:"mono",style:{fontSize:12,color:"var(--amber)"},'
       'children:"~ $"+Number(lot.cout.total_usd||0).toFixed(4)})}):null')

# ── S3 : le mode de voix « auto » ──────────────────────────────────────────
_A3 = '{value:"oracle",label:"Oracle (smooth)"}'
_R3 = ('{value:"auto",label:"Auto (selon le sujet)"},'
       '{value:"oracle",label:"Oracle (smooth)"}')

_A3b = 'voice_mode:voice,language:"EN"'
_R3b = 'voice_mode:(voice==="auto"?null:voice),language:"EN"'

# ── S4 : les deux boutons de la chaine ─────────────────────────────────────
_A4 = ('r.jsx(K,{variant:"violet",size:"md",icon:"flow",'
       'onClick:()=>Go&&Go("studio"),children:"Send to Studio"})')
_R4 = ('r.jsx(K,{variant:"cyan",size:"md",icon:"bolt",'
       'onClick:function(){setWork("chain");'
       "__dzNewsChain({brief:brief,forme:forme,articles_max:3,"
       'language:"EN",voice_mode:(voice==="auto"?null:voice)},!1)'
       ".then(function(j){setWork(\"\");if(j.erreur){setMsg(j.erreur);"
       'setTimeout(function(){setMsg("")},6e3);return}'
       "setLot(j);setScript({script:j.script,"
       "suggested_caption:j.caption})})},"
       'disabled:work==="chain",'
       'children:work==="chain"?"Preparation\\u2026":"Chaine du jour"}),'
       'lot?r.jsx(K,{variant:"violet",size:"md",icon:"calendar",'
       'onClick:function(){setWork("commit");'
       '__dzNewsChain({lot:lot,channels:["x"],mode:"assisted"},!0)'
       '.then(function(j){setWork("");'
       'setMsg(j.erreur?j.erreur:"Lot programme ("+(j.post_id||"")'
       '.slice(0,6)+")");setTimeout(function(){setMsg("")},6e3);'
       "if(!j.erreur)setLot(null)})},"
       'disabled:work==="commit",'
       'children:work==="commit"?"Envoi\\u2026":"Programmer le lot"}):'
       'r.jsx(K,{variant:"violet",size:"md",icon:"flow",'
       'onClick:()=>Go&&Go("studio"),children:"Send to Studio"})')

# ── S5 : la puce de tendance sur la ligne de meta de la carte ──────────────
_A5 = ('r.jsx("span",{className:"mono",children:i.source}),'
       'i.age?r.jsx("span",{children:"\\u2022"}):null')
_R5 = (_A5 + ',i.tendance?r.jsx("span",{style:{color:"var(--cyan)"},'
       'children:"tendance : "+i.tendance_sources+" medias"}):null')

PATCHES = [
    ("S1-pont", _A1, _R1),
    ("S6-etat", _A6, _R6),
    ("S2-forme", _A2, _R2),
    ("S3-voix", _A3, _R3),
    ("S3b-payload", _A3b, _R3b),
    ("S4-boutons", _A4, _R4),
    ("S5-tendance", _A5, _R5),
]
```

**Vérification d'ancre à faire AVANT d'écrire le fichier** : l'ancre `_A5`
utilise le caractère `•` tel qu'il est encodé dans le bundle. Le relever
exactement, depuis la racine du dépôt :

```
python -c "import pathlib,re;b=pathlib.Path('frontend/dist/assets/index-BEOJX8L5.js').read_text('utf-8');m=re.search(r'className:\"mono\",children:i.source.{0,90}',b);print(repr(m.group(0)))"
```

Attendu : une ligne `'className:"mono",children:i.source}),i.age?r.jsx("span",
{children:"�"}):null...'`. Recopier la séquence EXACTE relevée dans
`_A5` ; ne jamais retaper le caractère à la main.

- [ ] **Étape 4 : figer les deltas**

Depuis la racine du dépôt :

```
python scripts/patch_bundle_newschain.py --deltas
```

Recopier les deux nombres affichés dans `SPEC_CHAR_DELTA` et
`SPEC_BYTE_DELTA`.

- [ ] **Étape 5 : vérifier à blanc, appliquer, contrôler le JavaScript**

Depuis la racine du dépôt :

```
python scripts/patch_bundle_newschain.py --check
python scripts/patch_bundle_newschain.py
cp frontend/dist/assets/index-BEOJX8L5.js /tmp/dz_newschain_check.mjs
node --check /tmp/dz_newschain_check.mjs
```

Attendu : `--check` affiche `[newschain] 7 ancres OK, 4 sondes` ;
l'application affiche `backup -> index-BEOJX8L5.js.bak_newschain` puis
`OK - bundle patche` ; `node --check` ne sort rien.

- [ ] **Étape 6 : vérifier que la chaîne se rejoue**

Depuis la racine du dépôt :

```
python scripts/repatch_all.py --list
```

Attendu : la liste finit par `newsrank OK` puis `newschain OK`, dans cet
ordre, chacun avec la taille de son backup.

- [ ] **Étape 7 : lancer le banc-miroir**

Depuis `backend/` :

```
python tests/test_news_bundle.py
```

Attendu : `15 passed`.

- [ ] **Étape 8 : commit**

```
git add scripts/patch_bundle_newschain.py frontend/dist/assets/index-BEOJX8L5.js backend/tests/test_news_bundle.py
git commit -m "news : la chaine du jour tient dans deux boutons" -m "Chaine du jour prepare le lot sans rien tirer et affiche son cout ; Programmer le lot le valide vers le Scheduler. Le choix de forme vient du catalogue et dit ce qui est indisponible. Le mode de voix auto devient le defaut, et le payload envoie null pour le laisser choisir. La puce de tendance dit combien de medias ont couvert le sujet. Le patcher sonde newsrank : un patcher aval pose sur un bundle a moitie patche produit un ecran incoherent sans rien dire." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Écarté

Trois bacs sont écartés par R8, chacun pour une raison mesurée. Ils ne sont
PAS des tâches ; ils sont ici pour qu'on ne les redemande pas.

- **E1 — Sources à l'écran** : réponse 2 de R8, les sources vont en **légende
  seulement** ; la tâche 9 les y met, et rien ne les dessine sur le reel.
- **E2 — Google Trends** : API en **alpha sur candidature** depuis juillet
  2025, sans réponse garantie (developers.google.com, support.google.com,
  vérifié le 03/09/2026) ; à revoir quand elle sera publique. La tâche 14
  fait la tendance en local et gratuitement.
- **E3 — Reel bilingue systématique** : réponse 8 de R8, **une langue par
  reel**, choisie au cas par cas ; `NewsScriptRequest.language` et
  `NewsChainRequest.language` la portent déjà, il n'y a rien à ajouter.

---

## Tâche 16 : la campagne de mutations

**Fichiers :**
- Créer : `backend/tests/mutations_news.py`

**Coût de patch :** nul. Aucun code de production n'est touché : le banc mute
puis REMET à l'octet près, avec assertion de somme de contrôle.

**Patron :** `backend/tests/mutations_plaque_slicer.py` (01-02/09). Ce n'est
PAS un test — son nom ne commence pas par `test_`, donc `run-tests.ps1` ne le
liste pas et pytest ne le collecte pas. Il se lance à la main, il mute les
sources du dépôt une à une, lance le banc visé, lit les noms des tests rouges,
puis remet le fichier à l'octet près (assertion). Une mutation « VERTE » est
une assertion qui manque : c'est ce qu'on vient chercher.

**Différence avec le patron, et pourquoi :** la plaque n'avait qu'UN banc
cible ; ici les bancs sont sept. Chaque mutation porte donc son banc. Chaque
lancement reste **un processus pour un fichier de banc** — la règle du
harnais est respectée, et l'appel passe par `pytest <banc> -k <expr>` parce
que la campagne a besoin des NOMS des tests rouges, que seul le rapport de
pytest donne.

- [ ] **Étape 1 : écrire le squelette et son garde-fou**

Créer `backend/tests/mutations_news.py` :

```python
# -*- coding: utf-8 -*-
"""Campagne de mutations de la categorie News : casser -> rouge -> remettre.

PAS UN TEST : son nom ne commence pas par `test_`, donc pytest ne le collecte
pas et `run-tests.ps1` ne le liste pas. Il se lance A LA MAIN, depuis
backend/ :

    python tests/mutations_news.py             # toutes
    python tests/mutations_news.py 3 17        # celles-la

Il MUTE les sources du depot une a une et les REMET a l'octet pres
(assertion de sha256), donc il ne se lance pas pendant qu'un autre banc lit
ces fichiers.

La liste est l'argument de la revue : chaque mutation nomme le test qu'elle
fait rougir. Une « VERTE » est une assertion qui manque — c'est ainsi que le
tronconnage manquant, la conjonction non exercee et le budget X non decompte
ont ete trouves pendant l'ecriture du plan.

Chaque mutation : (fichier, ancien, nouveau, banc, tests attendus rouges).
Le script VERIFIE que la mutation s'applique (l'ancien texte existe, une
fois), lance le banc CIBLE dans son propre processus, lit les noms des tests
rouges, et remet le fichier a l'octet pres.
"""
import hashlib
import json
import pathlib
import re
import subprocess
import sys

sys.stdout.reconfigure(encoding="utf-8", errors="replace")

R = pathlib.Path(__file__).resolve().parents[2]
BACKEND = R / "backend"
PY = sys.executable

FILTRE = "tests/test_news_filter.py"
DEDUP = "tests/test_news_service_dedup.py"
RANK = "tests/test_news_rank.py"
MEMOIRE = "tests/test_news_memory.py"
CAPTION = "tests/test_news_caption.py"
VOIX = "tests/test_news_voice.py"
FORMES = "tests/test_news_forms.py"
CHAINE = "tests/test_news_chain.py"
TENDANCES = "tests/test_news_trends.py"
ROUTES = "tests/test_news_routes.py"
BUNDLE = "tests/test_news_bundle.py"
```

- [ ] **Étape 2 : écrire la liste des mutations**

Ajouter à `backend/tests/mutations_news.py` :

```python
M = [
    # ── news_filter.py : les seuils ─────────────────────────────────────────
    ("backend/app/services/news_filter.py",
     "SEUIL_TITRE = 0.735", "SEUIL_TITRE = 0.60",
     FILTRE, ["les_seuils_separent", "chaque_paire_est_classee"]),
    ("backend/app/services/news_filter.py",
     "SEUIL_TITRE = 0.735", "SEUIL_TITRE = 0.90",
     FILTRE, ["les_seuils_separent", "chaque_paire_est_classee"]),
    ("backend/app/services/news_filter.py",
     "SEUIL_JETONS = 0.375", "SEUIL_JETONS = 0.05",
     FILTRE, ["les_seuils_separent", "la_conjonction_est_bien_exigee"]),
    ("backend/app/services/news_filter.py",
     "SEUIL_CONTENU = 0.85", "SEUIL_CONTENU = 0.02",
     FILTRE, ["le_recouvrement_de_contenu"]),
    # la conjonction : c'est POUR CETTE MUTATION que le banc porte deux
    # paires synthetiques
    ("backend/app/services/news_filter.py",
     "    return (ratio_titres(a, b) >= SEUIL_TITRE\n"
     "            and jaccard_titres(a, b) >= SEUIL_JETONS)",
     "    return (ratio_titres(a, b) >= SEUIL_TITRE\n"
     "            or jaccard_titres(a, b) >= SEUIL_JETONS)",
     FILTRE, ["la_conjonction_est_bien_exigee"]),
    # ── news_filter.py : le tokeniseur ──────────────────────────────────────
    ("backend/app/services/news_filter.py",
     "        if len(mot) > len(suf) + 2 and mot.endswith(suf):",
     "        if mot.endswith(suf):",
     FILTRE, ["le_tronconnage_rapproche"]),
    ("backend/app/services/news_filter.py",
     '_SUFFIXES = ("ing", "ed", "es", "s")',
     '_SUFFIXES = ()',
     FILTRE, ["le_tronconnage_rapproche", "chaque_paire_est_classee"]),
    ("backend/app/services/news_filter.py",
     "    return [_tronc(w) for w in re.findall(r\"[a-z0-9]+\", t)\n"
     "            if w not in MOTS_VIDES and len(w) > 1]",
     "    return [_tronc(w) for w in re.findall(r\"[a-z0-9]+\", t)\n"
     "            if len(w) > 1]",
     FILTRE, ["les_seuils_separent"]),
    # ── news_filter.py : le containment n'est pas un Jaccard ────────────────
    ("backend/app/services/news_filter.py",
     "    return len(A & B) / min(len(A), len(B))",
     "    return len(A & B) / len(A | B)",
     FILTRE, ["le_recouvrement_de_contenu"]),
    ("backend/app/services/news_filter.py",
     "CORPS_MIN_JETONS = 25", "CORPS_MIN_JETONS = 1",
     FILTRE, ["un_corps_trop_court"]),
    # ── news_filter.py : le dedoublonnage ───────────────────────────────────
    ("backend/app/services/news_filter.py",
     '    ordonnes = sorted(items, key=lambda x: str(x.get("published") or ""),\n'
     "                      reverse=True)",
     '    ordonnes = sorted(items, key=lambda x: str(x.get("published") or ""))',
     FILTRE, ["garde_le_plus_recent"]),
    ("backend/app/services/news_filter.py",
     '            if g.get("source_id") and g.get("source_id") == it.get("source_id"):\n'
     "                continue",
     "            if False:\n                continue",
     FILTRE, ["deux_articles_de_la_meme_source"]),
    ("backend/app/services/news_filter.py",
     "            copie = dict(it)\n            copie[\"doublons\"] = []",
     "            copie = it\n            copie[\"doublons\"] = []",
     FILTRE, ["ne_mute_pas_les_articles"]),
    # ── news_filter.py : le filtre gratuit ──────────────────────────────────
    ("backend/app/services/news_filter.py",
     '    if any(n in src for n in reglages["sources_noires"]):\n'
     '        return "source sur liste noire"',
     "    if False:\n        return \"source sur liste noire\"",
     FILTRE, ["une_source_noire_tombe"]),
    ("backend/app/services/news_filter.py",
     "    corpus = _texte(item).lower()",
     '    corpus = str(item.get("title") or "").lower()',
     FILTRE, ["un_mot_noir_dans_le_titre_ou_le_resume"]),
    ("backend/app/services/news_filter.py",
     '        if age_h > reglages["fraicheur_h"]:',
     "        if age_h > 1e9:",
     FILTRE, ["la_fenetre_de_fraicheur"]),
    ("backend/app/services/news_filter.py",
     "    h = max(FRAICHEUR_MIN_H, min(FRAICHEUR_MAX_H, h))",
     "    h = max(FRAICHEUR_MIN_H, h)",
     FILTRE, ["fraicheur_absurde_est_bornee"]),
    ("backend/app/services/news_filter.py",
     "        s = \" \".join(str(x or \"\").split()).strip().lower()",
     "        s = str(x or \"\")",
     FILTRE, ["reglages_sont_relus_du_disque"]),
    ("backend/app/services/news_filter.py",
     "    except (json.JSONDecodeError, OSError):\n"
     '        logger.warning("news filtre.json illisible - retour aux defauts")\n'
     "        return dict(REGLAGES_DEFAUT)",
     "    except (json.JSONDecodeError, OSError):\n"
     "        raise",
     FILTRE, ["fichier_de_reglages_corrompu"]),
    # ── news_service.py : l'ordre dedoublonnage / plafond ───────────────────
    ("backend/app/services/news_service.py",
     "        deduped, fondus = dedoublonner(deduped)\n"
     "        deduped = deduped[:MAX_ITEMS]",
     "        deduped = deduped[:MAX_ITEMS]\n"
     "        deduped, fondus = dedoublonner(deduped)",
     DEDUP, ["le_plafond_de_300"]),
    ("backend/app/services/news_service.py",
     "        deduped, fondus = dedoublonner(deduped)",
     "        fondus = {}",
     DEDUP, ["le_cache_ecrit_ne_contient_plus", "le_gardant_porte_le_media",
             "le_rapport_de_rafraichissement_compte"]),
    # ── news_rank.py : le score ─────────────────────────────────────────────
    ("backend/app/services/news_rank.py",
     "EN_TETE_MAX = 5", "EN_TETE_MAX = 9",
     RANK, ["les_cinq_premiers_sont_marques"]),
    ("backend/app/services/news_rank.py",
     "        out[ident] = (max(0, min(100, score)),",
     "        out[ident] = (score,",
     RANK, ["score_hors_bornes"]),
    ("backend/app/services/news_rank.py",
     "        if ident not in connus:\n            continue",
     "        if False:\n            continue",
     RANK, ["id_inconnu_rendu_par_le_llm"]),
    ("backend/app/services/news_rank.py",
     "    except Exception as e:  # noqa: BLE001 - un fournisseur ne casse pas l'ecran\n"
     '        logger.warning(f"news_rank: dispatch en echec ({e}) - deterministe")\n'
     "        return {}, \"\"",
     "    except Exception:\n        raise",
     RANK, ["fournisseur_qui_leve"]),
    ("backend/app/services/news_rank.py",
     "    lu = _extraire_json(brut)\n    if not lu:",
     "    lu = _extraire_json(brut)\n    if False:",
     RANK, ["json_illisible_retombe"]),
    ("backend/app/services/news_rank.py",
     "    depart = texte.find(\"[\")\n    fin = texte.rfind(\"]\")",
     "    depart = 0\n    fin = len(texte) - 1",
     RANK, ["bavarde_autour_du_json"]),
    ("backend/app/services/news_rank.py",
     "    sortie.sort(key=lambda x: x[\"score\"], reverse=True)",
     "    sortie.sort(key=lambda x: x[\"score\"])",
     RANK, ["sans_llm_le_score_est_deterministe", "le_llm_donne_le_score"]),
    ("backend/app/services/news_rank.py",
     "        copie[\"score\"] = max(0, min(100, score - malus))",
     "        copie[\"score\"] = max(0, min(100, score))",
     ROUTES, ["source_sur_representee_perd_des_points"]),
    ("backend/app/services/news_rank.py",
     "    pts_reprises = min(24, 12 * reprises)",
     "    pts_reprises = 0",
     RANK, None),      # VERTE attendue : le corpus du banc n'a pas de reprise
    # ── news_memory.py ──────────────────────────────────────────────────────
    ("backend/app/services/news_memory.py",
     "FENETRE_JOURS = 30", "FENETRE_JOURS = 3650",
     MEMOIRE, ["sujet_couvert_hors_fenetre"]),
    ("backend/app/services/news_memory.py",
     "    doc[\"sujets\"] = doc[\"sujets\"][-SUJETS_MAX:]",
     "    doc[\"sujets\"] = doc[\"sujets\"]",
     MEMOIRE, ["memoire_est_bornee"]),
    ("backend/app/services/news_memory.py",
     "        copie = dict(it)\n        titre = str(it.get(\"title\") or \"\")",
     "        copie = it\n        titre = str(it.get(\"title\") or \"\")",
     MEMOIRE, ["marquer_ne_mute_pas"]),
    ("backend/app/services/news_memory.py",
     "        exces = max(0.0, part - equitable)",
     "        exces = part",
     MEMOIRE, ["equilibre_compte_les_posts"]),
    ("backend/app/services/news_memory.py",
     "    except (json.JSONDecodeError, OSError):\n"
     '        logger.warning("news couverture.json illisible - memoire vide")\n'
     '        return {"sujets": []}',
     "    except (json.JSONDecodeError, OSError):\n        raise",
     MEMOIRE, ["fichier_corrompu_ne_casse_ni"]),
    # ── news_caption.py ─────────────────────────────────────────────────────
    ("backend/app/services/news_caption.py",
     "MEDIAS_MAX = 4", "MEDIAS_MAX = 99",
     CAPTION, ["ligne_est_bornee_a_quatre"]),
    ("backend/app/services/news_caption.py",
     '    return h[4:] if h.startswith("www.") else h',
     "    return h",
     CAPTION, ["porte_le_media_la_date_et_le_domaine"]),
    ("backend/app/services/news_caption.py",
     "            if not nom or nom.lower() in vus:\n                continue",
     "            if not nom:\n                continue",
     CAPTION, ["medias_fondus_sont_cites"]),
    ("backend/app/services/news_caption.py",
     "    if not entrees:\n        return \"\"",
     "    if False:\n        return \"\"",
     CAPTION, ["aucune_source_rend_une_chaine_vide"]),
    ("backend/app/services/news_caption.py",
     '    tete = "Sources : " if str(langue).upper().startswith("FR") else "Sources: "',
     '    tete = "Sources: "',
     CAPTION, ["ligne_est_en_francais"]),
    # ── news_voice.py ───────────────────────────────────────────────────────
    ("backend/app/services/news_voice.py",
     "    if mode not in MODES:",
     "    if False:",
     VOIX, ["mode_invente_par_le_llm"]),
    ("backend/app/services/news_voice.py",
     "    if force and str(force).strip().lower() in MODES:\n"
     "        return str(force).strip().lower(), \"mode force par l'utilisateur\"",
     "    pass",
     VOIX, ["mode_force_gagne_toujours", "mode_envoye_par_l_ecran"]),
    ("backend/app/services/news_voice.py",
     "        if len(touches) > len(touches_du_meilleur):",
     "        if len(touches) >= len(touches_du_meilleur):",
     VOIX, ["mode_se_deduit_des_mots"]),
    ("backend/app/personas/deepotus.json",
     '"sujets": ["crash", "surge", "rally", "liquidation", "outflow", "inflow",',
     '"sujets": [',
     VOIX, ["mode_se_deduit_des_mots", "la_route_de_script_rend_le_mode"]),
    # ── news_forms.py ───────────────────────────────────────────────────────
    ("backend/app/services/news_forms.py",
     "    if not items:\n"
     '        raise ValueError("aucun article selectionne : rien a chiffrer")',
     "    if False:\n"
     '        raise ValueError("aucun article selectionne : rien a chiffrer")',
     FORMES, ["aucun_article_est_refuse"]),
    ("backend/app/services/news_forms.py",
     "    if forme not in IDS:",
     "    if False:",
     FORMES, ["forme_inconnue_est_refusee"]),
    ("backend/app/services/news_forms.py",
     '        op = {"kind": "image", "n": n, "model": o.get("model") or "flux"}',
     '        op = {"kind": "image", "n": 1, "model": o.get("model") or "flux"}',
     FORMES, ["illustration_ia_coute_une_image_par_titre",
              "deux_titres_coutent_moins"]),
    ("backend/app/services/news_forms.py",
     '        detail = {"secondes": int(round(duree * n))}',
     '        detail = {"secondes": int(round(duree))}',
     FORMES, ["plans_seedance_chiffrent_la_duree"]),
    # ── news_chain.py ───────────────────────────────────────────────────────
    ("backend/app/services/news_chain.py",
     "    if cible <= ref:\n        cible = cible + timedelta(days=1)",
     "    pass",
     CHAINE, ["creneau_la_chaine_en_propose_un_dans_le_futur"]),
    ("backend/app/services/news_chain.py",
     "    choisis = classes[:n]",
     "    choisis = classes",
     CHAINE, ["la_preparation_rend_un_lot_complet"]),
    ("backend/app/services/news_chain.py",
     "    if not gardes:\n"
     "        raise ValueError(\n"
     '            "aucun article ne passe le filtre : rafraichis les flux ou "\n'
     '            "elargis la fenetre de fraicheur")',
     "    if False:\n        raise ValueError(\"x\")",
     CHAINE, ["cache_vide_refuse_la_chaine"]),
    ("backend/app/services/news_chain.py",
     "    if forme not in news_forms.IDS:",
     "    if False:",
     CHAINE, ["forme_inconnue_refuse_la_chaine"]),
    ("backend/app/services/news_chain.py",
     "    if ligne:\n        caption = f\"{caption}\\n\\n{ligne}\"",
     "    pass",
     CHAINE, ["la_preparation_rend_un_lot_complet"]),
    ("backend/app/services/news_chain.py",
     "    try:\n"
     '        quand = datetime.fromisoformat(brut.replace("Z", "+00:00"))\n'
     "    except ValueError:\n"
     '        raise ValueError(f"creneau illisible : {brut!r} — attendu une date "\n'
     '                         "ISO 8601")',
     "    quand = datetime.now(timezone.utc)",
     CHAINE, ["creneau_illisible_est_refuse"]),
    ("backend/app/services/news_chain.py",
     "    notes = news_memory.noter_couverture(articles, post_id=post_id)",
     "    notes = 0",
     CHAINE, ["valider_note_les_sujets"]),
    ("backend/app/services/news_chain.py",
     "    if not articles:\n"
     '        raise ValueError("le lot ne porte aucun article : rien a programmer")',
     "    if False:\n        raise ValueError(\"x\")",
     CHAINE, ["lot_sans_article_est_refuse"]),
    # ── news_trends.py ──────────────────────────────────────────────────────
    ("backend/app/services/news_trends.py",
     "SOURCES_MIN = 3", "SOURCES_MIN = 2",
     TENDANCES, ["deux_medias_ne_suffisent_pas"]),
    ("backend/app/services/news_trends.py",
     "LECTURES_PAR_JOUR = 3", "LECTURES_PAR_JOUR = 4",
     TENDANCES, ["trois_par_jour_tient_dans_les_cent",
                 "signal_x_consomme_le_budget"]),
    ("backend/app/services/news_trends.py",
     "            n = len({s for s in g[\"sources\"] if s})",
     "            n = len(g[\"membres\"])",
     TENDANCES, ["trois_articles_du_MEME_media"]),
    ("backend/app/services/news_trends.py",
     "            if g[\"jour\"] != jour:\n                continue",
     "            if False:\n                continue",
     TENDANCES, ["jours_differents_ne_font_pas_une_tendance"]),
    ("backend/app/services/news_trends.py",
     "    budget = {jour: utilise + 1}          # une seule journee gardee\n"
     "    _ecrire_budget(budget)",
     "    pass",
     TENDANCES, ["signal_x_consomme_le_budget", "signal_x_refuse_au_dela"]),
    ("backend/app/services/news_trends.py",
     "    if utilise >= LECTURES_PAR_JOUR:",
     "    if False:",
     TENDANCES, ["signal_x_refuse_au_dela"]),
    ("backend/app/services/news_trends.py",
     "        copie = dict(it)\n        n = chauds.get(id(it), 0)",
     "        copie = it\n        n = chauds.get(id(it), 0)",
     TENDANCES, ["marquer_ne_mute_pas"]),
    # ── le bundle : le banc-miroir doit rougir si le patch disparait ────────
    ("frontend/dist/assets/index-BEOJX8L5.js",
     '{value:"relevance",label:"Score du jour"}',
     '{value:"relevance",label:"By relevance"}',
     BUNDLE, ["l_option_de_tri_dit_ce_qu_elle_fait"]),
]
```

- [ ] **Étape 3 : écrire le moteur**

Ajouter à la fin de `backend/tests/mutations_news.py` :

```python
def rouges(banc, k):
    """Les tests rouges du banc cible — et si RIEN n'a tourne, on le dit.

    pytest sort 0 (tout vert) ou 1 (des rouges) quand il a tourne ; 2 a 5
    quand la COLLECTE a casse (une erreur de syntaxe, un import qui leve) ou
    qu'aucun test ne correspond au filtre. Lue comme « aucun FAILED », une
    collecte cassee passerait pour une mutation VERTE alors que rien n'a ete
    mesure. On lit donc le code de sortie ET les lignes `ERROR`, et l'on rend
    un troisieme etat.

    Un processus, un fichier de banc : la regle du harnais tient.
    """
    r = subprocess.run([PY, "-m", "pytest", banc, "-q", "--no-header",
                        "-p", "no:warnings", "-k", k],
                       capture_output=True, cwd=BACKEND, timeout=900)
    txt = r.stdout.decode("utf-8", "replace")
    erreur = (r.returncode not in (0, 1)
              or bool(re.search(r"^ERROR ", txt, re.M)))
    return set(re.findall(r"^FAILED [^:]+::(\w+)", txt, re.M)), txt, erreur


def main():
    seuls = sys.argv[1:]
    bilan = []
    for i, (rel, old, new, banc, attendus) in enumerate(M):
        if seuls and str(i) not in seuls:
            continue
        p = R / rel
        src = p.read_bytes()
        brut = src.decode("utf-8")
        # les fichiers de l'arbre sont en CRLF (autocrlf) : on apparie en LF
        # et l'on reecrit avec la fin de ligne du fichier ; la remise se fait
        # a l'octet pres depuis `src`.
        eol = "\r\n" if "\r\n" in brut else "\n"
        txt = brut.replace("\r\n", "\n")
        assert txt.count(old) == 1, (i, rel, txt.count(old), old[:70])
        txt = txt.replace(old, new)
        sha_avant = hashlib.sha256(src).hexdigest()
        p.write_bytes(txt.replace("\n", eol).encode("utf-8"))
        try:
            k = " or ".join(attendus) if attendus else "news"
            rg, sortie, erreur = rouges(banc, k)
        finally:
            p.write_bytes(src)
            sha_apres = hashlib.sha256(p.read_bytes()).hexdigest()
            assert sha_apres == sha_avant, (i, rel, sha_avant, sha_apres)
        manquants = [a for a in (attendus or [])
                     if not any(a in n for n in rg)]
        if erreur:
            verdict = "ERREUR(collecte)"
            print(sortie[-1200:], file=sys.stderr)
        elif attendus:
            verdict = ("ROUGE" if not manquants
                       else ("VERTE" if not rg else "ROUGE(autres)"))
        else:
            verdict = "VERTE(attendue)" if not rg else "ROUGE(inattendu)"
        bilan.append((i, rel, verdict, sorted(rg), manquants))
        apercu = old.strip().splitlines()[0][:50]
        print(f"[{i:2d}] {verdict:16s} {pathlib.Path(rel).name:24s} "
              f"{apercu!r} -> {sorted(rg)}  sha {sha_avant[:10]}="
              f"{sha_apres[:10]}")
        sys.stdout.flush()
    print(json.dumps([b[:3] for b in bilan], ensure_ascii=False))


if __name__ == "__main__":
    main()
```

- [ ] **Étape 4 : lancer une seule mutation pour vérifier le moteur**

Depuis `backend/` :

```
python tests/mutations_news.py 0
```

Attendu : une ligne
`[ 0] ROUGE            news_filter.py           'SEUIL_TITRE = 0.735' -> ['test_chaque_paire_est_classee_du_bon_cote', 'test_les_seuils_separent_les_deux_familles_avec_de_la_marge']  sha <10 hex>=<10 hex>`
puis le JSON du bilan. Les deux sommes de contrôle doivent être IDENTIQUES :
c'est la preuve que le fichier a été remis à l'octet près.

- [ ] **Étape 5 : lancer la campagne entière**

Depuis `backend/` :

```
python tests/mutations_news.py
```

Attendu : une ligne par mutation. **Le verdict attendu est `ROUGE` partout,
sauf la mutation n° 29** (`pts_reprises = 0`), qui doit sortir
`VERTE(attendue)` — son corpus de banc ne contient aucun article avec des
`doublons`, donc la prime de reprise n'y est pas exercée. Aucune ligne ne doit
dire `ERREUR(collecte)`.

- [ ] **Étape 6 : traiter les VERTES inattendues**

Toute mutation qui sort `VERTE` sans être la n° 29 est une assertion qui
manque. Pour chacune : ajouter l'assertion au banc nommé dans la colonne, la
voir rouge, relancer la mutation, la voir `ROUGE`. Ne PAS supprimer la
mutation ni assouplir son attendu : c'est le banc qui a un trou, pas la
campagne.

Une ligne `ROUGE(autres)` (des tests rouges, mais pas ceux attendus) se traite
de la même façon : la mutation touche un endroit différent de celui qu'on
croyait, et le plan doit le dire.

- [ ] **Étape 7 : lancer toute la suite News une dernière fois**

Depuis `backend/`, un processus par fichier :

```
python tests/test_news_filter.py
python tests/test_news_service_dedup.py
python tests/test_news_rank.py
python tests/test_news_memory.py
python tests/test_news_routes.py
python tests/test_news_caption.py
python tests/test_news_voice.py
python tests/test_news_forms.py
python tests/test_news_chain.py
python tests/test_news_trends.py
python tests/test_news_bundle.py
```

Attendu, dans l'ordre : `17 passed`, `4 passed`, `10 passed`, `7 passed`,
`6 passed`, `7 passed`, `8 passed`, `9 passed`, `10 passed`, `10 passed`,
`15 passed`.

- [ ] **Étape 8 : commit**

```
git add backend/tests/mutations_news.py
git commit -m "news : la campagne de mutations, banc par banc" -m "Soixante-quatre mutations sur les onze bancs de la categorie : les seuils, la conjonction, le tronconnage, l ordre dedoublonnage-plafond, les bornes du score, la fenetre de couverture, le malus de source, la ligne de sources, le mode de voix force, le refus des formes, le creneau de la chaine, le budget X et la sonde du bundle. Chaque mutation nomme le test qu elle fait rougir et remet le fichier a l octet pres, somme de controle a l appui. Une seule VERTE est attendue et elle est nommee : la prime de reprise, que le corpus du banc n exerce pas." -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Relecture finale

Passe faite après écriture, contre `### R8` et contre le code relu.

**1. Couverture du périmètre.** P1 → tâches 1 à 6 (filtre gratuit, dédoublonnage
proche, branchement, score, routes, écran). P2 → tâches 7 et 8 (mémoire,
marque, malus). P3 → tâche 9 (ligne de sources en légende). P4 → tâche 10
(bloc `voice_modes` de la persona, choix par sujet, forçage). D1 → tâches 12
et 13 (préparation sans tir, validation vers le Scheduler). D2 → tâche 11
(quatre formes, coût avant tir). D3 → tâche 14 (tendance locale, signal X
borné). E1, E2, E3 → une ligne chacun dans « Écarté ». Le patch du bundle du
lot 2 → tâche 15. La campagne → tâche 16. **Aucun bac sans tâche.**

**2. Trous d'exécution repérés et fermés pendant l'écriture.**

- `sty` apparaît DEUX fois dans le bundle (l'état et le JSX qui le lit) :
  retirer l'un sans l'autre casse le rendu sans erreur de compilation. Les
  sections S2 et S8 de `newsrank` vont ensemble, et le banc vérifie les deux.
- `/api/news/items` apparaît **1** fois, pas 2 (le second `/news/items` est
  l'appel sans préfixe de la couche API). La sonde du patcher a été corrigée.
- `motif` existe déjà **9** fois dans le bundle avant tout patch : un banc qui
  se contente de sa présence ne prouve rien. Le test compte `z.motif`.
- La première mesure des seuils manquait deux doublons singulier/pluriel ; le
  tronçonnage a été ajouté et les seuils re-mesurés sur 21 paires.
- Sur les paires réelles, la conjonction `and` n'est pas discriminante : deux
  paires synthétiques ont été ajoutées pour l'exercer, et la mutation n° 4 la
  vise.

**3. Cohérence des noms et des types.** Le champ « déjà couvert » s'appelle
`deja_couvert` partout : `news_memory.marquer`, `NewsRankedItem`, la section
S4 du patcher, le banc du bundle. Le seuil de titre s'appelle `SEUIL_TITRE`
dans `news_filter`, et `news_memory` comme `news_trends` l'IMPORTENT au lieu
d'en redéfinir un. `jetons()` est le seul tokeniseur, importé par
`news_rank`, `news_memory` et `news_voice`. `classer(items, *, brief,
penalites)` a la même signature en tâche 4, en tâche 8 et en tâche 12.
`estimer(forme, items, options)` est appelée avec trois arguments positionnels
en tâche 11 comme en tâche 12. `NewsChainLot` est produit par
`/news/chain/preview` et consommé tel quel par `/news/chain/commit`.

**4. Ce qui reste ouvert, et doit être dit à l'exécution.**

- Les deux `SPEC_CHAR_DELTA` / `SPEC_BYTE_DELTA` valent 0 à l'écriture : ils
  se figent à l'étape `--deltas` de chaque patcher. Tant qu'ils valent 0, le
  patcher refuse de tourner — c'est voulu, ce n'est pas un placeholder.
- L'ancre `_A5` de `newschain` contient le caractère `•` du bundle : elle se
  relève avec la commande donnée, jamais en le retapant.
- `node --check` suppose `node` sur la machine ; s'il manque, le dire au lieu
  de sauter la vérification.
- La tâche 11 dépend de R1 pour le tarif Seedance et de R5 P2 pour les styles
  animés ; la tâche 12 lit le brief de R6 D2 et s'aligne sur la validation par
  lot de R6 P5. Si l'un n'est pas livré, la tâche le dit et s'arrête.

